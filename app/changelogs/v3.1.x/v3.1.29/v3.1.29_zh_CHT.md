## 概述

該版本改進了一些細節。

重要變更：列表 Markdown 符號從 `*` 變更為 `-`（#14680）

**影響範圍**：

1. 複製或匯出列表 Markdown 時符號變更為 `-`
2. 依賴列表 `*` 符號的 SQL 查詢語句或外掛程式需調整邏輯

不影響輸入，輸入仍可使用 `*` 符號。

**適配操作**：

1. 執行 `文檔樹` - `更多` - `重建索引` 以更新數據索引中的列表符號
2. 修改相關的 SQL 查詢語句
   * 僅支援新格式：例如將 `markdown LIKE '* [x]%'` 改為 `markdown LIKE '- [x]%'` 
   * 相容新舊格式：例如改為 `markdown LIKE '- [x]%' OR markdown LIKE '* [x]%'`

## 變更記錄

以下是此版本中的詳細變更。

### 改進功能

* 預覽模式複製到微信公眾號支援兩層以上列表
* 支援匯出至微信公眾號時渲染公式
* 改進浮動視窗互動
* 支援透過快捷鍵切換程式碼塊語言
* 流程圖與 PlantUML 圖表未完全顯示
* 文件標題中的 `/` 自動替換為 `/`
* 支援版面搜尋過濾
* 改進行級元素的過渡效果
* `複製` 後選取塊
* 支援配置是否匯出行級備註
* 改進貼上超連結和塊引用錨文本
* 改進全域搜尋中的 SQL 方法
* 點擊虛擬引用跳轉時 Android 上不再彈出鍵盤
* 插入多個資源檔案時使用自然升序
* 改進滑鼠選擇程式碼塊
* 日記快捷鍵自動重設為預設值
* 改進行動端塊跳回動畫
* 改進瀏覽器剪藏擴充功能
* 改進換行後嵌入塊的渲染
* 改進嵌入塊中程式碼塊的互動
* 匯出 Markdown 時使用 `-` 作為列表標記
* 改進市場刷新計時器
* 改進閃卡管理排序
* 改進 HTML 表格剪藏
* 支援導入 Markdown 時轉換 HTML 超連結
* 複製貼上行級元素時忽略編輯器 Markdown 設定
* 改進匯出 PDF 水印字體
* 支援匯出 PDF 時塊內分頁
* 改進段落開頭行級元素前的 Tab 字元輸入
* 當塊引用錨文本僅包含空白字元時設定為靜態錨文本 ID
* 改進塊引用導出
* 匯出 PDF 時頁面縮放下改進程式碼塊行號對齊
* 改進列表中表格後的回車換行
* 改進資料庫範本欄位存取匯總多選
* 複製資料庫範本欄位時將範本填入輸入框
* 點選大綱後遊標定位到塊末
* 「關閉目前頁籤」快速鍵可關閉浮窗
* 匯入 Markdown 時遵循編輯器 Markdown 語法設定
* 改進資料庫欄位和視圖編輯介面
* 改進程式碼塊本地語言渲染優先權
* 行動端返回時更新「退出聚焦」按鈕狀態
* 移除資料庫標題中的 `<br>`
* 改進文檔樹名稱字母排序
* 改進資料庫批次貼上效能並支援更多類型的轉換
* 改進塊屬性更新的穩定性
* 瀏覽器剪藏擴充支援範本配置
* 改良末軟換行

### 修復缺陷

* 行動端點擊影片會跳到遊標所在的塊
* 程式碼塊中按 Delete 鍵時出現例外狀況
* 行級公式輸入解析異常
* PlantUML 匯出圖片錯誤
* 塊級公式在預覽模式下無法渲染
* 基於選擇列的總列篩選無效
* 行動端搜尋替換後結果列表未刷新
* 固定表頭導致多選儲存格高亮錯位

### 開發重構

* 升級至 Electron v35.3.0

### 開發者

* 改進內核介面 `/api/file/putFile` 參數校驗

## 下載

* Scribli contributors
* GitHub
