## v2.8.3 / 2023-04-04

### Enhancement

* PDF export supports setting footer template
* Improve the style of the slash menu on the mobile
* Slide and pull out the left and right panel on the mobile
* macOS support `⌥[` to enter default characters when no text is selected
* Improve virtual reference split words
* Remove the footer `Export by Scribli` when exporting image and PDF 
* Android check WebView version 75+
* Flashcard new/review card limit support configure to 0
* Remove `/` or `、` when trigger AI Chat
* Support scaling when exporting pdf preview
* Browser web clipper extension show tip while clipping images
* Improve the menu style of the doc tree on the mobile
* After opening the doc on the mobile, the editor is no longer focused to avoid the pop-up of the soft keyboard
* Spaced repetition supports full screen mode
* Create a new document from the search field
* Prompt to search for the most recently updated block when the search keyword is empty
* Add interaction tips at the bottom of the search dialog
* Search replace generate file history
* The merged cells of the table no longer add a space at the end

### Bugfix

* PDF annotation do not follow zoom
* Data synchronization online check not using configured endpoint
* API getFile/putFile does not perform read-write lock
* Focused heading block export does not contain its following blocks
* Related PDF annotation information is not exported when exporting .sy.zip
* Markdown hyperlink parsing exception when using API `/api/block/*` to upsert block
* Exporting PDF hangs after focusing heading block
* The editor has not been closed after closing notebook or deleting doc on the mobile
* The search fails in REGEXP/SQL with order by relevance
* Group by Document Replace exceptions in some cases
* Escape character `\` were not handled correctly when importing Markdown
* Click search from the doc tree, pressing `Del` in the editor will delete the doc
* Can't choose scale in PDF

### Refactor

* Improve copy file stability
* Upgrade PDF.js

### Development

* Attribute View refresh data table queue

## v2.8.2 / 2023-03-30

### Enhancement

* Update the number of flashcards after removing the flashcards
* Opening a PDF with a new window requires positioning and the positioning fails when opening it for the first time
* Use `Endpoint` for network connectivity checks when syncing
* The current page number needs to be kept after the PDF is opened in a new window
* Browser web clipper support tag `<img>` attribute `srcset`
* Characters are lost when referenced in a block containing `))`
* Move some buttons to more menu in PDF tab
* The reminder message will no longer pop up when the subscription expires for more than 2 days
* PDF export supports setting margins
* Adjusted the YFM style with widget export
* Add recent documents on mobile

### Bugfix

* Incorrect path when entering flashcard management from document tree
* The document tag delete button is covered by the floating layer
* An exception occurs when exporting HTML and PDF
* Kernel panic on some systems on Windows
* After entering `\` in the table, the next column is merged incorrectly
* Rename tag in new window, prompt won't disappear
* After the document is transferred to the heading, occurs two copies of the reference block
* Wrong parsing virtual reference with `\` before it
* Parsing and rendering YFM incorrectly when exporting widget block attribute `data-export-md`
* Recent docs not updated after renaming

## v2.8.1 / 2023-03-28

### 改进功能

* 再次打开已在新窗口中存在的页签时不在主窗口中打开
* 移动端左右侧栏全屏和点击区域优化
* 闪卡支持设置新卡上限
* 闪卡支持设置是否启用列表块制卡
* 闪卡支持设置是否启用超级块制卡
* 闪卡支持设置复习上限
* 支持应用内配置人工智能
* 支持配置闪卡使用卡包制卡
* 导出 Markdown 时在文档头添加 YFM 开关
* 为收集箱移动笔记本添加图标及转义处理
* 粘贴块时需移除其闪卡属性
* 用鼠标划选文本 `Ctrl+C` 后再松开鼠标不应选中块
* Graphviz 中指定 shape 后无法添加填充色
* 改进导入 data.zip 和 .sy.zip 校验失败时的提示
* 新窗口界面独立临时缩放
* 支持配置反链面板提及默认展开数
* 数据同步忽略用于文件系统检查的文件
* 改进搜索相关细节
* 支持应用内删除模板
* macOS 端对工作空间放置在 iCloud 路径下做检查
* 移动端菜单打开的面板关闭后返回到上一层菜单
* 搜索中的命名查询移到输入框下
* 挂件导出属性 `data-export-md` 支持 YFM
* 代码块支持设置非高亮语言
* 更新部分关闭/移除图标为圆形
* 闪卡管理添加复习次数、卡片总数及路径优化
* 删除文档时提示删除数量
* 内容块人工智能自定义操作持久化
* 提供列出文件 API
* 搜索分组文档颜色调整及块缩进
* 提供 Sprig 模板渲染 API
* 改进 Windows 端第三方同步盘检测
* 升级 abc.js 至 6.2.0
* 增加 kernel.log
* 闪卡支持设置是否启用标记制卡
* 拖拽调整表格宽度应不受单元格宽度影响

### 修复缺陷

* 使用快捷键制卡时不应该将光标处和所选块同时制卡
* 文档树自定义排序拖拽不稳定
* 移动端缩放后会显示退出聚焦
* 块标拖拽到闪卡块上时无定位线条
* 桌面端设置访问授权后退出应用时抛出异常
* 浏览器扩展无法获取微信公众号的图片文章
* 重启后窗口大小恢复默认问题
* 多个 Mermaid mindmap 无法渲染
* 嵌入块 SQL 脚本中存在换行时无法导出
* 集市更新获取不同步的问题

### 开发重构

* 改变非移动端前端编译目标为 `es2021`

## v2.8.0 / 2023-03-21

### 引入特性

* 支持保存和加载不同的界面布局

### 改进功能

* 属性视图增减列
* 属性视图增减行
* 块菜单、编辑器更多菜单等全屏菜单样式优化
* 移动端上架 vivo 应用商店
* 移动端支持点备注打开属性设置界面
* 菜单显示不下时加入滚动条
* 移动端设置二级页面滑动返回上一层
* 不支持配置坚果云 WebDAV 进行同步
* PDF 矩形标注支持默认颜色
* 移动端属性设置界面优化
* 属性设置对话框中不再显示 `riff-decks`
* 快速制卡后属性顺序应保持不变
* 数据同步加入系统时间校验
* 调整输入框和下拉框样式
* 改进闪卡下一轮重复时间显示精度
* 更新加减号图标大小及对齐 `添加到卡包` 弹窗中的图标
* 快捷键设置界面优化
* 面包屑第一项如果显示文档名则不进行省略
* 提供删除文件 API
* 移动端输入不触发斜杆菜单
* 导入 Markdown 时不自动转换超链接
* 使用第三方同步盘时弹出提示并退出内核
* 添加 PDF 快捷键说明及显示
* 新增当前位置至已加载内容页头/页尾快捷键
* 调整编辑器右侧边距
* 改进动态加载判断
* 闪卡复习时支持跳过
* 编辑器处于只读模式时启动后提示用户
* 文件系统读取或写入失败时退出内核
* 调整 Windows 端安装和卸载程序侧栏图片
* 导出 PDF 时代码块强制换行
* 对齐页签下拉选择和文档的菜单按钮
* Pad 端支持思源协议 `scribli://` 跳转打开
* Windows 端安装脚本检测是否存在工作空间数据
* Windows 端安装包支持自定义安装路径
* 删除大量内容后光标依旧显示在可视区域内
* 为插入内容前后的滚动添加动画效果

### 开发重构

* 升级 Pandoc

### 修复缺陷

* 移动端较老 WebView 版本启动后卡在加载界面
* 在容器块子块为空时 `Ctrl+/` 弹出块菜单功能失效
* 块引锚文本导出时 `&` 变为实体 `&amp;`
* Android 端某些系统上返回桌面后再进入白屏
* 移动端搜索框显示 `undefined`
* PDF 页签打开进度和白边问题
* 文件名包含 `.` 时的重命名问题
* PlantUML 无法导出图片
* 导出 PDF 时显示行号的代码块宽度不正确
* 打开某些 PDF 文件时缺失部分内容
* 快捷键复制嵌入块为纯文本无效
* 打开多个工作空间和多个新窗口时退出未关闭关联窗口
* 表格单元格内存在多行时末尾输入转义符 `\` 导致 `<br />` 暴露

## v2.7.10 / 2023-03-14

### 改进功能

* 浏览器端不显示 `退出应用`
* 升级 Mermaid 以支持 timeline 和 mindmap
* 表格和有内容的段落块中插入多个文件时不进行分块
* 点击搜索项将依次高亮该结果中的多个关键字
* 表格中插入图片时偶尔会多一个空行
* 移动端软键盘顶部工具栏遮挡编辑位置
* 反链提及搜索范围增加命名、别名和备注
* 人工智能插入公式块、代码块和图表块后自动进行渲染
* 代码片段设置界面用户体验优化
* 部分图标改进
* Linux 端打开多工作空间托盘无法关闭
* 表格块写入数据库表时在单元格之间添加空格

### 移除功能

* 移除 设置 - 搜索 - 虚拟引用 - 关键字数量限制

### 修复缺陷

* 使用 Markdown 标记符嵌套行级元素后被还原为纯文本
* 闪卡管理分页失效
* 集市包更新下载进度显示问题
* 搜索中指定路径选为文档时搜索结果不包含子文档

## v2.7.9 / 2023-03-10

### 改进功能

* 加入针对内容块的人工智能辅助支持
* 鼠标划选没有选中块时不应划选所有块
* Android 端长按弹出内容操作菜单后底栏需滑动
* 移动端底部工具栏移除删除按钮
* 改进任务列表项完成后的样式
* Android 端块菜单返回支持左右滑动
* OpenAI API 接入增加环境变量 `Scribli_OPENAI_API_BASE_URL`
* 批量插入文件时每个文件占一个块
* 鼠标移动到页签上时 `&` 不显示为 `&amp;`
* 移动端复习过闪卡后顶部需出现同步按钮
* 引用搜索命名空格未显示
* 改进内核只读模式

### 修复缺陷

* 斜杆菜单缺失 `AI Chat`
* Android 端某些系统上界面显示不全
* 移动端块引用、嵌入无法使用
* 图片无法设定宽度百分比
* 浏览器剪藏扩展丢失部分元素

## v2.7.8 / 2023-03-07

### 改进功能

* 移动端编辑器悬浮工具栏移到底部
* 移动端键盘工具栏上添加块标菜单功能
* 隐藏 iPhone 键盘顶部工具栏
* 编辑引擎添加属性视图节点
* 全屏后需更新块标位置
* 优化动态加载没有标题时的大纲高亮
* 选中多行时浮动工具栏不遮挡第一行
* 云端收集箱剪藏超时调整为 30s
* 持久化属性视图
* 移动端根据不同的输入场景对输入工具栏进行切换
* 移动端根据当前块的类型调整输入工具栏中对应的操作按钮
* 支持编辑/只读模式在浏览器和桌面应用中保持一致
* 桌面端接入 OpenAI Chat API 
* 粘贴图片后图片宽度会被挤压
* 移动端斜杠菜单移到底部
* 改进判断工作空间路径实现
* 合并超级块时自动刷新包含的嵌入块
* 移动端数据同步提示改进
* 移动端数据历史图标和桌面端保持一致
* 移动端键盘工具栏上添加面包屑更多菜单功能
* 搜索结果被高亮分割的同一行内元素应进行合并
* 搜索结果内输入字符不应进行分割
* 桌面端禁止自动播放多媒体
* 低于 Windows 10 安装时弹出警告
* 只读模式下支持刷新和全屏快捷键操作

### 开发重构

* 使用 esbuid-loader 提高前端代码的编译速度

### 修复缺陷

* 导入 Markdown 文件夹后无法在其中继续导入
* 更新版本后自动打开最新版的用户指南重复
* 关闭所有页签后点击后退无法重新打开页签
* 正则表达式搜索失效
* 无法在表格中的引用前后加入空格
* 表格中有图片时插入图片链接定位错误
* 关系图日记过滤失效
* 行级代码选中 `Ctrl+G` 取消后前方出现空代码元素
* PDF 导出预览点击资源文件链接白屏
* 关闭页签再点击后退缩放状态错误
* 有图片的表格拖拽后列宽异常
* 移动端基于文档的间隔重复被文档树遮挡
* 超级块内递归嵌入的嵌入块不应该显示面包屑
* 搜索结果被高亮隔断后加粗导致内容丢失

## v2.7.7 / 2023-02-28

### 改进功能

* 导出 PDF 时支持将资源文件作为附件嵌入
* 悬浮面板支持边缘拖拽改变宽度
* 编辑器浮动工具栏最多显示一个
* 挂件搜索时过滤不相关文件
* 按下鼠标时不触发停靠栏浮动面板
* 拖动页签分屏后原有页签切换改进
* 为闪卡复习界面添加 1024px 的宽度限制
* 优化 `添加到卡包` 对话框卡包列表
* 点击图片时更新面包屑
* 闪卡复习评分支持撤销
* 拖拽后布局自适应调整
* 调整闪卡评分按钮
* 支持文档块制作闪卡
* 改进浏览器剪藏扩展转换本地图片成功率
* 虚拟引用和反链提及关键字按最长匹配优先
* 历史数据列表标题转义处理
* 改进浏览器剪藏扩展转换本地图片后缀
* 闪卡复习时动态加入到期的卡片
* 更新版本后自动打开最新版的用户指南
* 当鼠标划出软件时隐藏悬浮面板
* 改进 PDF 未加载完时切换页签会空白
* 多个行内元素转换为引用时不再保留原有行内元素属性
* 改进标题下方块和列表项子块引用时的反链定位
* 改进浏览器剪藏扩展超链接 `#hash` 转换绝对路径
* 改进任务列表项完成后的样式
* 新页签应在拖拽到的显示器中打开
* 新窗口需随主界面切换编辑模式
* 支持笔记本级闪卡复习
* 闪卡复习界面中的预览调整到管理界面中
* 锁屏后新窗口也需要进行锁屏
* `闪卡` 菜单加入管理子项
* 光标在悬浮面板的输入框中时悬浮面板不进行隐藏
* 网络代理协议支持 `HTTP`

### 开发重构

* 升级 Electron

### 移除功能

* 移除顶部停靠栏

### 修复缺陷

* 导出空文档 Word 和 PDF 时合并子文档失败
* 文档和标题相互转换失效
* 文档转换标题后文档树刷新不正确
* 跨窗口拖拽到块底部异常
* 选中包含双引号的文本 `Ctrl+Shift+C` 后无法粘贴为引用
* 展开包含 mermaid 或 flowchart 图表的折叠块时无法渲染
* API `/asset/upload` 使用子文件夹时返回结果不正确
* 重命名父标签误修改子标签
* 外部内容粘贴到表格中后编辑导致换行丢失
* API token 绕过校验漏洞
* 反链面板悬浮后其编辑区域内浮层错误
* 生成文件历史索引报错

## v2.7.6 / 2023-02-21

### 改进功能

* 支持聚焦导出 图片/PDF/HTML/Word
* 支持基于文档复习闪卡
* 支持窗口之间拖拽移动内容块
* 优化界面动画效果
* 移动端链接菜单调窄
* 搜索自定义属性改为搜索全部属性
* 闪卡界面大小调整及添加预览
* 校验索引阶段自动删除历史遗留的笔记本 history 文件夹
* 文档名虚拟引用遵循搜索大小写设置
* 导出时去掉内容块闪卡样式
* 面板菜单中添加钉住菜单，停靠栏钉住按钮悬浮显示
* 子文档缺失父文档时自动补全
* 移除状态栏中停靠按钮悬浮菜单
* 改进打开虚拟引用后加载文档的性能
* 导出 PDF data-theme-mode 需固定设置为 light
* PDF 双页视图下支持矩形标注右侧页面
* `搜索结果显示数` 最小限制为 32
* 通过队列化索引历史数据以提升数据库稳定性
* 升级 vis-network 至 9.1.2
* 开启数据同步情况下启动时如果未联网应该提示
* 登录链滴账号后即可使用 `分享到链滴`
* Android 端恢复保活用的通知栏
* 改进状态栏显示后台任务执行计数
* 闪卡复习时编辑工具栏遮挡内容
* 支持列表制作闪卡
* 面包屑仅有一项时显示不全
* 改进数据同步以避免旧的本地数据覆盖云端数据
* 钉住的页签不再使用斜体
* 停靠栏面板最小宽度改进为根据类型来判断
* 改进右分屏打开引用后原文偶尔会滚动错位
* 搜索结果条目区禁用复制
* 集市包详情界面增加 `更新` 按钮
* 更改父文档标题或者移动父文档后使用状态栏推送子文档移动进度
* 支持在 All 卡包中移除闪卡
* 关闭当前页签后切换到最近打开的页签

### 修复缺陷

* 右侧点击钉住会出现在左侧
* 反链面板上下键会影响编辑且点击后浮动的面板会消失
* 关系图部分关联丢失
* 编辑器中输入 `#` 后标签搜索提示不全
* 校验索引生成冗余的 Untitled 父文档
* 导出模板无法保留第一个空段落
* `只读模式` 下闪卡复习和闪卡预览界面可以编辑
* 复制含链接的块为引用块异常

## v2.7.5 / 2023-02-14

### 改进功能

* 停靠栏面板支持鼠标悬停展开
* 改进导出 PDF 效果
* 改进删除折叠列表的下一个空列表项
* 改进官方数据同步性能
* 自定义表情文件过多时偶发更改文档图标卡顿
* 改进建立引用索引
* 网络过慢时保存命名查询会加载多次
* 改进重命名资源文件性能
* 拖拽文件到行内元素中需将其移动到行内元素后
* 数据同步发生冲突时在界面上进行提醒
* 大于 2MB 的图片默认不进行 OCR
* `完全手动同步` 下载后创建合并快照
* 加宽链接菜单
* 文档数据文件 ID 重复时自动重置 ID
* `空格···` 后回车异常
* 文档数据文件名不符合 ID 格式时自动移动到 corrupted 文件夹下
* 改进官方云端存储的统计性能
* 导入 Markdown 时支持 `[alt](<url> "title")` 语法
* SQL 搜索支持多个搜索关键字匹配文档
* 移动端支持重新打开已经移除的工作空间
* abc.js 升级至 6.1.9
* 新窗口支持钉住置顶
* 文档中的子块 ID 重复时自动重置 ID
* 文档树上支持新窗口打开
* 支持使用输入 `[[` 后粘贴文字也能被搜索
* 优化关系图生成性能

### 开发重构

* 降级 Electron
* package.json 构建前端使用正则表达式
* eslint js 文件并启用缓存 `--cache`
* 后台任务队列支持设置超时
* 确保 PR 只被提交到开发分支上

### 修复缺陷

* 桌面端系统睡眠唤醒后内核可能会自动退出
* Windows 7/8 桌面端无法运行
* `只读模式` 下搜索预览窗口仍然可以编辑
* 导出图片不显示表格
* `完全手动同步` 模式下 Android 前后台切换时不应该触发同步上传
* 移动端删除的工作空间无法同名再次新建
* 搜索不到超链接元素的 URL 和标题
* 显示器缩放后新窗口的关闭按钮和下拉按钮重叠
* 书签面板跳转被折叠的子块后及搜索面板未正确显示 `退出聚焦`

## v2.7.4 / 2023-02-09

### 改进功能

* 改进标签、引用、嵌入、表情和斜杆提示交互
* 通过 `Alt+M` 最小化后焦点回到先前的窗口
* 去掉数据同步增量索引时的界面遮罩
* 改进 `退出聚焦` 后的定位
* PDF 页签保存上次浏览位置
* 降低检查网络连通性所需时间
* 去掉 PDF 标注引用元素伪元素图标并调整颜色
* 云端同步模式支持 `完全手动同步` 模式
* 为新建日记添加笔记本图标
* 文档树拖拽遵循排序规则
* 页签和新窗口同步修改文档图标

### 开发重构

* 升级 Electron

### 移除功能

* Android 端去掉保活用的通知栏

### 修复缺陷

* 查询嵌入块点击面包屑后不应该出现可编辑状态
* 关系图点击标签节点报错
* 非折叠块搜索跳转不应该聚焦
* 未登录状态云端同步状态图标显示错误
* 导入 Markdown 时解析报错
* 代码块剪切后粘贴渲染不全
* 在代码块中复制粘贴不应该携带 HTML
* 设置启动时关闭所有页签后重启界面异常
* 点击文末空白时导致全选
* 数据同步后冲突文件未生成文件历史索引
* 使用或移除命名查询后搜索方式的悬浮提示信息没有更新
* Android 端返回桌面后再次进入白屏
* Android 端返回桌面时自动同步请求连接泄露
* 桌面端系统睡眠唤醒后内核可能会自动退出

## v2.7.3 / 2023-02-07

### 改进功能

* 支持笔记本设置独立的排序规则
* 支持转移引用
* 浮窗为文档块时支持动态加载
* `新建文档名模板` 改为 `新建文档存放位置` 并支持设置路径
* 支持划选复制并标注 `文本 *` 引用
* 使用鼠标跨屏选择块
* 桌面端浮窗和引用菜单添加 `在新窗口打开` 按钮
* 改进浏览器剪藏扩展处理带 `title` 的图片
* 改进浏览器剪藏扩展拉取图片
* 内核 API 校验 ID 格式
* 改进字体颜色设置背景颜色
* 支持插入 `.ms` 和一些国家域名后缀超链接
* IFrame 块、视频块、音频块、超链接和图片的输入选框支持拉伸
* 行级备注内容仅保留文本内容
* 表格删除行时不应该进行缩放
* 改进复制以便其他应用识别并移除 `复制 HTML`
* 改进块树数据索引稳定性
* Tesseract OCR 语言包支持通过环境变量设置
* 改进数据同步算法
* 数据快照对比右边为最新版本
* 改进多设备同时同步数据的可靠性
* 数据同步忽略最近文档存储
* 搜索时隐藏编辑器块引候选列表
* 避免图片等菜单、公式块等输入框、提示、行内元素菜单同时显示
* 改进 `Alt+M` 激活窗口
* 为页面 `html` 标签添加属性 lang、data-theme-mode、data-light-theme、data-dark-theme 
* 不再自动从临时文件中恢复数据文件
* 改进块引搜索的性能
* 改进标题转换文档的性能
* Tesseract OCR 加锁串行执行提升稳定性
* 降低重建索引内存占用
* 后台任务按任务加入先后顺序去重执行
* 浮窗中上下文和退出聚焦交互改进
* 引用计数浮窗传递型折叠改进

### 修复缺陷

* 反链面板和浮窗的面包屑上 Ctrl+Click 跳转后没有退出聚焦的按钮
* 移动文档后打开文档滚动触发动态加载后会关闭页签
* 聚焦列表后反向缩进触发状态异常
* Android 端返回桌面白屏
* 上传资源文件到云端图床统计重复
* 重复执行 `重建索引` 导致内核崩溃
* 取消超级块后撤销超级块外观相关设置丢失
* 搜索处于折叠状态的块时未聚焦显示结果
* 存在命名的块复制为引用时未转义命名文本
* 笔记本间移动文档后块可读路径未跟随

## v2.7.2 / 2023-02-01

### 改进功能

* 改进订阅到期提醒短信内容
* 调整外观配色
* 嵌入块查询结果中显示块引用计数
* 窗口变动后隐藏块标
* 大纲字号不应该跟随字体设置
* 浏览器端不使用内核伺服设备的剪贴板
* 改进缩放后 PDF 复制标注中的截图
* 终身订阅账号支持通过积分兑换扩容云端存储
* 导出没有内容的引述块为模板时添加空段落块
* 在 `关于` - `在浏览器上使用` 中提示用户固定端口 `6806`
* 改进清理未引用资源
* 改进从 IDE 中粘贴行内代码后的光标位置

### 修复缺陷

* Android 端部分系统闪退
* 选中图片状态下折叠回车再撤销触发状态异常
* 嵌入块每查询一次多一个结果
* 块引用后创建行内公式转为无效引用
* 索引嵌入块内容可能会导致内核崩溃
* 反链面板列表项层级折叠计算问题
* 未找到 ID 为 xxx 的内容块
* 网络请求时潜在的空指针问题

## v2.7.1 / 2023-01-28

### 改进功能

* 支持移动页签到新窗口
* PDF 按住 `Ctrl+D` 矩形标注，`Alt+D` 矩形标注并显示背景
* 为浮窗四周添加大小拖拽功能
* Android 端支持通过 `scribli://` 协议拉起
* 为同步状态添加对应的图标
* 标签联想改进
* 导出图片支持保留原有折叠状态
* 移动端不显示 `退出聚焦`
* 嵌入块纳入引用计数和反链
* 插入资源文件时文件名长度最大限制 189 字节
* 改进设置行级元素字体颜色和块外观背景
* 改进搜索预览结果定位
* Docker 镜像中加入环境变量 `RUN_IN_CONTAINER`
* 移动端底部工具栏遮挡侧边栏问题
* 改进图片 OCR 提取文本结果中的多余字符
* 自动校验索引时排除非 id 格式的 .sy 文件
* 嵌入块支持搜索
* 改进内核任务调度机制提升稳定性
* 数据同步后需要重新加载图片 OCR 提取结果
* 前端用户界面引入后台任务列表
* 移动端编辑后显示 `立即同步` 提示
* Android 端前后台切换时自动触发同步
* 导入 Markdown 和 `.sy.zip` 时增量建立索引
* 改进关闭笔记本体验
* 自动校验索引时考虑大小写忽略表
* 重命名资源文件不再需要等待数据写入
* 双击图片浏览时简化文件名
* 改进导入 Markdown 文件时解析 HTML 块
* 引用时按下左右键取消引用
* iOS 端支持通过 `scribli://` 协议拉起
* 重建索引改为后台任务
* 书签和标签重命名、删除不再需要等待数据写入
* 数据同步前先判断网络连通性
* Tesseract OCR 使用用户安装的语言包
* 使用 HTTPS 访问认证页面时无法建立 WebSocket 连接
* 改进块树数据存取
* 搜索时隐藏字体设置面板
* 改进创建文档性能
* 改进打开文档性能
* 搜索界面的类型过滤设置界面的优化
* 扩展页签拖拽范围

### 开发重构

* 改进 getSearch 函数实现
* 升级 Electron
* 重构定时任务实现

### 移除功能

* 移除桌面端左上角窗口标题栏应用图标

### 修复缺陷

* PDF 文件名长短影响页签图标大小
* macOS 端 Tesseract OCR 安装后不识别
* 包含列表时 Md2BlockDOM 不会进行转义
* 图片 OCR 搜索结果后出现多余的 `span` 字符串
* 响应较慢时连续点击工作空间菜单会重复
* 嵌入块面板按钮显示不全
* 数据同步时展开文档树操作可能导致数据丢失
* 折叠标题移动到下方后触发状态异常并导致数据丢失
* 闪卡预览的下一页可以一直点击
* 打开两个相同文档后关闭其中一个时属性转义会被执行

## v2.7.0 / 2023-01-17

### 引入特性

* 分享文档到链滴
* 桌面端支持搜索图片 OCR 文本
* 支持链滴积分兑换扩容云端存储

### 改进功能

* 为桌面端窗口标题栏添加应用图标
* 顶栏工作空间、外观模式和状态栏帮助菜单二次点击收回
* `Alt+N` 锁屏调整为全局快捷键
* 初次启动选择工作空间后不再创建 `Scribli` 这一层文件夹
* 改进块聚焦和只读模式情况下的提示
* PDF 预览页面点击链接后应打开浏览器
* 优化窗口最小时设置、模版和资源的界面
* 桌面端快捷方式工作空间参数 `--workspace` 变更
* 改进主界面菜单和功能入口布局
* 无动态滚动时应保持编辑器滚动的流畅性
* 支持多级菜单及菜单滚动
* 标签右键增加操作菜单
* 初次安装完浏览器剪藏扩展后提示刷新页面
* 编辑器宽度改变后需保持光标位置在可视区域内
* 支持通过 URL 查询字符串参数 `id` 和 `focus` 跳转到 Web 端指定块
* 数据仓库迁出时忽略 `.tmp` 临时文件
* 桌面端快捷方式支持 `--port` 启动参数

### 开发重构

* 改进 `getSearch` 函数实现

### 修复缺陷

* 反链面板 `链接` 子面板收起后 `提及` 子面板未显示内容
* 桌面端删除当前工作空间后报错
* 多开工作空间 `锁屏` 后访问鉴权失败
* 文档标题输入时右键粘贴无效
* 调用模板时模板名首字符乱码
* 搜索高亮和虚拟引用不需要转义 Markdown 标记符
* 多个桌面端快捷方式指定不同的 `--workspace` 参数后无法启动多个工作空间
* 工作空间列表中出现多个相同的路径
* 特定情况下块首删除会移除整个块
* 三击后快速 `Alt+D` 可能导致块显示异常
* 特殊情况下浮窗会消失
* 按住 Ctrl 拖拽反链的块到文档中触发状态异常

## v2.6.3 / 2023-01-11

### 改进功能

* PDF 画框复制标注带截图
* 桌面端支持工作空间多开
* 移动端支持切换工作空间
* 反链面板默认展开
* 搜索框和搜索页签布局分开保存
* `上传资源文件到图床` 每次都全量上传刷新 CDN
* Mermaid 时序图增加序号
* 改进桌面端拉起内核
* 拖拽 PDF 到页签上无法保存滚动位置
* 桌面端内核进程根据 Electron 主进程判断是否自动退出
* 改进界面缩放
* 剪藏支持知乎链接卡片
* 自动校验数据库索引
* 桌面端工作空间切换从 设置 - 关于 中移动到顶栏
* 改进重建索引
* 改进 Edge 浏览器 `Web 选择` 复制
* 升级 Mermaid 到 9.3.0
* 统一块超链接点击和右键打开行为
* 改进 `/模板` 搜索排序
* 反链面板支持过滤关键字高亮

### 修复缺陷

* 搜索高亮超链接锚文本后预览窗口修改导致地址失效
* 搜索指定路径参数解析异常
* localStorage 请求顺序异常导致图标无法加载
* 图片元素解析错误触发状态异常

## v2.6.2 / 2023-01-03

### 改进功能

* 卡包浏览时移除闪卡后更新右侧编辑器

### 修复缺陷

* 图标全部消失
* 点击编辑器 `...` 菜单不应该弹出订阅提示
* 浏览卡包界面无法移除闪卡
* 新建卡包没有持久化

## v2.6.1 / 2023-01-03

### 改进功能

* 支持英文拼写检查
* PDF 侧边栏大小调整
* 搜索结果高亮支持大部分行级元素
* 改进闪卡复习交互
* 折叠的标题块拖动后未显示折叠状态
* 浮窗和公式图表编辑窗移动后自动钉住
* 重复使用闪卡/日记快捷键优化
* 设置窗口最小尺寸并对设置界面进行自适应
* 文档标题粘贴后无法撤销
* 支持超级块制作闪卡
* 支持浏览卡包内的闪卡
* 新版本更新安装提示 15s 后自动消失
* 改进数据同步算法
* 为闪卡中的块添加块标
* 避免启动时重复全量重建索引
* 闪卡复习中有背景色的高亮答案不应该显示
* 不再覆写浏览器 localStorage 存取数据
* 卡包预览中继续制卡需关闭原有制卡窗口
* 改进搜索指定路径后不指定关键字的行为
* 改进导出 Data 压缩包过程
* 改进数据同步网络请求重试
* localStorage 不再支持数据同步
* localStorage 支持在多界面实例间同步
* `最近使用过的字体` 鼠标悬浮提示背景色/字体颜色

### 修复缺陷

* 移动端点击闪卡右侧栏没有隐藏
* 默认闪卡包移除后卡住
* 数据同步报错 `400 Bad Request openresty/1.17.8.2`
* 复习闪卡编辑器弹出的输入框中无法输入"s","a", "h", "g", "e"
* 合并单元格导出图片异常
* 空标题中输入 ``` 回车后异常
* 引用嵌套容器块触发状态异常

## v2.6.0 / 2022-12-27

### 引入特性

* 支持间隔复习

### 改进功能

* 改进块引计数浮窗显示逻辑
* 新建表格时移除表头 col1 col2 col3
* PDF 页签移动后阅读位置保留
* 改进数据同步算法
* 数据快照对比加入时间标识
* 为搜索添加 `移除命名查询` 功能
* 按文档分组搜索结果时文档 title 属性丢失
* 导出 Markdown 文件日期元数据字段变更
* 编辑器过窄时引用数被滚动条遮挡
* 官方数据同步使用加速域名
* ECharts 图表块支持 loose-json
* 命名查询不再持久化布局
* iOS 端题头图删除按钮不易被点中

### 修复缺陷

* 不存在 `云端标记的快照` 时界面一直转圈
* 搜索界面报错
* 数据同步报错 `file exists`
* 导入 Markdown 时转换 Base64 图片失效
* 搜索内容编辑后面包屑不可见
* Windows 端设置开机启动失效
* 设置块引静态锚文本转义问题
* 文档导出为图片时图片地址不正确

## v2.5.5 / 2022-12-20

### 改进功能

* 搜索上下及左右布局可进行大小调整
* 屏蔽列表项拖拽到列表项第一个块上方和列表前一个块的下方
* 支持数据快照对比
* 导出 Markdown zip 包内不带文件夹
* 显示本地数据快照文件类型计数
* 改进数据快照加载性能
* 桌面端检查磁盘可用空间
* 导出 Markdown 文件时开头附上一些元数据
* 支持去掉 PDF 导出页脚处 `由思源笔记导出`
* 改进部分繁体中文为习惯性用语
* 托盘菜单支持多语言
* 改进桌面端拉起内核

### 修复缺陷

* 复制超链接嵌套行级代码丢失链接地址
* HTML 块渲染可能触发状态异常
* 数据同步报错 `file exists`
* 建立索引时无法解析 `v2.2.0-` 版本的块引用

## v2.5.4 / 2022-12-13

### 改进功能

* 支持合并子文档导出 Word/PDF
* 支持列出和切换最近打开的文档
* 嵌入块复制为图片的范围不应局限于屏幕显示范围
* `块引新建文档存放位置` 支持模板变量
* 代码块输入光标错误
* Windows 端支持 Ctrl+Tab 不松开 Ctrl 后使用方向键选择切换
* 大纲、书签和标签面板支持键盘操作
* `Ctrl+F3` 遵循笔记本设置中的 `块引新建文档存放位置`
* 字号设置过大时导出图片优化
* 改进行级代码位于软换行后的编辑
* 导出 PDF 和 Word 时不带文件夹
* Ctrl+K 后如锚文本为空则将链接缩略后填入锚文本中
* `优化排版` 支持行级元素加粗、斜体等
* `Ctrl+Shift+↑/↓` 子项可移动到上下无子项的列表中
* 改进桌面端初始化时使用的外观语言
* 导出 Data 不带 data 父文件夹
* 统一块剪切粘贴逻辑为生成新块
* 浏览器端和移动端导出 Data 去掉 `data-` 前缀
* 导出 PDF 时根据所选页面大小对带行号的代码块宽度进行设定
* 自定义表情图片对齐
* 增加工作空间内核伺服锁
* 桌面端启动获取内核端口失败弹窗提示
* 改进标签重命名
* 开启 `始终定位打开的文档` 后再次选中文档页签时不再定位
* 访问授权页支持暗黑模式
* 访问授权页忘记授权码提示文案
* `Ctrl+Tab` 切换到的页签标题显示在可视区域内
* 访问授权页支持多语言
* Android Pad 端旋转屏幕后不重新载入
* 搜索支持按相关度排序
* `Ctrl+F` 覆盖命名查询中的指定路径
* Windows/macOS 桌面端支持开机启动
* 改进移动端只读模式图标
* `/模版` 和 `/资源` 提供上一个下一个按钮
* 标记快捷键修改为 `Alt+E`
* PDF 导出页脚处添加 `由思源笔记导出`
* 重复剪藏相同的页面后创建的文档不跟随机数
* `F3` 划选文本不重复创建子文档引用
* 改进命名查询持久化
* Windows 端支持窗口置顶
* 改进系统托盘单击交互

### 开发重构

* 升级 Electron
* 重构 Android 端启动
* 移除旧的行级元素相关实现代码

### 修复缺陷

* 带有字体样式的公式复制之后内容不正确
* 从左到右图标错误
* 切换主题导致页签空白
* 外观模式 `跟随系统` 时切换主题未刷新
* 启用的 CSS 片段删除后未从 `<head>` 中移除
* 列表项剪切撤销触发状态异常
* 导入空内容 Markdown 文件时报错
* 导入 Markdown 文件解析任务列表异常
* 导出 PDF 时表格中存在 SVG 图片出现滚动条
* PDF 导出公式被截断
* 集市包下载进度没有展现

## v2.5.3 / 2022-12-06

### 改进功能

* 搜索支持命名查询
* Ctrl+F 和 Ctrl+P 不再共用指定路径
* 分组后的搜索结果支持一键展开/折叠
* 搜索 `指定路径` 支持多选
* 搜索界面支持左右布局
* 更新搜索替换图标
* 搜索转换为 `固定搜索` 时向右分屏
* 搜索设置固定显示
* 搜索结果按文档分组时支持按块在文档中的先后排序
* iPad 端文本菜单有时不会显示
* 链接取消后导致 `Ctrl+K` 无法识别出链接
* 优化本地标记快照交互
* 构建 Docker linux/arm64/v8 镜像
* iOS 端支持打开 `zotero://`
* 新增百度网盘下载渠道
* 搜索类型选项加入确定按钮
* 优化窗口大小调整时上下面板的布局
* 自动下载更新包时在状态栏显示下载进度
* HTML 块自动使用 `<div>` 包裹
* 优化选择过多后的移动界面
* 搜索支持排序
* 导出 Markdown 时引用的超级块转脚注移除超级块标记符
* S3/WebDAV 设置界面中隐藏密钥/密码
* S3/WebDAV 数据同步支持设置超时时间
* 改进数据仓库 `通过密码生成密钥`
* 弹窗或关闭窗口应关闭无关的菜单
* 设置代理切换时不再需要重启
* 调整网络请求超时为 30s

### 修复缺陷

* 删除未引用资源后已删除的资源仍能搜索到
* 使用第三方主题时默认配色失效问题
* 反链面板 `Alt+[` 无法新建文档
* Ctrl+F 时 `包含子文档` 失效
* `复制 HTML` 无法复制非文本行级元素
* 向后删除遇到行级公式会删除公式
* 搜索结果命中代码块后定位异常

## v2.5.2 / 2022-11-28

### 改进功能

* 搜索支持按文档分组展现搜索结果
* 搜索页签支持过滤条件
* 搜索 `指定路径` 支持按文档树选择
* 块 `移动` 框支持可按文档树选择
* 拖拽移动块支持 `重复`
* 光标位于列表项末尾的行内公式后时回车表现异常
* 设置代理切换时不再需要重启
* 桌面端系统休眠唤醒后判断网络连通性后再执行数据同步
* 导入 Markdown 时支持图片路径包含空格
* 支持快捷键切换只读模式 `Ctrl+Shift+G`/`⇧⌘G`
* `虚拟引用关键字排除列表` 支持正则表达式
* 改进 S3/WebDAV 同步设置时参数校验
* GA 统计不发送浏览数据
* 插入资源文件时去除 `[`、`(` 等符号
* Android 端只读模式下长按支持复制
* API `/api/block/*` 支持插入或更新带有块级 IAL 的空段落块
* 改进某些网络环境下无法启动
* 启动时不再缓存笔记本根一级文档树
* 改进文档树展开性能
* 折叠列表项转换为文档时自动展开子块
* 改进反链面板加载性能
* 全局搜索和搜索页签统一界面呈现
* 快捷键切换页签时为初始化的文档无图标
* 页签过多时页签切换界面优化
* 改进搜索方式选项
* 当前块被选中后右键优化为弹出块菜单
* 带鱼屏边距比例调整
* 改进图标最右侧区域点击范围

### 开发重构

* 升级 highlight.js 至 11.7 并添加 panda-syntax-light 主题

### 修复缺陷

* 文档标题以空格结尾时导出 HTML 报错
* 超级块下方块被作为嵌入块时设置显示面包屑后不渲染
* S3/WebDAV 数据同步无法使用代理
* 收集箱渲染自定义协议超链接问题
* 从其他软件复制图片后无法解析粘贴
* 只读模式块标复制内容错误
* 在空的代码块中使用快捷键 `Ctrl + A` 选择并删除后出现异常
* 数据库 `markdown` 字段与 API `getBlockKramdown` 中行级元素 IAL 前存在零宽空格
* 使用 API 插入行级元素嵌套 HTML 实体时丢失实体内容
* 快捷键新建行内元素粘贴后行内元素消失
* 查找替换中使用正则表达式后替换不正确
* 使用 API `/api/block/updateBlock` 更新时 IAL 设置错误
* Ctrl+Tab 后 Ctrl+Shift+Tab 后切换页签错误

## v2.5.1 / 2022-11-22

### 改进功能

* PDF 中按下 `h` 为手型工具 `s` 为文本工具
* 反链面板支持关键字过滤
* 优化文档标题不支持 `/` 提示信息位置
* 浏览器端支持外观模式 `跟随系统`
* 改进导出长图
* 改进虚拟引用和反链提及搜索匹配
* 移动端支持外观模式 `跟随系统`
* 虚拟引用关键字缓存调整为 10 分钟
* 在 设置 - 搜索 中分别增加虚拟引用和反链提及 `关键字数量限制`
* S3/WebDAV 数据同步支持设置跳过 HTTPS 证书验证
* S3/WebDAV 数据同步单个请求默认超时 30s
* `Shift+↑/↓` 被取消的块应在可视区域
* 改进导出时引用嵌套锚文本的渲染
* 加一个用户指南入口按钮在顶栏 `设置` 按钮右侧
* 改进 S3 上传标记快照性能
* 导出 PDF 时过长的表格显示不全
* 调整边距后代码块行号错位
* 在右侧打开已存在但未初始化的文档时会重复打开该文档
* 升级五线谱 abcjs 到 6.1.7
* 同步时忽略 .git 文件夹清理空目录
* 移动端右侧菜单滚动时会触发菜单功能
* 改进粘贴 HTML 时 `<mark>` 标签的解析处理
* 减少嵌入块抖动
* 支持插入 `.sh` 域名超链接
* iPad 端中 PDF 无法正常显示
* 块从页签拖入悬浮窗后自动更新页签
* 图片缩放受限第一个块的长度
* 自定义表情右键添加复制选项
* 改进搜索性能
* 设置 - 账号中不再显示订阅价格
* 使用 API `/api/block/getBlockKramdown` 查询时返回 IAL
* 导入 Markdown 时将 Base64 编码的图片转换为文件
* 移动折叠标题后自动展开
* `复制纯文本` 时移除所有零宽空格
* 改进书签和标签面板加载速度

### 文档相关

* 增加 设置 - 搜索 - 搜索结果显示数 影响说明
* 更新 CI/CD 发布变更中的链接地址

### 开发重构

* 升级 Electron
* 移除不再使用的创建反链接口

### 修复缺陷

* 编辑器滚动条无法拖拽
* 文档标题快速输入中文光标位置异常
* S3 本地标记快照上传报错
* S3/WebDAV 本地标记快照上传未增量上传文件对象
* 导入 `.sy.zip` 后丢失自定义排序
* S3 无法删除云端标记快照
* 新建文档没有建立在当前页签的同级
* 表格内设置字体格式报错
* macOS、Linux 和移动端移动父文档排序时报错
* 数据库层缓存失效
* 全局搜索结果路径展现转义问题
* 数学公式染色后无法保存
* 导出 ECharts 图表块时被转义的符号未恢复
* 折叠标题移动后再撤销导致内容丢失和触发状态异常
* 剪藏时列表下方块缩进不正确
* WebDAV 删除云端标记快照报错
* 含有自定义表情的段落剪切后表情丢失
* 特定情况下主题切换无效
* 包含 `\"` 的行级公式解析问题
* 搜索设置忽略大小写时结果统计不正确
* 使用居中快捷键导致图片上有空行
* 更新任意文档图标后大纲都会更新
* 超级块中包含标题块时下方块面包屑计算不正确

## v2.5.0 / 2022-11-15

### 引入特性

* 导出为长图
* 数据同步和备份支持接入第三方 S3 协议对象存储服务
* 数据同步和备份支持接入第三方 WebDAV 服务

### 改进功能

* 编辑器右侧加入滚动到顶部和底部按钮
* 插入资源文件时不再去除 `[`、`(` 等符号
* 桌面端退出拉起更新安装时有时需要重启两次
* `网络图片转换为本地图片` 支持处理 `file://` 本地路径图片
* 文档块转换为标题块时将文档标签移动到标题块下方
* 只读模式下不应该能够剪切
* WebDAV 数据同步跳过 HTTPS 证书校验
* S3 云端存储支持设置使用路径风格寻址
* 超宽屏幕优化
* 当有其他软件带有划选弹出功能时浮动工具栏消失
* 改进数据量较大时的启动速度

### 开发重构

* 改进更新集市包的发布流程

### 修复缺陷

* 清除样式导致误删文本
* 无法删除云端标记快照
* 标题块无法转换为被选中文档的子文档
* 只读模式下列表前的箭头无法折叠
* 反向链接面板复制问题
* 只读模式下多项折叠会导致数据类型错误
* 图片居中顶部有空行
* PDF 页签中查看属性报错
* PDF 页签中点击搜索再点击其他地方报错
* `F2` 重命名文档后编辑器内未跟随改变
* `[[` 引用动态锚文本长度限制无效
* 选中备注及其后文本无法将其后文本设置为备注
* 禁止文档历史内容可编辑
* 对没有渲染的文档进行重命名、移除、关闭，页签没有对应的变化
* 复制单个块时无法复制属性

## v2.4.12 / 2022-11-10

### 改进功能

* 光标位于段落块首的图片前时无法显示
* 为块重复添加快捷键 `Ctrl+D` 删除线快捷键修改为 `Ctrl+Shift+S`
* 快捷键新建行内元素后光标消失
* 行级清除样式不再清除备注、链接、标签、公式、PDF 标注和引用
* 为字体添加恢复默认功能
* 改进 WebDAV 数据同步创建目录实现
* 数据同步接入 S3 协议兼容的对象存储服务
* 行级代码、标签和键盘元素在块首时光标无法移动到元素前
* 调整数据同步接入第三方存储服务
* 文档树多选后右键菜单仅显示可操作的项
* 支持跨块及多选块重复
* `Ctrl+Click` 选块应禁止嵌套选中
* 改进 WebDAV 上传数据性能

### 修复缺陷

* 文档树上的文档无法拖动
* 设置在当前页签中打开失效
* 浏览器端数学公式内撤销导致状态异常
* 选中段落块和列表项块后粘贴会出现独立的列表项块

## v2.4.11 / 2022-11-08

### 改进功能

* 文档树移除笔记本双击操作并优化点击笔记本展开速度
* 改进鼠标移动到空文档名处光标状态
* 移动端后退页面包含嵌入块时定位不准确
* 在 `新页签中打开` 快捷键更改为 `Ctrl+Alt+Click` 或鼠标中键
* 优化在当前页签中打开的交互
* 开源数据同步 WebDAV 实现
* 移动端弹起键盘时减少抖动
* iPad 端外接键盘底部不留空
* iOS/iPad 端系统版本最低要求从 13.6 提升到 14.0
* 隐藏 iPad 顶部状态栏

### 开发重构

* 重构 `文件历史` 查询

### 修复缺陷

* macOS 端报错 `内核连接中断` 以及 `数据库被锁定`
* 移动端文档树菜单点击无效
* 行内备注无法被搜索到
* 块末尾为数学公式时 `Shift+↓` 选中范围错误
* 文档树中笔记本无法进行排序
* 通过 `Shift+Click` 多选块移动时丢失块
* `Ctrl+Shift+V` 外链资源文件失效

## v2.4.10 / 2022-11-05

### 改进功能

* 文档树支持 `Ctrl+Click` 和 `Shift+↑/↓` 进行多选
* 开源云端同步服务实现
* 页签下拉和属性书签下拉面板优化
* 支持 `Alt+I/E/N/U` 配置
* 支持小键盘中的 `/*-+.`
* 导出 HTML 和预览时不再将视频链接转换为 IFrame
* 行内元素的下划线颜色继承当前元素前景颜色
* Windows 端退出拉起更新安装时有时需要重启两次
* 导出 Markdown 行级元素改进
* 快捷键设置页面左右抖动
* 改进导入 Markdown 时 `<img>` 标签的处理
* 超级块下的子块被引用数会被后一个块遮挡
* 调整页签文字居中
* 快捷键重置需弹框确认
* Android 端键盘收起头部会闪一下
* `Ctrl+Shift+V` 调用浏览器原生功能，不再支持配置

### 修复缺陷

* 移动端长按拖拽导致界面透明度异常
* 未登录情况下创建数据快照失败
* 文档树更多操作对应的是上一次选中的文档而非当前文档
* 数据历史每页最后一条记录时间异常
* 无法引用标签开头的块
* 删除文档后应关闭该文档的关系图、大纲和反链页签
* 删除父文档时子文档排序配置未清理干净
* 无法替换包含 HTML 转义的代码内容

## v2.4.9 / 2022-11-01

### 改进功能

* 编辑器设置支持两侧对齐布局
* 编辑器设置及块布局支持文本从右到左显示
* 保存 `data/storage/local.json` 时格式化
* 改进 `网络图片转换为本地图片` 微信图片拉取
* 自动更新下载安装包时优先使用 GitHub Releases
* Android 端禁用文本拖拽

### 开发重构

* 重构数据仓库同步
* 用 `os.ReadDir` 替换 `Readdir(-1)`

### 修复缺陷

* 代码块内注释无法剪切
* 只读模式下禁止粘贴操作和拖拽插入资源文件
* macOS 端录音无声音
* 安装时无法在工作空间覆盖警告弹框处取消安装
* 有序列表折叠后不显示数字
* 暗黑模式下 PDF 使用明亮模式导致标注显示异常 

## v2.4.8 / 2022-10-31

### 改进功能

* 支持设置悬浮窗弹出选项
* PDF 支持深色和浅色模式选择
* 改进伺服代码片段 `/snippets/`
* 切换回通过 IP `127.0.0.1` 加载
* Windows 端不再支持安装时选择安装路径
* 反链面板中面包屑支持 `Ctrl+点击` 打开新页签
* 改进触摸屏中嵌入块工具栏显示规则
* Android/iOS 端不显示数据索引和搜索索引状态提示
* 前进后退按钮图标更新
* 支持插入 `.im` 和 `.build` 域名超链接
* 鉴权兼容 IPv6 本机回环地址
* 明显区分折叠和非折叠状态颜色
* 支持关闭 Google Analytics
* 每个工作空间单独持久化 localStorage
* 支持 `HEVC` 编码视频播放
* `网络图片转换为本地图片` 遵循笔记本级资源文件设置
* 编辑器动态加载块数由定值改为可配置
* 优化 Pad 竖屏显示

### 文档相关

* 听说思源笔记会通过用户电脑挖矿

### 移除功能

* Windows 端不再支持 32 位系统
* 移除 `--resident` 和 `--servePath` 内核参数

### 修复缺陷

* 设置代码片段报错
* 录音功能无法点击
* 引用非文档命名块时动态锚文本没有使用命名
* 修改命名文档块后引用动态锚文本未跟随
* 标记公式为标签后无法在标签面板删除标签

## v2.4.7 / 2022-10-27

### 改进功能

* 支持通过界面设置代码片段
* 反链面板块标菜单移除缩放功能
* 某些主题下页面向上滚动时会左右抖动
* 改进块引用语法解析避免识别 `((foo))` 文本
* Android 端 IFrame 块加载不打开其他 APP 但支持继续加载页面
* 加入 Google Analytics
* 使用鼠标的复制粘贴按钮导致 `Command` 不被释放 
* 改进云端收集箱剪藏
* 订阅试用从 30 天缩短为 7 天

### 文档相关

* 修改隐私政策

### 开发重构

* Android 端适配 Android 13
* Android 端最低版本要求从 6.0 改为 7.0
* 通过域名 `Scribli.localhost` 加载

### 修复缺陷

* 反链面板首块起始插入行导致状态异常
* 拖拽窗口后反链面板块标位置错误
* 从全局搜索打开文档后反链面板为空
* 快速来回滚动会导致嵌入块重复

## v2.4.6 / 2022-10-26

### 改进功能

* 支持非列表大纲式缩出
* 块滚动条跟随滚动
* 桌面端内核进程使用随机端口
* 列表里代码块折叠后丢失列表线
* IFrame 块不打开浏览器重定向
* 触摸屏点击题头图可对相关按钮进行显示和隐藏的切换
* 导出 PDF/HTML 时遵循是否显示书签、命名等设置
* 更新在页签右侧或下侧打开的图标
* 为向上扩选和向下或选添加快捷键配置
* 内核参数增加 `--port`
* 某些系统上下载资源文件后打开是 zip
* 浏览器剪藏扩展转义 Markdown 标记符
* 导出 PDF 后主窗口保持原有缩放
* 去掉工作空间路径尾部空格
* 支持设置 `虚拟引用关键字包含列表`
* 支持伺服代码片段 `/snippets/`

### 开发重构

* 桌面端内核进程不再以游离模式拉起
* 桌面端内核进程端口号写入文件 `port.json`
* 前端统一使用内核端口号变量

### 修复缺陷

* `Ctrl+Shift+V` 复制内容到代码块中导致渲染偏移
* 浏览器剪藏扩展丢失标题和代码块
* 图表块的内容在数据库 `blocks` 表 `content` 字段中被转义
* 短时间内多次修改文档定义块标题后动态锚文本没有跟随
* 超级块起始删除撤销触发状态异常
* 同步中的文档应禁止块标操作
* `粘贴转义文本` 失效
* Linux 桌面端自动更新未赋予可执行权限
* 内核参数 `--resident` 和 `--readonly` 参数解析问题
* 渲染虚拟引用和搜索高亮导致 Markdown 标记符重复解析
* 编辑器内修改标题后顶部标题没有更新

## v2.4.5 / 2022-10-23

### 改进功能

* Shift+↑/↓ 逻辑改进
* 云端同步数据在启动后执行
* 优化触摸屏题头图和 emoji 点击
* 微信小助手支持语音、视频和文件
* 改进虚拟引用和反链提及搜索分词
* 限制笔记本名和文档名最大长度为 `512`
* 升级 Electron 解决导出 PDF 页边距设置无效
* 微信小助手发送图片、语音、视频和文件单个文件最大限制为 `10M`
* 限制页签最大打开数量为 `32`
* 云端收集箱文档内添加刷新按钮
* 嵌入块在原文上方时更新原文其中的数学公式会抖动
* 数据快照内置忽略 `.tmp` 文件
* 提升数据仓库快照性能

### 修复缺陷

* 给文字和图片同时设置字体格式后图片丢失
* 优化排版未处理样式文本
* 点击图片 `Ctrl+Shift+A` 新建一行输入文本后按 `Backspace` 会导致图片消失
* 块首 `Shift + Backspace` 异常
* 优化排版导致无法删除 HTML 块
* 嵌入块在原文上方时拖拽导致数据异常

## v2.4.4 / 2022-10-21

### 改进功能

* `Alt+5` 打开已有日记时不在内核伺服客户端之间同步
* 浏览器剪藏扩展改进为由下往上滚动
* 标题后直接跟图片时图片的提示文本不再渲染到大纲中
* 切换到全屏后支持拖拽窗口
* 拆分或合并单元格后光标消失
* 停靠栏图标大小调整

### 修复缺陷

* 由未使用 `theme.js` 的主题切换至使用 `theme.js` 的主题时文档 emojis 图标不显示
* 关系图全屏切换后图标位置不对
* 聚焦列表项回车后输入 `[]` 后异常
* 合并单元格后 `Ctrl+A` 再 `Ctrl+B` 内容消失
* Windows 端主窗口关闭按钮悬浮背景色没有变红
* 浏览器剪藏扩展列表下方段落缩进成为子块
* 云端同步后如果 Emojie 文件夹为空不应该删除

## v2.4.3 / 2022-10-20

### 改进功能

* 桌面端 `/模板` 增加 `打开文件位置`
* 钉住有图标的页签样式错误
* 历史和集市页签大小调整
* 标点符号不纳入虚拟引用和提及搜索

### 修复缺陷

* 集市图标下载和使用后不会更新
* 复制富文本和剪藏异常
* 在加载界面卡住

## v2.4.2 / 2022-10-19

### 改进功能

* 查询嵌入块支持设置是否显示面包屑
* 触摸屏嵌入块固定显示操作按钮
* 支持例外情况输入的块引嵌套老数据迁移
* 只读模式禁止一些操作
* 移动端的只读模式使用编辑器配置项
* 改进 `Ctrl+K` 插入超链接补全锚文本
* 有序列表拖拽成新列表后从 `1` 开始排序
* 移动端文档树中的自定义文档图标错位
* 改进虚拟引用和反链提及搜索分词
* 改进外部复制富文本时相邻行级元素加粗、斜体和代码的处理
* 快捷键设置中点击列表都可使输入框获取光标
* 查询嵌入块 `隐藏标题下方的块` 开关移动到块标菜单中
* 加载文档时如果发现父文档 .sy 文件缺失则自动重建
* 列表配色及缩进改进
* 文件历史支持按文档名搜索
* 位于超级块中的嵌入块不显示面包屑
* 导出 PDF 预览界面不受主界面缩放影响
* 调整默认主题外观
* 启动或使用下拉列表切换页签后当前页签不在可视区域内

### 修复缺陷

* 列表项内容为空时数据库表 blocks 的 markdown 字段缺少换行
* 使用属性 `data-export-html` 导出时丢失 `<style>` 标签
* 行级元素前输入转义符 `\` 导致异常
* 全屏模式图标错位
* 切换主题会把图标切换回默认
* 字体上色后大小识别错误
* 反链面板的提及子面板高度异常
* 嵌入块中有列表时块标显示位置错误
* 修改带命名的文档名锚文本会更新为文档名
* 清理资源文件后再次上传相同文件异常
* 网络超时导致同步失败时提示报错异常

## v2.4.1 / 2022-10-16

### 改进功能

* 样式类行级元素中支持 `/菜单` 和 `((引用`
* 文档树文档拖到指定分屏窗口页签栏在指定分屏窗口中打开
* 顶栏改进数据历史图标
* 桌面端自动更新安装包不进行并发下载
* 改进虚拟引用和反链提及搜索分词
* 改进编辑器只读模式
* 虚拟引用和反链提及大小写敏感遵循搜索设置
* 顶栏加入切换外观模式
* 增加挂件块数据导出属性 `data-export-html`
* 剔除块尾多余的软换行
* 第三方图标不全时使用系统自带的 material 图标替代
* 改进默认主题外观
* 区分引用块和关系图图标
* 反链面板文档块标 `Ctrl+Click` 从静默打开修改为直接打开
* 支持插入 `.la` 域名超链接
* 搜索资源文件支持跟随 assets 文件夹符号链接

### 开发重构

* 升级 Electron

### 修复缺陷

* 编辑器和块操作 API 解析 `~~~foo~~~` 失败
* 反链提及排序失效
* 快速折叠展开反链面板项异常
* 引用列表时锚文本获取错误
* API `/api/attr/setBlockAttrs` 设置属性值转义问题
* 合并折叠标题为超级块后内存溢出导致内核崩溃
* 合并超级块后删除再撤销超级块解体
* 查询嵌入块面包屑过长导致分栏失效
* `Ctrl+K` 弹层输入框没有聚焦
* 调整挂件块尺寸后 `data-export-md` 与 `data-export-html` 丢失
* 剪切行级元素粘贴为纯文本后输入标点重复
* 排版优化转义内容异常
* 行级元素键盘和下划线无法粘贴为纯文本
* 拖动分屏窗口时会漏出底层的拖拽线

## v2.4.0 / 2022-10-13

### 引入特性

* 支持设置编辑器只读模式
* 增加公共代码 js/css 片段

### 改进功能

* 查询嵌入块添加面包屑
* 起始块由于文字太长导致换行时上键会直接定位到文档标题
* 带子级升降级标题层级支持列表块中的标题
* 修改桌面端顶栏数据历史图标和图标顺序
* 改进查询嵌入块加载性能
* 给备注的 tooltip 设置选择器
* 改进提及和虚拟引用搜索分词
* 软换行后 `》` 无法创建引述块
* 出现行级 HTML 节点时转换为 HTML 块渲染

### 移除功能

* 移除编辑器内文本拖拽功能

### 修复缺陷

* 无法通过拖动块标移动非列表项的块
* 通过快捷键添加行级代码、键盘、标签后方向键跳出行不一致
* 标签中的单引号不应该被替换为 HTML 实体
* 行级元素内粘贴纯文本应保持该行级元素的状态

## v2.3.3 / 2022-10-11

### 改进功能

* 鼠标悬浮显示超链接 URL 与标题时标题另起一行
* 鼠标移动到反链面板左侧空白位置不会显示图标
* 反链面板提及中的提及词高亮显示
* 公式编辑框存在时点击其他块后光标依旧会跳转到公式后
* 浏览器剪藏扩展改进
* 退出时自动删除超过 7 天的安装包
* 拖拽移动块后移除搜索高亮标记
* 行级代码、键盘和标签位于段首时光标交互改进
* 标题块标菜单支持 `删除标题及下方块` 和 `剪切标题及下方块`
* 图表、公式块等关闭编辑面板后高亮选中该块
* 改进表格交互
* 同步时如果发现数据仓库损坏则自动重建仓库
* 单双击文档树不应该对其进行滚动
* 浏览器剪藏扩展限制 Chrome 最低版本

### 开发重构

* 升级 Electron

### 修复缺陷

* 表格块过大时搜索无法高亮并定位匹配的关键字
* 解析 Markdown 嵌套行级元素异常
* 展开反链面板时第一项没有展开
* 桌面端自动更新创建文件夹失败
* 超链接标题内容转义问题
* 反链面板切换面包屑后公式无法渲染
* 在表格块中空单元格时使用 `Ctrl+/` 选中表格块后无法使用 `Backspace` 删除该块
* 清除某些样式导致文本顺序错乱且光标无法移动
* Android 端粘贴显示 `undefined`
* 在图片前插入分隔线后光标丢失
* 转义字符加行级样式后继续输入会出现标记符
* 嵌入块删除后撤销会出现渲染两个完全一致的查询结果
* `* * foo` 列表项拖拽到父级列表项下列表消失

## v2.3.2 / 2022-10-09

### 改进功能

* 代码语言可进行清空
* 改进检查更新时版本号比较
* 同步生成冲突文档时补全 `Spec` 属性字段
* F3 在纯文本且没选中情况下应替换原有文本为引用
* 云端同步冲突时支持设置是否产生冲突文档

### 修复缺陷

* 不应该重置锚文本为 `block not found`
* 同步创建数据快照失败
* 反链面板嵌入块无法加载

## v2.3.1 / 2022-10-08

### 改进功能

* 改进行级代码、键盘和标签元素边界编辑交互
* 文档滚动状态不产生同步冲突
* 移动端编辑工具栏仅在编辑器中触发
* 块引缺失锚文本情况下自动补全
* 内核接口 `api/system/getConf` 脱敏处理
* 分割线不应该支持折叠
* 关闭自适应宽度后反链面板无法根据内容自适应

### 修复缺陷

* 加载自定义表情可能导致内核崩溃
* 文档实时统计失效
* 段落末尾为行级元素时右键异常
* 快捷键 `⌥⌘ ` 识别失败
* 反链面板刷新后数学公式无法加载
* 列表项行级元素结尾和转义元素编辑异常
* 列表项中 `Ctrl+Tab` 异常
* 分隔线块标菜单点击异常

## v2.3.0 / 2022-10-07

### 引入特性

* 移动端支持编辑工具栏
* 编辑器式反链面板

### 改进功能

* 块引用与所引用的内容块进行交换
* 浏览器剪藏扩展迁移到 Manifest V3
* 移除文件锁
* 生成重复/冲突文档时移除 `scroll` 浏览位置属性
* Windows 端支持 `AppsKey` 按键 打开右键菜单
* 上传云端标记快照（云端备份）数据量较大时超时
* 支持按文档大小排序
* 支持按子文档数排序
* 复制引用中的一部分粘贴后应为静态锚文本
* 文档树面板 `定位打开的文档` 显示在中间
* PDF 搜索换行隔断文本
* 编辑时状态栏实时统计文档信息
* 文档信息统计增加超链接数、图片数、引用数和大小
* 块操作 API 支持使用 `<u>` 和 `<kbd>` 插入对应行级元素
* 桌面端重启以后保证窗口最小大小
* 大纲面板定位时显示在中间
* 桌面端托盘加入选项 `重启时重置窗口`
* 块标拖拽按住 `Alt` 为引用，按住 `Shift` 为嵌入
* 从外部复制富文本时加粗、斜体等排版元素不再自动添加空格
* 从外部复制富文本时不产生多余换行
* `F3` 没有选中也可以新建子文档
* 从外部复制富文本时超链接嵌套加粗、斜体等元素解析改进
* 通过浏览器使用时剪切再粘贴会导致光标定位到文档开头
* 移动端行级元素编辑跳出
* 浏览器端无法读取路径，不支持 `Alt` 拖拽
* 移动端键盘弹起后光标定位居中
* 打开块引快捷键变更为 `Alt+,` / `⌥,`

### 开发重构

* 将 `windowControls` 的样式移至 base.css 中
* 升级 pdf.js
* 升级 Electron
* 升级 mermaid

### 修复缺陷

* 浏览器剪藏扩展对于某些页面滚动到底部以后没有响应
* 无法定位到后退内容为动态加载的块
* 行内末尾 `Ctrl+M` 会匹配前一个数学公式
* 添加字体风格会把行级公式删掉
* Windows 端安装包数字签名问题
* 引用块合并后锚文本重复
* 自定义表情后无法进行回车
* 使用 `Enter` 创建双链位置异常
* 动态锚文本剪切修改后没有变为静态锚文本
* 在空白段落块/标题块中无法使用快捷键 `Ctrl+M` 插入行级公式
* 选中两个公式后 `Ctrl+M` 导致公式异常
* 引用命名块时动态锚文本没有使用命名
* 粘贴引用后转义消失
* 文档标题为空时顶部栏没有显示
* `Ctrl+M` 行级公式后输入标点符号重复
* 解析 Markdown 围栏代码块 `~~~` 异常
* 行级文本标记解析异常

## v2.2.3 / 2022-09-28

### 改进功能

* 带子级升降标题层级
* 标题块支持复制下方块
* 表格内光标和按键交互优化
* 提升桌面端自动更新安装包下载速度
* 手动检查更新时自动下载更新安装包
* 桌面端自动下载更新安装包选项默认开启

### 修复缺陷

* 使用社区主题时外观模式跟随系统切换异常
* 浏览器端界面异常
* 切换同一种外观模式时主题没有清除 js 残留
* 点击引用到同一文档的聚焦块中后退光标消失
* 复制带超链接的图片无法保存到本地
* 桌面端拉起自动更新安装包偶尔失败
* 云端数据同步报错导致界面溢出异常

## v2.2.2 / 2022-09-27

### 改进功能

* 外观模式支持跟随系统切换
* 退出时自动删除超过 30 天的安装包
* 行级备注自动移除换行
* 超链接地址更好地兼容本地路径
* 支持插入 `.ch` 域名超链接
* 改进选中块删除后光标位置

### 修复缺陷

* 偶发编辑文档标题后引用处的动态锚文本不更新
* 虚拟引用无法通过快捷键转换为引用
* 字号回填错误
* Windows 下 `Ctrl+Shift+9` 按键不识别
* 设置访问授权码后无法导出 PDF
* 块引锚文本输入框转换动/静态锚文本异常
* `\\` 开头的 Windows 网络共享路径超链接问题
* Markdown 超链接锚文本转义解析问题
* 在行级元素中添加公式、备注和链接导致类型丢失
* 表格内多个连续的超链接无法换行显示
* 集市已下载栏打开图标文件位置路径错误
* 行级代码不按照编辑器连字设置渲染
* 表格内无法将 `$` 字符在行内代码内正常使用
* 无法插入资源文件的问题
* 浮窗中图表编辑层拖动错误
* 全局搜索后使用 `Alt+[` 引用异常
* 超链接元素标题中存在 `"` 字符时粘贴无法正常解析
* PDF 标注引用在数据库所有表中对应的 `markdown` 字段均没有标识符
* 动态锚文本后删除文本变静态锚文本

## v2.2.1 / 2022-09-25

### 改进功能

* PDF 页签分屏后会跳转到开始位置
* 修改属性时更新 `updated` 字段
* 文档标题输入框回车新建块
* 同文档块引转脚注缩略定义考虑标题块
* Lute 实例提供 `BlockDOM2Md` 函数
* `Ctrl+↑` 长按仅执行一次
* 以行级元素开头在起始位置输入的文字应为普通文本
* 识别选中内容字体的大小
* 标题开头 `Backspace` 不应该转换为纯文本
* 改进快捷键搜索
* 导出导入 .sy.zip 保持自定义排序
* 导出 PDF 支持保留折叠状态
* 快捷键搜索屏蔽 autohotkey 发送的无效信息
* 引用计数浮窗大小不应根据引用数位置不同而变化
* 引述块中输入行公式块后无法跳出

### 修复缺陷

* 块引用在数据库所有表中对应的 `markdown` 字段均没有标识符
* 超链接嵌套图片情况下数据迁移导致未引用资源文件
* 搜索代码内容转义问题
* 搜索打开遇到引用后 `⌘.` 无法打开引用
* 导出预览复制到知乎报错
* 预览虚拟引用报错
* PDF 标注预览失效
* `⌥⌘X/Ctrl+Alt+X` 无效且最近使用过中没显示字号
* 粘贴 `<span>` 解析器报错
* 虚拟引用不应该保留编辑状态
* 链接转化为文本后没有保存
* Markdown 转义嵌套解析问题
* 快捷键识别不正确

## v2.2.0 / 2022-09-22

### 引入特性

* 支持行级备注
* 行级元素支持多重样式类型
* 支持设置行级文本字体大小

### 改进功能

* 改进行级元素解析
* 图片后插入代码块图片后的光标错误
* 改进 `Ctrl+K` 超链接粘贴识别
* 优化排版时键盘元素前后自动空格
* 内核接口返回非 200 状态码时报错提示用户
* 细化云端同步锁提升稳定性
* 搜索区分大小写根据配置项单独建立索引
* 重建索引细节进度推送展现
* 以 Docker 容器运行时不再进行 WebSocket 传输压缩
* 长按关闭页签快捷键不应连续快速关闭页签
* 平板端提供退出应用方式
* 改进 `优化排版` 性能
* 初始化界面加入不支持第三方同步盘的警告提示文案
* 搜索替换支持块引锚文本
* 暗黑模式下 mermaid 通过 `%%{}%%` 设置的参数没有效果
* 被 `\` 转义的标记符在大纲显示
* 下拉页签提供关闭功能
* `/模板` 搜索按名称自然升序排序

### 开发重构

* 升级 Electron

### 移除功能

* 不再支持使用 Markdown 标记符嵌套行级元素
* 不再支持通过 HTML `<kbd>` 和 `<u>` 输入键盘和下划线元素

### 修复缺陷

* 导出 PDF 的 SVG 图片链接无法正常显示
* 使用 API `/api/block/updateBlock` 更新列表块时无法渲染行内公式
* 快捷键设置输入框回显异常
* PDF 导出时路径选择报错
* 块滚动条最后一个块后向上滚动失效
* 使用 API `setBlockAttrs` 设置文档块属性后未变更文档 DOM 属性
* 导出较大的 PDF 预览白屏
* 双击打开图片不对
* 网络代理设置 HTTPS 协议后显示不正确
* 空的引述块中回车报错
* 自定义表情搜索报错
* macOS 端代码块中空行处使用 `⌘+Del` 删除异常
* 云端同步创建数据快照报错 `invalid argument`
* 导出 PDF 预览时点击块引转换后的脚注跳转不正确
* 横向排版 Shift+Click 从右向左无效
* 表格四周点击导致光标消失

## v2.1.14 / 2022-09-09

### 改进功能

* PDF 自动缩放、适合页面、适合页宽只有切换时有效
* 导出 PDF 预览并提升导出速度
* 移动端支持题头图位置调整
* 公式导出时展开宏定义
* 支持自定义 Ctrl+↑↓
* 桌面端内置 Pandoc 可执行文件
* 快捷键组合支持 `Shift+Alt` 与 `Shift+Alt+Ctrl`
* 桌面端自动下载更新安装包
* 集市已下载包在配置文件不正确的情况下不应该显示更新按钮
* GitHub Releases 下载安装包加入校验和
* 打开数据历史时不自动打开预览
* 调用 API `setBlockAttrs` 后推送 Transaction
* 云端同步时自动删除空文件夹
* 改进已经损坏的数据处理

### 开发重构

* 升级 Electron

### 修复缺陷

* Android 端打包错误
* 页签拖拽分屏问题
* 在列表项中非第一个子块中使用快捷键折叠光标会消失
* 行内标签内容为空时没有清除
* 标题复制粘贴撤销后触发状态异常
* 自定义属性更改与删除异常
* 文档大纲页签移动后刷新界面布局丢失

## v2.1.13 / 2022-09-05

### 改进功能

* 为每个分屏添加页签下拉菜单
* 页签栏支持 `+` 创建
* 列表项以公式等不可直接编辑的块结尾后无法回车创建新列表项
* 改进集市已下载包的显示和更新
* 改进 `Ctrl+K` 超链接粘贴识别
* 公式导出时将宏定义使用 `\newcommand` 插入
* Linux 端不再支持 `粘贴为纯文本` 时处理文件绝对路径

### 文档相关

* 修改 Android 端隐私政策
* 修复帮助文档快捷键相关的错误描述

### 修复缺陷

* 挂件移动或设置大小后属性丢失
* 列表项第一个子块为空时导出渲染错误
* Windows 端打开本地文件所在位置失效
* PDF 阅读器内存泄漏
* 标签名称存在包含关系时重命名标签会出现误修改
* 搜索结果中文档图标使用自定义图标时大小异常
* 行内公式导出被字母 a 和 b 包裹
* 标题块下方块反向链接计算问题
* 自动补全后 Ctrl+Z 行为不对 
* 快捷键复制包含 `'` 的文档名为引用时无法识别

## v2.1.12 / 2022-09-03

### 改进功能

* 反链面板块移动支持复制为引用或嵌入
* 导出为 .docx 时无法正常转换公式
* 超链接锚文本中 `\]` 符号渲染时未正确转义
* 集市支持已安装的包单独显示
* 为搜索结果列表中的文档块添加文档图标
* 改进移动端的数据历史界面
* 数据历史浏览时忽略内容块折叠状态
* 改进非订阅用户集市包下载速度
* 重新梳理打开引用的方式和快捷键
* 改进设置快捷键界面加入缩进竖线
* 文档重命名后变更文档更新时间
* 快捷键设置标题后光标应保持原有位置
* 聚焦到上层可按文档路径依次返回父文档
* 拖拽资源文件到编辑器中 `使用 file:// 绝对路径并链接` 功能修改为按住 Alt
* 移动端图表编辑框固定到顶部
* 列表中的图片后双击换行图片光标错误
* 点击大纲中父级为折叠的标题才进行聚焦

### 修复缺陷

* 页签右键菜单分屏功能异常
* 检查系统更新失效
* 导入 Markdown 文件报错
* 块菜单转换一级标题变成六级标题
* 云端同步在系统休眠后唤醒会重复执行
* 折叠标题后编辑触发状态异常
* 拖入文件名包含 `)` 的文件以 `file://` 插入后链接解析错误
* 导出时 KaTex 宏无效
* 包含较大列表的文档加载不全
* 剪藏微信公众号文章丢失图片

## v2.1.11 / 2022-08-31

### 改进功能

* 数据历史文档和资源文件支持分页和搜索
* 数据历史文档支持只读可视化预览
* 通过环境变量 `RUN_IN_CONTAINER` 判断是否在容器内启动
* 支持未激活的文档页签进行复制和分屏操作
* 为复制纯文本添加可配置的快捷键
* Ctrl+Alt+0/⌥⌘0 将标题转换为文本
* 查找替换支持替换超链接地址
* 数据历史文档中支持操作类型过滤
* 改进同步后全量重建索引判断
* 移除图片、音频、视频和 iframe 菜单中打开的点击提示

### 修复缺陷

* 链接中存在 HTML 标签渲染效果不一致
* 同步生成冲突文档报错
* 重复打开同样的页签
* 任务列表交互后无法通过任务框拖动
* 父文档名带有结尾空格导致子文档无法导出
* 在页签右侧或下侧打开功能失效
* 文档只有一个标题块时全选其行级内容后剪切刷新触发状态异常
* 模板渲染错误
* macOS 端复制粘贴时未保留原格式
* 居左快捷键 Alt+L 处理挂件时是居中不是居左
* HTML 块执行脚本导致白屏
* 禁止拖拽内容到文档标题框中
* 聚焦模式下拖拽列表项/标题到文档树后原文档错误
* 更新或删除文档后立即点击重建索引导致死锁
* 列表项中粘贴纯文本解析异常
* 移动端后退导致嵌入块错误

## v2.1.10 / 2022-08-28

### 改进功能

* 数学公式支持设置全局宏定义
* 提供重建单个文档索引的 API
* 支持导出系统日志
* 同步向导优化，登录后根据条件进行下一步提示
* 链接菜单中的转化为文本移动到删除上
* 使用 HTML 块时建议使用 `<div>` 包裹 HTML 代码
* 表格中的代码包含 `\|` 时复制纯文本应该剔除掉 `\`
* 改进云端同步图标
* 移动端实现状态栏提示

### 开发重构

* Android 端升级 SDK 为 32
* 升级 Electron

### 修复缺陷

* macOS 端无法使用 ⌘V 粘贴图片
* 复制粘贴带换行的文本拆分为多个块后解析不正确
* 拖动有序列表项生产超级块后撤销不正确
* 嵌入块中存在换行 SQL 语句时会被转换为段落文本
* 第一次打开页签有光标，关闭后第二次打开无光标
* 云端同步失败重试后导致旧数据覆盖新数据

## v2.1.9 / 2022-08-24

### 改进功能

* 桌面端拖入文件时可选择 `使用 file:// 绝对路径并链接` 或 `复制为资源文件并插入`
* 桌面端支持导出图片、音频和视频
* `清理未引用资源` 生成历史文件夹后缀改为 `-clean`
* 同步向导加入账号登录引导

### 修复缺陷

* 启动后会生成一次全量数据历史文件
* 行级公式复制转义问题
* 导出预览模式点击块引转换后的脚注跳转不正确
* 复制列表块纯文本内容不完整
* 移动端复制纯文本内容不完整
* 移动端后退到动态页面下拉无法加载
* macOS 端中带 alt 的组合数字键失效
* 设置为预览模式后重启打开页签一直转圈
* 编辑器窗口没有激活时 Ctrl+W 会关闭当前窗口中的所有页签

## v2.1.8 / 2022-08-22

### 改进功能

* 斜杆菜单模板预览
* 支持复制为 HTML
* 暗色主题下 `mindmap` 背景颜色设置为透明
* 为链接新增转化为文本选项，删除选项修改为删除链接
* 改进没有模板时 `/模板` 的交互
* `pdf?page` 资源文件链接会被判定为未引用资源
* 每次启动程序不再自动创建 Documents 文件夹
* 大纲切换页面题头图闪现
* 支持任务列表通过任务框拖动
* 支持复制为纯文本
* 工作空间/conf/conf.json 文件读写不再加锁
* 复制粘贴支持首尾空格
* API `/api/filetree/duplicateDoc` 返回新文档的 id/box/path/hpath
* `.dev` 域名网址纳入超链接
* 重复（克隆）文档后自身内部块引不应该指向原文档中的块
* 快捷键中使用小键盘无效
* `file://` 协议兼容 Window 平台使用 `/` 作为目录分割线
* 云端同步设置向导
* 剪藏时过滤空的超链接
* 云端同步发生冲突时生成副本
* 移动端若干问题改进

### 开发重构

* 升级 Electron

### 修复缺陷

* 块引搜索结果出现 `<mark>` 标记
* Ctrl+Tab 切换焦点没有跟随切换
* 拖动并交换两个窗口后重启宽度没有保持现状
* 空列表中插入任务列表面包屑显示问题
* 点击折叠箭头菜单不消失
* 恢复文档浏览位置报错导致文档无法打开
* 图表删除后撤销下方出现空白
* 图表类内容块编辑框钉住改进
* 浏览器剪藏扩展剪藏某些网页代码块丢失注释
* 启动后已打开文档鼠标悬浮不显示路径
* 数据库索引报错卡住
* 列表中图片末尾光标和回车异常

## v2.1.7 / 2022-08-15

### 改进功能

* 移动端和 Web 端支持导入 `*.sy.zip`
* 添加复制块 ID 快捷键
* 字体设置不应该影响非编辑器内容
* 改进无法读写块树文件后的处理
* 改进 Windows 端默认工作空间路径
* 提升块标在边界范围的高度
* 关系图搜索框存在内容时不收缩隐藏
* 向上滚动文档时题头图不应该闪现
* 移动端插入 iframe 输入框位置错误

### 开发重构

* 升级 Electron

### 修复缺陷

* 列表中无法使用 `[]` 插入子待办列表
* 代码块设置换行后渲染异常
* 导出 PDF、HTML 和 Word 时未移除不支持的文件名符号
* 关闭所有文档后无法退出程序
* 启动时关闭所有页签失效
* 恢复文档浏览位置时最后一个块没有显示
* 图片居中后打开会多一行
* 表情无法滚动
* 使用查询语法搜索时编辑区高亮不正确
* 表格中的块引锚文本中包含 `|` 时导致表格解析不正确
* Windows 端 Ctrl+K 添加超链接后撤销触发状态异常
* 恢复文档浏览位置时折叠标题下方块被计入
* 停靠栏下方右侧仅剩一个图标后重启会被自动移动到下方左侧
* 查询未引用资源文件异常
* Android 端在选择插入图片或文件的界面返回会导致应用崩溃
* 带空格文件名的图片无法作为题头图
* Ctrl+T 创建标签后撤销再创建标记符重复
* 云端流量统计失效

## v2.1.6 / 2022-08-11

### 改进功能

* 使用快速滚动条时题头图显示错误
* 记录文档浏览位置
* 浮动工具栏跟随滚动
* 云端收集箱支持直接转发微信聊天记录
* 弹出菜单后禁止滚动
* CSDN 代码块剪藏会多出行号
* Android 端支持批量插入图片
* API `setBlockAttrs` 中如果存在属性值设置为 `null` 时移除该属性
* 图片缩放限制最小宽度
* 改进云端同步性能
* 复制多块后光标定位修改
* 后端服务支持 CORS 预检请求验证
* 数据仓库快照忽略隐藏文件
* 改进意外情况下同步点损坏导致无法同步的问题

### 文档相关

* 明确说明不支持通过第三方同步盘进行数据同步

### 开发重构

* 将解析渲染器 Protyle 项目合并到 Lute 项目中

### 修复缺陷

* 大纲、引用块点击应跳转对应激活的页签
* Markdown 围栏代码块相关问题
* 插入/更新资源文件导致内核崩溃
* 标题转换文档后编辑原文档触发状态异常
* 鼠标移动在光标所在的块标上时进行剪切，粘贴后的块标会高亮
* 折叠标题剪切后没有展开下方块
* 优化排版功能导致文档部分内容丢失

## v2.1.5 / 2022-08-02

### 改进功能

* 同一个块中引用多个相同块时反链去重
* 剪藏扩展无法剪藏简书的代码块
* 右键单击块引用元素时锚文本闪烁
* 划选复制内容范围改进
* 行内公式快捷键在公式打开界面误触以后会自动最小化程序
* 为 `网络图片转换为本地图片` 提供可设置的快捷键
* 剪藏和粘贴网页内容加粗语法兼容处理
* 移动端支持标签和书签面板
*  局中、左和右的快捷键不支持多块且块和图片没有进行区分
* `[[`和快捷键插入的引用需进行区分

### 开发重构

* 升级 Electron

### 修复缺陷

* 嵌入块更新时渲染效果不一致
* 图片后 Backspace 无法删除问题
* 图片居中会多一个软换行
* Markdown 围栏代码块输入问题
* 图片调整大小后光标在图片后面显示异常 
* 查询嵌入块多层嵌套导出不完整

## v2.1.4 / 2022-07-30

### 改进功能

* 新增 跳转到上一层级的下一个块
* 搜索结果加入命中统计
* 划选单元格后右键应取消划选
* 选中单元格菜单顺序调整

### 文档相关

* API `渲染模板` 文档描述

### 修复缺陷

* ECharts、PlantUML 和 Mermaid 等图表块报错
* 无法原义粘贴含有 `~foo~` 的文本
* Windows  端后退按钮状态不可点击但可以使用快捷键
* 光标无法定位到表格内的行内公式后
* 上传标记快照报错 `key doesn't match with scope`
* 开启 `在当前页签打开` 后 Ctrl+[ 会在新页签打开
* 开启 `在当前页签打开` 后当有多个未修改页签时新页打开位置错误
* 导出 PDF/HTML 时丢失自定义表情

## v2.1.3 / 2022-07-27

### 改进功能

* 笔记本支持导出 `.sy.zip`
* `清理未引用资源` 支持跟随 assets 文件夹符号链接
* 改进导入 Markdown 时 `<img>` 标签的处理
* 改进云端同步性能
* 发起互联网服务请求时绕过安全策略
* 支持代码块搜索定位

### 修复缺陷

* Android 端资源文件 `打开` - `在浏览器中查看` 卡住
* 云端同步 `syncignore` 未生效导致误删文件
* 代码块移动光标 ↑ 跳过问题
* 不启用同步时无法使用备份
* 脚注定义元素语法问题兼容
* 光标在图片后面显示异常
* 窗口状态无法保留
* 移动端右侧子菜单超出屏幕
* 安卓端文档树上的属性项内容无法保存
* 移动端输入文字后面包屑无法再次显示
* 优化排版功能导致文档部分内容丢失
* 引用和链接菜单重合
* 移动端浏览器打开空链接白屏且菜单超出屏幕
* HTML 块选中部分内容无法复制

## v2.1.2 / 2022-07-22

### 改进功能

* 云端同步和云端备份不再限制单个文件大小
* 支持通过密码短语派生数据仓库密钥
* Ctrl+X 剪切后光标应跳到下一行行首
* 同步合并数据时不依赖系统时间
* 取消数据同步时的进度遮罩
* 引用锚文本中粘贴行级元素后转换为普通文本

### 修复缺陷

* 选中块之后按 Ctrl+A 或 Ctrl+B 会在当前块前后插入新行
* 当光标在文档标题栏时按下 `定位打开的文档` 焦点依然在编辑区
* 块聚焦后反链面板没有跟随聚焦
* 分割线后回车不会自动换行到下一行
* 移动端 `/` 弹出的行内元素不可用
* 搜索结果未转义导致脚本执行或白屏

## v2.1.1 / 2022-07-21

### 改进功能

* 云端图床支持按页批量删除
* 导出 Word 时可选择不导出资源文件
* 导出 PDF、HTML 和 Word 时支持覆盖已有文件
* 删除遇到不可编辑的块时不应该选中该块
* 使用文档树切换文档时无法回到上次编辑位置
* macOS 端程序图标调小
* 启动时同步进度展现
* 改进访问授权验证码安全性
* 脑图和图表设置高度后没有即时生效
* 同步下载后如果 20% 的文件存在更新则全量重建索引
* 开启自适应宽度的两种状态下设置不同的选择器
* 分割线后是不可编辑块时，光标落于分割线后
* 改进选中或光标位于不可编辑块时回车和 Ctrl+/  的行为

### 修复缺陷

* 剪切板复制 `粘贴转义文本` 丢失 `\`
* 快速切换文档，大纲、局部关系图和反链未切换
* 移动端点击空白后菜单不消失
* `定位打开的文档` 快捷键执行后光标没有离开编辑器
* 剪切撤销大段内容后导致内容错乱
* macOS 端启动时有概率会报错
* 复制块粘贴到选中的文本中，撤销后选中的文本无法恢复
* 移动端数据快照无法操作
* 移动端表情无法滚动
* 在鸿蒙系统上同步后报错 `cloud object not found`

## v2.1.0 / 2022-07-18

### 引入特性

* `/资源` 搜索时支持图片预览
* 资源文件重命名
* 云端同步数据仓库

### 改进功能

* `/资源` 支持搜索未索引的文件
* 开启 `启动时关闭所有页签` 后仅启动时关闭没有钉住的页签
* 优化插入文件链接的锚文本和图片的提示文本
* 收集箱列表项鼠标悬停显示完整标题与摘要
* 移动端新建日记均弹出选择笔记本的界面且默认选中上一次选项
* 访问授权码输入错误 3 次后加入验证码
* 链接菜单中支持 Tab 和 Shift+Tab 切换输入框
* 优化云端同步上传性能
* 优化云端同步下载性能
* 调整桌面端程序图标大小
* 系统唤醒后网络报错 `no such host`

### 开发重构

* 日志组件单独抽取项目

### 修复缺陷

* 顶部停靠栏与底部停靠栏图标多于一个时排列错误
* 编辑器模式切换后面包屑菜单按钮位置错位
* 有些文档的内容和块动态加载有问题
* 关系图全屏再最小化后打开文档会自动全屏
* 使用 Delete 删除文档后菜单不消失

## v2.0.27 / 2022-07-14

### 改进功能

* 支持设置启动时关闭所有页签
* 编辑器 `...` 菜单增加 `优化排版` 和 `网络图片转换为本地图片` 图标
* 当选中块时添加按键 `A`、`B` 和 `Enter` 交互
* Ctrl+Tab 切换面板添加路径显示
* Search ignore case supports Unicode character folding
* 系统公告消息提醒
* 当前应用的主题更新即时生效
* 云端同步和备份忽略 `.git`

### 移除功能

* 不再支持 Android 32 位系统
* 移除旧版云端同步和备份功能入口

### 修复缺陷

* 引用锚文本中粘贴行级元素问题
* 移动端点击外观会触发颜色面板的选中
* Windows 端笔记本右键 `打开文件位置` 报错

## v2.0.26 / 2022-07-12

### 改进功能

* 当自定义排序时支持在文档上/下新建同级文档
* 桌面端编辑器设置支持自适应宽度
* 新增西班牙语 `es_ES` 支持
* 多语言配置缺失项使用对应英文配置项补齐
* 块备注过长时悬浮提示不完整
* 云端支持多个数据仓库目录
* 数据仓库支持云端备份
* 公测云端同步数据仓库
* PDF 标注高亮文字中间出现空格
* 切换到新的工作空间时优先使用系统语言
* 反链面板多出一条横线
* 改进分割线回车交互
* 为动态加载增加保持所有已加载内容的选项
* 粘贴内容包含 `/`、`#`、`、`、`[`、`{{` 和 `:` 时不进行提示
* 在右侧/下侧打开时如果已有页签将不再打开新页签
* iPad 端的外观调整
* 支持自定义同步快捷键
* 忽略环境变量中的代理设置
* 网络代理支持 HTTPS 协议，移除 HTTP 代理协议
* ↑/↓ 键经过非编辑块时不应变为选中状态
* 文档树面板隐藏时 `定位打开的文档` 快捷键需打开文档树
* 识别未引用资源时考虑 PDF 标注引用
* 动态加载滚动太快时无法继续加载
* Enter 键长按可以进行多次换行

### 开发重构

* 移除移动端不需要的前端依赖
* 简化数据同步计划任务实现
* 重构内核代理设置
* 升级 Electron

### 修复缺陷

* 表格单元格内剪切行文本导致换行失效
* 浏览器端开启访问鉴权后文档更新未保存
* 复制资源文件后导出再导入后丢失
* 行内代码删除后不保留标记符
* 块标菜单选择起始/末尾插入行后未取消选中状态
* 导出 PDF 图表无法渲染
* 切换/关闭页签后状态栏字数统计没有更新
* 左侧分屏拖拽到右侧分屏的左侧应保持分屏位置不变
* 当打开多个 PDF 时 PDF 内搜索功能失效
* PDF Annotation 锚文本中无法直接插入 Emoji
* 嵌入超级块中的列表项删除撤销后状态异常
* 反链面板拖拽列表项到文档中异常
* Ctrl+K 后 Esc 应清除空链接
* 如下布局右侧打开文档失败
* 关系图全屏再最小化后无法移动窗体
* 关系图全屏后 Ctrl+W 最小化失效
* 移动端菜单外观超出屏幕
* 拖拽后代码块异常及不能撤销

## v2.0.25 / 2022-06-29

### 改进功能

* 状态栏显示选中内容的字数
* 新增获取块 kramdown 源代码的 API
* 改进 Ctrl+Tab 支持选择切换
* 桌面端支持隐藏底部状态栏
* 同步按钮和反馈按钮互换位置
* 改进桌面端底部状态栏通知
* 同步忽略文件配置使用 gitignore 规则
* 图表和脑图宽度自适应
* 移动端文件树的文档菜单添加属性项、文档树菜单顺序调整
* 数据仓库快照支持 `syncignore` 配置
* `中西文间插入空格` 改为 `优化排版`

### 文档相关

* 用户指南 `数据安全` 文档中加入 `威胁模型` 章节

### 修复缺陷

* 使用块滚动条跳转到最后一个块时无法加载上面的块
* 新建文档填入文档名后大纲显示 `Untitled`
* 链接的打开菜单和点击事件保持一致
* 复制资源文件后导出父级文档再导入问题
* 打开文档不会更新文档树聚焦、大纲和反链面板
* 代码块上方为折叠块时 ↑ 键预期错误
* 点击大纲顶部标题未跳转到文档开始
* `优化排版` 影响行级标记符
* 导入 `.sy.zip` 后查询嵌入块失效
* 缓存参数设置不正确

## v2.0.24 / 2022-06-26

### 改进功能

* 支持设置页签打开最大数量
* 块内容较多时全局搜索定位到关键字命中位置
* 多次动态加载以后卡顿问题
* 改进复制 PDF 标注到笔记中时对换行的处理
* 列表项中包含多个块时 Ctrl+↑/↓ 功能等同于点击三角形
* 支持搜索超链接元素 URL
* 减少因向上动态加载的代码块渲染导致的偏差
* 标题和列表项以外的块标拖拽到文件树上不应有交互
* 桌面端加入底部状态栏
* 隐藏/显示停靠栏加入鼠标悬浮菜单
* 移除顶栏 `...` 菜单

### 文档相关

* 用户指南 `引用内容块` 文档中加入 `锚文本` 和 `语法` 章节

### 开发重构

* 内核中的 HTTP 客户端拆分项目

### 修复缺陷

* 部分以 `\*` 开头的行级元素未被转义并在粘贴后撤销时触发状态异常
* 包含 `Enter` 的组合快捷键设置错误
* 粘贴代码块后点击菜单按钮会弹出复制提示
* 文档第一个块使用查询嵌入块触发状态异常
* PlantUML 使用 Tab 键缩进内容时导致内容丢失且无法恢复
* 云端备份增量失效
* 关闭文档后大纲面板仍然有内容
* 搜索历史提示转义处理
* 严格校验思源块超链接地址

## v2.0.23 / 2022-06-22

### 改进功能

* 支持粘贴转义文本
* 改进英文快捷键搜索为大小写不敏感
* 复制 Edge 浏览器 URL 粘贴特殊符号额外转义
* 行级代码和键盘元素为空格时应该保留

### 开发重构

* 数据仓库索引文件存放在 `indexes` 文件夹下

### 修复缺陷

* 粘贴公式触发状态异常
* 大纲悬浮提示转义问题
* 悬浮窗点击后会置于引用提示下
* 移动端文档树菜单层级错位
* 重启后已经存在的页签动态加载失效
* 书签面板层级显示错位

## v2.0.22 / 2022-06-19

### 改进功能

* 弹出的编辑框（如公式/Mindmap 等）支持撤销和重做
* 重置数据仓库
* 嵌入块添加 Alt/Shift/Ctrl 点击
* 全局关系图中将标签链接到文档块上
* 添加文档树 `删除文档时不需要确认` 选项 
* 移动端超级块中表格改进
* 快捷键支持 Alt+F1-F12
* 快捷键提示中 Enter 修改为 ↩
* 关闭分屏页签后光标消失

### 开发重构

* 数据仓库索引 `Index` 不再加密

### 修复缺陷

* 移动端菜单遮住左右侧栏
* 复制数据仓库密钥错误
* 折叠标题跨层级展开问题
* 文档标题编辑粘贴内容后不会进行修改
* 在提及中搜索不到结果问题

## v2.0.21 / 2022-06-17

### 改进功能

* 改进上下键遇到折叠的超级块和引述块时的交互
* 拖拽/删除后形成超级块下仅有一个块时取消该超级块
* 书签面板添加 `删除` 按钮用于删除书签组
* 云端同步时自动创建数据快照
* 希望修改公式时可以做到无鼠标操作
* 潜在的文件锁问题
* 改进云端同步和备份文件读写健壮性
* 优化清理未引用资源内存占用
* 代码块末尾向后删除改进
* Added multi-arch support for Docker image
* 桌面端顶栏加入订阅试用图标

### 修复缺陷

* Android 端无法复制数据仓库密钥
* 下载云端备份报错
* 编辑文档标题时空格和置空问题
* 重命名书签后属性面板中的书签属性没有更改
* 列表项回车断开触发状态异常

## v2.0.20 / 2022-06-15

### 改进功能

* 支持空列表项回车断开
* 数据快照
* 数据快照回滚
* 支持表格内容全部居中
* 悬浮窗最小高度调整
* 数据快照密钥初始化
* 改进端到端密码设置交互
* 列出 `在浏览器上使用` 的 IP 中默认加入 `127.0.0.1`
* 文档标题右键菜单修改为文本复制、粘贴、删除、全选和剪切

### 修复缺陷

* 清理未引用资源时未忽略 `custom-data-assets` 属性定义的资源文件
* 有序列表项缩放后，在末尾插入行会状态异常
* 快捷键无法更改
* 查找替换文档名以后动态引用锚文本未跟随改变
* Windows 端右上角的按钮会因为窗口压缩而消失
* 云端收集箱 SSRF（服务端请求伪造）漏洞

## v2.0.19 / 2022-06-12

### 改进功能

* 快捷键设置支持搜索
* 改进网络图片转换本地图片消息提示
* 为图片右键菜单添加剪切功能
* 文档图标区分是否存在子文档
* 更新节点 `updated` 时不再更新子节点 `updated`
* 补全光标在标题位置时缺失的快捷键
* 表头右键隐藏删除行菜单
* 右上角最大化/最小化/关闭这一组图标大小不协调

### 修复缺陷

* 第一次同步数据时报错 `index.json` 找不到
* 任务列表项完成状态改变以后 `updated` 字段没有更新
* 顶部工具栏切换明暗模式的提示语有误
* 移动端更换 Emoji 后文档树没有更新
* 移动端删除打开的文档应关闭编辑器
* 导入包含 `.` 的 Markdown 文件夹问题
* 鉴权页关闭窗口失效
* 移动端预览页面新建文档失败
* 使用 Ctrl+A 选中块时菜单栏状态没有体现
* 划选图片后不应出现文本工具栏
* 悬浮窗中点击菜单后 Ctrl+X 无效

## v2.0.18 / 2022-06-08

### 改进功能

* `定位打开的文档` 增加快捷键
* 文档树面板左右键优化
* 屏幕太长导致无法动态加载且无动态滚动条
* 快速连击使用模板时是在斜线的下一行出现
* 云端同步上传大量文件时不新增通知
* 菜单中划选输入框超出菜单范围会导致菜单消失
* 改进全局搜索多个关键字命中时高亮片段
* 块引搜索结果高亮片段改进

### 开发重构

* `工作空间/incremental/` 文件夹移动到 `工作空间/temp/incremental/`

### 修复缺陷

* 多级标题折叠后上级块引浮窗中未折叠
* 移动端没有通知消息时消息清除按钮未隐藏
* 全局关系图不显示
* 大纲行级元素渲染问题
* 移动端点击账号刷新失效
* 无法导出 `.sy.zip` 和 Markdown
* 云端同步下载文件时断点续传失效
* 英文单引号无法搜索
* 同一文档分屏后大纲定位问题

## v2.0.17 / 2022-06-06

### 改进功能

* 消息提示支持显示多条
* 移动端支持数据历史
* 删除、剪切和移动聚焦的列表项后返回上一层
* 优化浮窗加载个数
* 不使用原生 Emoji 的情况下文档内表情提示和最终输入结果保持一致
* 支持学生优惠订阅
* 大纲面板和大纲页签改进
* 为桌面端添加 `同步模式` 选择
* 改进云端同步目录名称校验
* 优化云端同步上传资源占用和耗时
* 大纲点击跳转时会先跳转到文档开始
* 提供消息提示推送 API 接口

### 修复缺陷

* 浏览器网页图片和文字直接拖入编辑器报错
* 导出预览模式更多菜单按钮位置异常
* 云端备份下载恢复报错
* 嵌入块中禁止拖拽列表圆点
* 历史数据无法切换笔记本
* 大纲文档图标不跟随文档切换而切换
* 标题下有代码块时剪切标题后撤掉导致状态异常

## v2.0.16 / 2022-06-02

### 改进功能

* 免费提供一个月的订阅试用
* 代码块横向滚动条改进
* `Echarts` 图表块无法渲染 3D 图表
* 桌面端资源文件链接支持菜单操作
* 断网时能够暂停云端同步
* 更换隐藏/显示停靠栏图标
* 为 Mermaid 添加支持 HTML 标签功能
* 升级 Mermaid

### 开发重构

* chore: update eslint deps
* chore: deprecate `node-sass`

### 移除功能

* 付费订阅不再支持退款

### 修复缺陷

* 移动端导入大于 32M 的 Data 包失败
* 代码块语言选择列表覆盖在设置页面上
* Android 端状态栏在明亮模式下不正确
* 过早提示订阅即将过期

## v2.0.15 / 2022-06-01

### 改进功能

* Docker 和移动端支持导出模版、Markdown 压缩包和 `.sy.zip` 数据包
* 云端数据同步时降低交互阻塞时间
* 书签面板 Emoji 和折叠问题
* 通过 GitHub Actions 实现每日构建
* 桌面端托盘图标菜单加入菜单项
* 桌面端 `设置` - `账号` 中增加订阅续订入口
* 改进 `网络图片转换为本地图片` 微信图片拉取
* 改进 `网络图片转换为本地图片` 文件名后缀
* 同步下载支持断点续传
* 每次打开帮助文档时自动检查版本更新并提醒
* 开放 insider 内部预览版本仓库
* 新增内核启动参数 `mode`
* 改进移动端打开引用缩放逻辑和桌面端一致

### 文档相关

* 修改隐私政策
* 使用 GitHub Projects 描绘路线图

### 开发重构

* 前端使用 `pnpm` 管理构建

### 修复缺陷

* 一级标题被面包屑强制省略两个汉字
* 单元格所在行有跨行的单元格时禁止删除行
* 标题中设置字体颜色问题
* 打开页签时应跳过钉住的页签
* 关闭页签后大纲没有刷新

## v2.0.14 / 2022-05-29

### 改进功能

* 光标在文档标题位置时可使用快捷键唤出大纲、反链和关系图面板
* 大纲面板加入文档标题
* 完整开源界面和内核
* 面包屑继续改进 
* 插入较大的资源文件时内存占用较大
* 桌面端托盘图标菜单加入 `Hide Window`
* 锚文本修改后 `Enter` 可退出修改层并跳转到锚文本末尾

### 文档相关

* 公开内置密钥算法
* 修改用户协议

### 修复缺陷

* 文档标题处粘贴内容会选中粘贴的内容
* 标题存在字体颜色时大纲面板异常
* `网络图片转换为本地图片` 链接参数处理异常
* 表格内颜色字体不能换行
* 文档树拖拽子文档后排序不正确

## v2.0.13 / 2022-05-25

### 改进功能

* 更新嵌入块内容不重新渲染嵌入块
* 更新主题后不需要对该主题进行切换
* 新开页签应置于当前激活页签的右侧
* 大纲面板移除顶层文档标题
* 面包屑鼠标悬浮文字显示不全
* 改进同步下载数据稳定性
* 笔记本配置文件丢失后重新生成名为 `Untitled` 的笔记本配置
* 局部关系图中添加文档链接关系
* 文档树引用计数不使用缓存
* 搜索页签和浮层路径长度最大调整
* 云端数据同步和备份时忽略隐藏文件

### 移除功能

* 移除局部关系图 `层级` 参数

### 修复缺陷

* 表格内粘贴 HTML 时异常
* 无法给整行文字设置样式，只能应用于行内部分文字

## v2.0.12 / 2022-05-23

### 改进功能

* 面包屑改进：`Click` 为聚焦，聚焦返回文案修改
* 搜索页签和全局搜索支持 `Alt+Click` 打开分屏
* 改进启动报错窗口
* 支持导入文档数据包 `.sy.zip`
* 对选中的单元格支持清除内容
* 支持导出文档数据包 `.sy.zip`
* 选中图片后支持 `Ctrl+X` 和 `Ctrl+C`
* 大幅提升启动速度
* iOS 端根据系统语言初始化外观语言
* 增强锚文本的键盘操作
* 在 PDF 标记和链接内可以使用 `Ctrl+/` 弹出对应的菜单
* 桌面端在系统睡眠和唤醒时进行一次数据同步

### 文档相关

* README 中增加 Docker 部署说明

### 移除功能

* 移除 `退出界面时关闭内核` 选项

### 修复缺陷

* iOS 端图片保存到相册导致崩溃
* `Alt+X` 选择状态问题
* 窗口拉窄后顶部工具栏消失
* 类型转换导致折叠标题下方块丢失
* 复制内容为空的块作为块引用时粘贴无效
* 列表项下图片删除后撤销触发状态异常
* 代码块未显示行号时切换语言界面错位
* 对已有颜色的文字添加不了背景
* 聚焦后折叠会触发重建索引
* 悬浮窗面包屑显示异常

## v2.0.11 / 2022-05-19

### 改进功能

* 记住集市的排序状态

### 修复缺陷

* OPPO 移动端上超链接无法打开浏览器
* 桌面端初次安装向导选择工作空间报错
* iOS 端导出 Data 无法下载
* iOS 端关于页未显示 IP 地址

## v2.0.10 / 2022-05-19

### 改进功能

* 桌面端初次安装启动后向导
* `---` 插入分隔线后如果下方已经存在块则不生成空白段落
* 通过链滴系统通知发放终身会员激活码
* 子块的宽度不够填满超级块时无法调整超级块的整体布局
* 大纲点击折叠标题跳转聚焦
* 文档第一个块为包含嵌入块的列表时下上键无法在文档标题和第一个块之间切换
* 优化新版主题头部及 Windows 端关闭按钮
* 改进集市包更新版本对比
* 内核启动参数加入 `lang`
* 使用快捷键调出颜色面板
* 改进云端同步 404 问题
* 云端同步初始化默认 `main` 目录

### 修复缺陷

* 导出 `段落开头空两格` 失效
* 引述块下删除列表触发状态异常
* 头部为列表时上键光标会跳到标题位置
* API `appendBlock` 插入渲染重复
* 大纲点击产生动态加载后 `data-doc-type` 错误
* 同步后文档树文档图标没有更新
* 编辑器和标题中 `Alt+5` 会留下字符
* 云端同步偶尔报错 `The system cannot find the path specified.`

## v2.0.9 / 2022-05-16

### 改进功能

* 订阅激活码
* 集市包增加下载数量统计
* 集市包支持按下载数量排序
* 块超链接光标定位
* 结束推荐订阅送终身会员活动
* 更换默认的笔记本图标
* 云端同步传输数据使用 HTTPS
* 回滚文档数据时保持原路径
* 改进数据历史文档页签选择笔记本入口
* 代码块横向滚动问题
* 优化开启同步时的启动速度
* 默认主题优化
* 列表项聚焦返回和面包屑保持一致

### 开发重构

* 块树索引加入 `HPath` 字段

### 修复缺陷

* 块标纵向排列以后折叠小三角失效
* 删除文档图标后默认图标不正确
* 聚焦返回问题
* 文档页签标题被 Emoji 覆盖
* API  `/api/block/updateBlock` 更新文档块为空的问题
* 重命名文档动态锚文本未跟随
* 创建日记问题
* 移动端无法保存书签属性
* 下载恢复备份问题
* 同步过程中断导致的一致性问题

## v2.0.8 / 2022-05-12

### 改进功能

* 块图标竖项排列时进行倒序
* 下线旧版云端同步服务
* 外观菜单修改为横排
* 调整启动/退出时云端同步网络连接超时为 15s
* 修改用户协议
* 支持导入 Data 压缩包
* 改进桌面端获取系统 ID 方式
* 为超级块添加快捷键
* 取消提示浮层移除延时
* 云端同步接口使用 `443` 端口
* 默认主题微调
* 改进集市界面加载速度
* 聚焦返回后定位到当前块

### 开发重构

* 缓存引用元素

### 修复缺陷

* macOS/iOS 端监听资源文件报错导致启动卡住的问题
* Android 移动端导出 Data 未弹出下载
* 嵌入块 SQL `LIMIT` 失效
* 插入引用后立即修改定义块后引用锚文本未变化
* 跨文档拖动列表项块后源文档状态异常

## v2.0.7 / 2022-05-10

### 改进功能

* 钉住页签改进
* 移动搜索带关键字时过滤笔记本
* 文档修改时间展示使用 `updated` 属性

### 修复缺陷

* 启动闪退
* 全局搜索无法弹出

## v2.0.6 / 2022-05-10

### 改进功能

* 钉住的页签使用 Emoji 或标题的第一个字
* 文档树增加随机图标按钮
* 改进移动端/Docker 端新版同步算法
* 数据历史未展示最新的条目
* 在设置外观中切换模式导致闪烁
* 自动同步下载失败次数过多则调整同步间隔为 1 小时
* 标签元素错误结构订正
* 文档页签鼠标悬浮提示全路径
* 数据变动后 30 秒进行一次同步
* 桌面端同步加入快捷键 `F9`
* 为合并超级块添加图标
* 字体颜色添加多个最近使用

### 修复缺陷

* 块标高亮无法自动消除
* 删除文档以后数据库中的数据没有删除
* SQL 不支持字符串拼接操作

## v2.0.5 / 2022-05-08

### 改进功能

* 改进反链面板标题下方块展现判断
* 改进块引设置
* 优化账号头像和背景图加载速度
* 浏览器端支持导出完整 data 文件夹 zip 压缩包
* `Ctrl+F` 指定路径搜索加入 `包含子文档` 选项
* 提升内核关闭速度

### 修复缺陷

* 浏览器端锁屏后无法使用
* 列表开头使用自定义 emojis 失效
* 全局搜索时通过 ID 搜索不应该需要开启查询语法
* `Alt+1` 文档树焦点不对
* 字体设置最近一次使用快捷键不对
* 从搜索打开时文档树未自动定位
* 块引排序问题

## v2.0.4 / 2022-05-06

### 改进功能

* 支持查看和回滚资源文件历史
* 代码块改进
* 搜索过滤支持自定义属性开关
* 搜索页签使用全文搜索实现
* 数据库表 `blocks` 新增字段 `tag`
* 搜索加入 ID 支持
* 当光标移动到图表等块上时统一显示编辑和菜单按钮，取消其点击事件
* 导入 Markdown 时避免所有块的 `created` 和 `updated` 一致
* 反链提及搜索使用全文搜索实现
* 统一历史数据存放位置为 `工作空间/history/`
* 备份恢复时生成历史
* API 请求鉴权提示优化
* Alt+H 不在编辑器中时无效
* 数据历史查看界面独立打开
* 全局搜索加入单独的查询语法选项
* 改进数据同步接口安全性
* 使用备份恢复时自动暂停同步
* 支持查看和回滚被删除的笔记本
* 改进收集箱剪藏稳定性
* 数据库 `content` 字段块级之间增加空格
* 集市挂件名称过长遮挡星标
* 为字体设置添加最近一次使用

### 修复缺陷

* 使用 API `api/block/updateBlock` 更新列表项时渲染错误
* 无法在下划线左侧粘贴
* 搜索替换不支持包含符号的关键字
* 嵌入块包含折叠标题时不应该显示其下方块
* 到达文档树最大列出限制以后排序不正确
* 在资源文件很多的情况下 macOS 端启动异常
* 在行首输入 `$$` 数学公式无法渲染
* 文档关系图不显示
* 表格中行内公式使用下标时再次编辑时光标位置不对
* 任务列表中无法使用鼠标选中文字
* 文档转换标题时 block not found

## v2.0.3 / 2022-04-29

### 改进功能

* 支持块引和块超链接的相互转化
* 文档名支持搜索替换
* HTML 块若内容为空时无法在数据库中查询到
* 外部修改已有资源文件后纳入云端同步
* 改进桌面端新版同步算法
* 全局搜索支持搜索自定义属性
* 收集箱剪藏失败时保留链接
* 剪藏扩展容错改进
* 收集箱公众号输入按日期合并
* 上架 OPPO 软件商店
* 引用块为折叠块时，点击应缩放进入该块
* 为订阅会员提供更快的集市包下载加速
* 代码块语言样式改进 & 移除复制按钮
* ECharts 图表块将编辑代码的功能放到菜单中
* 改进块引搜索时空格分隔多关键字处理

### 修复缺陷

* 新版同步云端目录无法删除
* 表格单元格中使用代码和 `|` 的问题
* 新版同步下载报错 404 问题
* 搜索折叠内容时面包屑显示错误
* 在文档首插入代码块后三击后退格会删除下一个相邻块的内容
* 引用文档时锚文本没有跟随文档重命名
* 某些情况下缺失 `updated` 属性
* 模板函数 `queryBlocks` 返回数量问题

## v2.0.2 / 2022-04-27

### 改进功能

* 桌面端支持导出完整 data 文件夹 zip 压缩包 
* 导出时题头图放在文档标题前

### 修复缺陷

* 旧版同步下载报错问题
* 新版同步上传报错问题
* 部分文档不能导出
* 部分网页无法使用收集箱剪藏问题
* Windows 端悬浮菜单和顶部标题栏重合时无法点击

## v2.0.1 / 2022-04-26

### 改进功能

* 导出时支持导出题头图
* 公式块悬浮窗增加固定键
* 绘图更新机制改进
* 导出 PDF 时可选择不导出资源文件
* 当光标不在表格区域中时表格无法被复制
* 改进搜索过滤
* 导出 Markdown 时去除公式内容中的首尾空格
* 改进 `Ctrl+K` 超链接粘贴识别
* 使用 API `getFile` 时自动解锁文件
* 导出 PDF 文案改进及记录上一次选择
* 新的数据同步实现
* 全选块时弹出排版工具栏
* 块引搜索默认使用双引号包裹
* 提及搜索时纳入超链接锚文本

### 移除功能

* 云端同步目录不再支持重命名

### 修复缺陷

* 粘贴文本到带有输入框的 HTML 块后该块消失
* 文档标题图标与文档第一个块图标重叠
* 导出 PDF 未加载样式
* 标题展开时进行动态加载导致重复内容
* 搜狗输入法划选输入中文再撤销触发状态异常
* 新工作空间初次同步下载或备份恢复时可能出现的报错
* 跨块多选转换导致顺序错误
* 三个空块合并的超级块导出模版后使用会变成两个块

## v2.0.0 / 2022-04-22

### 引入特性

* 云端收集箱
* 全局搜索支持查询语法

### 改进功能

* 结束早鸟订阅优惠
* 定义块引用计数浮窗高亮引用处锚文本
* 启用伺服时显示本机所有网卡的 IP
* 改进块引搜索排序规则
* 公式块悬浮窗在输入时会跳回默认位置
* 在 `DOM` 树中区分不同弹出搜索菜单
* 表格改进
* 支持使用 `* [ ]` 和 `* [x]` 创建任务列表
* 为订阅续订操作提供更醒目的提示对话框
* 网络代理支持 HTTP 协议
* emoji 面板中鼠标移动不会影响上下键对表情的选择
* 增加反馈按钮
* 为思源协议链接添加 Alt/Shift/Ctrl+Click 事件
* 升级 KaTex 并调整其行间距
* 浮窗改进
* 代码块语法高亮支持  `yul`、`solidity` 和 `abap`
* 推荐订阅送终身会员活动附加结束时间条件
* 题头图上下调整使用百分比
* 当存在链接时打开链接面板光标应该在锚文本上
* 改进导出 Markdown 的请求端点
* 改进自定义表情图片的请求端点
* 改进静态资源请求端点
* 废弃内核参数 `--servePath`
* 选中图片后回车，应取消图片的选中状态
* 改进代码块行号自适应
* 下载集市包时展示进度

### 开发重构

* 重制全局搜索
* 降级 Electron

### 修复缺陷

* Windows 端窗口异常问题
* 块引搜索条件为空时选择候选结果后没有创建块引
* 不含有子项的列表项在折叠同级之后，不应带有折叠标记
* 行级排版元素后软换行无法保存
* 某些情况下缺失 `updated` 属性
* 导出文档时未移除不支持的文件名符号
* 选中文字后中文输入时删除字母后无法撤销
* 行级公式 `$foo$1` 解析失败问题
* 代码块使用 `Tab` 缩进后渲染异常
* 合并过的表格无法在上方插入一行
* 列表项和标题转换为文档后反链异常
* 列表项-引述-列表-列表项 图标位置错误
* 列表回车跳出后撤销光标位置不对
* Shift+Click 无法从下往上选
* 从浮窗三击全选复制内容粘贴后触发状态异常
* PDF 标注浮窗引用重复

## v1.9.9 / 2022-04-11

### 改进功能

* 反链提及转换为引用时支持选择静态锚文本
* `((` 引用时容器块显示子块内容
* `blocks` 表新增字段 `fcontent`
* 改进列表折叠
* 改进搜索排序规则
* 自定义块属性名过长时无法完整显示
* 移动面板路径全显示

### 修复缺陷

* 空的行级元素删除后撤销触发状态异常
* 云端图床部分内容删不掉
* 调用模板 SQL 查询报错
* 导出嵌入块时报错
* 超级块和标题展开折叠出现问题
* 拖拽如触发滚动后将无法进行移动

## v1.9.8 / 2022-04-09

### 改进功能

* 支持查看和删除云端图床资源文件
* 改进列表折叠
* 集市 `preview.png` 图片预览不完整
* 支持代码块高亮语言 `bat`, `graphql`；新增 felipec，intellij-light，tokyo-night-dark，tokyo-night-light 主题
* 播放图片时会闪屏一次
* 数据库表 `blocks` 中的 `length` 字段按 `content` 长度取值
* 部分 Windows 端上卡顿的问题
* 列表样式改进
* 虚拟引用排除当前文档名
* `((` 引用候选中排除当前块的父块
* 改进集市包预览图加载
* 桌面端主窗体背景色设置为 `#FFF`
* 改进搜索排序规则
* 搜索框的类型开关优化

### 开发重构

* 降级 Electron

### 修复缺陷

* 集市包偶尔显示不完全
* 鼠标右键单击段落中插入的自定义 `emojis` 时控制台输出异常
* `((` 引用列表项时使用第一个子块作为动态锚文本
* Ctrl+End 最后一个块无法加载
* 输入内容无法保存
* 查询嵌入块未禁止划选编辑

## v1.9.7 / 2022-04-07

### 改进功能

* 统计云端数据同步和备份使用的上传和下载流量
* Docker 端支持网络代理选项
* 改进鼠标悬浮提示文字的位置
* Docker 部署时如果没有设置 `--servePath` 参数则终止启动
* 改进云端同步和备份稳定性
* 搜索时 Ctrl+Enter 可打开新页签
* 改进搜索排序
* 移动端点击嵌入块直接跳转到正文
* 挂件块支持内置属性 `命名`、`别名` 和 `备注` 搜索
* 支持 HTML 块中使用 JavaScript
* 同步统计信息支持多语言
* 为复制嵌入块添加快捷键 Ctrl+Shift+E
* Linux 端支持 URL Scheme `scribli://`
* 移动端支持导出完整 data 文件夹 zip 压缩包
* 支持播放 .webm 视频文件
* 改进新建文档后的光标位置

### 开发重构

* 升级 Electron

### 修复缺陷

* Alt+7 反链面板切换文档时没有触发刷新
* 输入 6 个反引号 ` 后解析问题
* 复制并粘贴 5 个反引号 ` 后点击触发状态异常
* 生成的用户指南笔记本在文档树中排序异常
* 带有输入框的 HTML 块每次输入一个字符就失焦
* XSS 安全漏洞
* 非嵌入块的输入框中的 `隐藏标题下方的块` 开关未隐藏
* 调整表格宽度时鼠标拖拽状态保留异常
* iPad 端块滑条无效
* 空代码块中输入 `[]` 时变为任务列表
* 划选后输入触发状态异常
* 视频块点击进度无效
* 点击动态加载条跳转到最后一个块后无法继续动态加载
* 列表块删除后再撤销操作出现内容位置异常
* 符号转义后无法被搜出
* 划选多块撤销后没有充分还原
* 点击大纲后再点击动态滚动条后，反链显示错误
* 集市中同时支持浅色与深色模式的主题无法在过滤结果中同时显示
* 标题和文档互转后大纲未刷新
* 点击大纲后双击正文，面包屑高亮错误
* 设置完属性后引用数会消失
* 标题转换文档后文档标题状态错误
* 折叠标题导出为模板后使用会出现内容重复
* API `appendBlock` 插入多块时顺序不正确
* 焦点在表格中未能在文档树自动定位
* 启动后同步按钮悬浮未显示统计信息
* 移动无法搜索到新建的空笔记本
* iPad 无法设置字号
* 关闭左侧分屏后重启会重置窗口布局
* 云端同步服务宕机时不应该导致内核崩溃
* 缩放后 F5 刷新文档会导致面包屑高亮错误

## v1.9.6 / 2022-03-31

### 改进功能

* HTML 块将编辑代码的功能放到菜单中
* 嵌入块支持隐藏标题下方的块
* 减小 Android/iOS 端安装包大小
* 桌面端启用硬件加速
* 改进标题折叠后的视觉
* 剪切折叠标题后撤销，加载界面没有消失
* 点击块标弹出菜单后，无法通过快捷键的方式操作块
* Microsoft Store 版支持 `scribli://` 协议拉起应用
* 悬浮窗光标在编辑器左侧无法显示图标
* 改进云端同步和备份稳定性
* 页签钉住后不显示取消钉住按钮
* 反链提及转换为引用时使用动态锚文本
* 反链提及转换为引用后自动刷新反链面板
* 编辑器分屏视觉区隔
* 为当前选中的编辑器页签添加高亮显示

### 修复缺陷

* PDF 页签 Ctrl+F 和全局搜索冲突
* 默认折叠的块中代码块展开后不渲染行号
* 手动安装挂件包后挂件集市无法打开的问题
* 嵌入超级块时不应该展开其中的标题块
* 全选删除包含折叠标题，删除后快速输入触发状态异常
* 折叠标题后打开属性设置不显示
* iframe 块的资源不支持 `//` 开头的 URL
* 部分同步报错未记录到日志文件中
* 升级 PDF 库后的一些问题
* 锚文本反链提及搜索不到问题

## v1.9.5 / 2022-03-28

### 改进功能

* 预览图片时支持缩放和拖动视觉焦点位置
* 预览图片支持切换上一张和下一张
* 左右侧栏面板最小宽度限制
* 订阅到期提醒
* 减少代码块行号渲染抖动问题
* 订阅到期后云端数据会被立即删除
* 改进集市稳定性
* 改进云端同步性能
* 不允许在段首的块引用前进行软换行
* Android 端通过通知栏返回应用
* 微信公众号编辑器无法抓取云端图床图片
* 升级 PDF 库
* 表格中全选为选中当前单元格
* 桌面端禁用硬件加速
* 反链面板图标在拖拽过程中不弹出悬浮层

### 开发重构

* 更换内核使用的 HTTP 客户端库
* 更换内核使用的缓存库

### 修复缺陷

* 网络图片转本地图片失败
* 行内公式渲染换行问题
* 大纲面板条目中无法显示多个空格
* 任务列表粘贴到表格中触发状态异常
* 重启后通过引用打开的页签不应该进行聚焦
* 从 VS Code 粘贴 HTML 时解析错误
* 中西文插入空格导致内核崩溃
* Windows 端行级元素通过快捷键排版后输入中文撤销触发状态异常
* 引用列表块时动态锚文本未跟随定义块内容变动
* iPad 端不能新建笔记本
* 导出 Word .docx 失败
* 任务列表内的块拖拽到其他笔记后原始位置无法输入
* Ant 停靠栏图标在 Windows 端下无法显示
* 通过命名进行块引用时文本显示不同
* API `appendBlock` 插入多块时顺序不正确

## v1.9.4 / 2022-03-24

### 改进功能

* 提供读写文件 API
* 代码块中间换行，高亮错误
* 文档重命名不阻塞

### 开发重构

* 升级 Go 1.18

### 修复缺陷

* 动态加载时 data-doc-type 错误
* 划选引用搜索时触发状态异常
* 仅包含一个单元格表头剪切后解析报错导致内核崩溃
* Adnroid 端无法插入大的资源文件
* 折叠/展开标题撤销后状态不对
* 标题更改层级时出现 HTML

## v1.9.3 / 2022-03-22

### 改进功能

* 移动端复制粘贴问题
* 划选文字引用搜索支持输入关键字
* 光标在文档树上时支持 Ctrl+/ 唤起右键菜单，并支持对应的上下左右及回车操作
* 调整云端获取集市哈希和版本检查接口
* 续订时间不再限制 180 天后
* 划选块引用时，禁止弹出浮窗
* 重建索引时不再清理已过时索引
* 桌面端点击大纲中的文档名可以定位至文档顶部
* 使用 Sentry 自动上传报错信息和诊断数据

### 修复缺陷

* 使用 API  `/api/block/appendBlock` 往超级块中插入内容后渲染错误
* 使用 API `/api/block/prependBlock` 往任务列表项块中插入内容后解析异常
* 云端同步空间大小判断问题
* 选中块引用和文本时，剪切后有残余
* 段首连续换行时结果不一致
* 数据库 `spans` 表中上标元素 `type` 字段错误
* iPad 端 Shift+Enter 输入无效
* 光标无法进入剪切之后剩下的空段落
* 复制 Markdown 任务列表第一项为标题时错位
* 移动端点击块引跳转后，无法后退到块引原文
* 修改过标题后再次选中标题中的所有内容进行剪切，无法剪切整个标题
* 锚文本提及无法转换为引用
* 文档重命名不生效问题
* iPad 端切换页签大纲不会跟随切换
* 新打开的块中选中内容后使用搜狗输入法输入，再撤销会状态异常
* 容器块在顶层内容删除后撤销导致状态异常

## v1.9.2 / 2022-03-18

### 改进功能

* 表格支持最后一个单元格使用 Tab 添加下一行
* 光标在文档树等面板中，按 Esc 回到编辑器中
* 支持订阅续订
* 图片顶部对齐
* 当块选中且折叠时，使用上下键不应选择其折叠块

### 文档相关

* 改进导入导出帮助文档

### 修复缺陷

* 动态渲染的长文档后退问题
* 标题折叠没有折叠下方最后一个块
* 导入 Markdown 文件夹时卡住
* 块引用后软换行异常

## v1.9.1 / 2022-03-17

### 改进功能

* 改进标题和列表折叠
* 移动端只读状态下折叠和展开操作提示
* 标签批量删除
* 仅含有行级 HTML 标签的 HTML 块导出为模板后再导入会渲染为段落块
* 书签面板和标签面板上的删除操作增加确认对话框
* 折叠后光标移到被折叠块上
* 折叠列表下删除改进
* 折叠的列表项无法通过鼠标多选
* 数据库 `spans` 表中超链接元素的 `markdown` 字段未保存为超链接 markdown 文本
* 表格超长时粘贴会自动调整至最左侧

### 开发重构

* CSS 类 `b3-text-filed` 系列重命名为 `b3-text-field`
* 重构锁机制

### 移除功能

* 移除自动版本更新检查

### 修复缺陷

* 代码块里面剪切时触发前文 `#` 号标签搜索以及剪切抖动
* 导入 Markdown 文件内核连接中断
* 文档重命名后有时文档树没有刷新
* HTML 块复制/剪切后粘贴，块内容转义与解析错误
* 代码块中回车后滚动位置计算错误
* 创建文档时标题未转义 `"`
* 标签转义问题
* 代码块光标错误
* 列表项下代码块中按 `↓` 时光标移动问题
* 数学公式后无法显示光标
* 块引打开文档动态加载不全
* 右键弹出菜单错误
* 新开页签定位错误
* 嵌入块标题错误显示为 HTML 块
* 拖拽文档排序后位置不正确
* 创建列表项触发状态异常
* 移动端没有渲染 HTML 块
* 代码块中出现分隔线解析错误

## v1.9.0 / 2022-03-14

### 引入特性

* 书签重命名
* 支持 HTML 块

### 改进功能

* 思源超链接支持浮窗预览
* 编辑区宽度较小时块标弹出动画多次播放
* 将挂件的 `src` 与 `data-src` 属性由完整超链接更换为伺服根路径的绝对路径
* 公式输入框高度随编辑内容自动调整
* 思源协议块超链接加入 `focus` 参数
* 表格同时合并第一、二行时，可能会使第二行也成为表头
* 光标在块左侧时也需显示块标
* 增加快捷键获得当前文档的人类可读路径
* 降低文档重命名后文档树改变的延迟
* 增加根据 ID 获取人类可读路径 API

### 修复缺陷

* 折叠标题后未触发动态加载
* 使用 iframe 块嵌入挂件时部分操作会触发选中块并导致运行时异常
* 代码块粘贴代码时会出现闪烁问题并且光标被移动到最后
* 如果在一个块的上方嵌入该块，会导致无法正常修改原块的样式
* Windows 端下键无法触发滚动
* 标签面板中的条目内容未转义
* 粘贴块引时未转义锚文本
* 文档标题空格粘贴操作出现空格异常
* 标签比较长时自动补全不应该使用 `...` 省略
* 重建索引时卡住
* 折叠列表后回车没有创建新列表项
* 块聚焦时提及面板点击转换引用块没反应
* 列表中行级公式后进行退格会增加换行
* 端到端加密密码设置后提示不对

## v1.8.9 / 2022-03-09

### 改进功能

* 内容块支持两侧对齐方式
* 微软商店版任务栏图标问题
* 编辑器空标题光标无法进入
* 调整编辑器单个资源文件插入最大限制为 4G
* 优化数学公式、图表等渲染块编辑窗的弹出方式

### 修复缺陷

* 切换笔记时如果光标在表格中大纲不会自动刷新
* 选中图片后输入下一行，图片依然是选中状态
* 块属性备注栏 Ctrl+Z 撤销不撤销备注栏内容而撤销文档内容
* 导出 `data-export-md` 时未解析代码块与行内代码内的转义字符
* 代码块编辑光标乱跳和一些历史遗留问题
* 引用文档时锚文本没有跟随文档重命名
* 某些版本的 Windows 无法同步
* iPad 端启动偶尔白屏的问题

## v1.8.8 / 2022-03-08

### 改进功能

* 折叠按钮点击过快优化
* 改进云端数据同步服务稳定性

### 文档相关

* 改进云端同步文档

### 修复缺陷

* 图片居中后光标消失，无法通过 `Ctrl+Shift+↑/↓` 移动块
* 使用 API 插入空代码块后在其中粘贴时会粘贴至其外部
* 空的 `<kbd>` 无法删除
* 代码块编辑出现抖动
* 使用 API `/api/block/updateBlock` 无法更新文档块
* 表格中行内代码后换行问题
* 启动后大纲显示为空
* 中西文插入空格后代码块出现块 ID
* Pagedown 无法加载

## v1.8.7 / 2022-03-05

### 改进功能

* 列表项和标题折叠小三角
* Windows 端 PDF 导出代码块行号对不齐
* 存储时剔除文档标题首尾空格
* 代码块代码中划选后隐藏其他块内容划选触发的浮动工具条
* 拖拽上下分屏会导致滚动条滚动

### 修复缺陷

* macOS 端全屏幕后切换主题模式会导致工具栏左侧出现空白
* 复制块到表格触发状态异常
* 拖入资源文件到表格里会记录到上一个块里
* 列表编辑时动态加载触发状态异常

## v1.8.6 / 2022-03-03

### 改进功能

* Android 端上架小米应用商店
* `Ctrl+End/Home` 调整为在整个文档范围生效
* 在文档块的 DOM 中添加文档块属性
* 设置与搜索栏中的类型过滤相互独立
* 代码块行号和数学公式导出 PDF 改进
* `Alt+M` 隐藏窗口时不在任务栏上显示

### 修复缺陷

* 通过 Markdown 语法插入超级块时属性设置错误问题
* 粘贴文本到行级元素时插入位置不正确
* 移动端通过伺服下载某些主题后无法打开
* Markdwon 导入时 URL 编码后的图片地址导入失败
* macOS 中`Shift+Home/End` 范围选择错误及相关文档修改

## v1.8.5 / 2022-02-28

### 改进功能

* Android 端上架华为应用市场
* Android 端上架酷安
* 表格外部左右测禁止输入
* 禁止在分隔线、嵌入块、数学公式、iframe、音频、视频和图表渲染块内输入中文
* 改进某些情况下数据下载解密失败问题
* 浏览器剪藏扩展支持大尺寸数据网页
* 数学公式块上下对齐问题
* 导出 HTML/PDF 支持渲染表格合并单元格
* 浏览器剪藏扩展不申请剪切板读取权限

### 文档相关

* 上线官网英文版

### 修复缺陷

* 表格左右滚动时隐藏所选单元格
* 含标题行的拆分单元格错位
* 跨表头合并单元格错误
* 表格居中时选中效果渲染错位
* 表格居中时无法对单元格宽度进行拖拽
* `/` 后输入 `引用块` 和 `引述` 没有候选结果
* 脑图、图表删除再撤销后无法显示
* 导出 PDF 数学公式没有居中

## v1.8.4 / 2022-02-25

### 改进功能

* ECharts 和 Mindmap 支持高度设定
* Windows 端上架 Microsoft Store
* iPad 端文档点导出 Markdown 没反应
* 表格支持合并拆分单元格
* 取消表格列自定义宽度
* Android 端初次安装可选择拒绝隐私政策
* 设置 - 账号链滴社区服务协议与隐私条款改为勾选同意
* 支持长表格导出 PDF
* 导出 PDF 时代码块应保持折行
* 数学公式渲染改进
* 多选时禁止按键操作
* 折叠的块导出 HTML/PDF 时固定展开
* 关于中提供隐私政策和用户协议查看入口
* 移动端设置 - 账号中提供账号注销功能
* custom.css 编辑或通过外观设置修改后自动刷新
* 改进特殊情况下大纲需点击两次的问题
* 折叠列表子内容，按回车不会新建列表项
* Excel 粘贴时第一个单元格会出现换行
* 录音采样率调整为 44100
* Android 端编辑状态下键盘上方遮挡
* 图表块标菜单中不应出现代码块
* 集市包下载超时调整为两分钟

### 文档相关

* API 英文文档

### 移除功能

* Android 端移除录音功能

### 修复缺陷

* 桌面端更新版本时没有结束老内核进程
* 列表项下标题剪切问题
* 挂件编辑问题
* Android 端长按扩选后复制内容不全
* 当标题有 `#` 的时候导出 Markdown 失败
* 不关闭思源几天后可能出现账号鉴权失败
* 代码块折叠后下方输入文字错误
* 有序列表项插入行再撤销后序号错误
* 有序列表项拖拽、撤销再删除后导致的状态异常

## v1.8.2 / 2022-02-13

### 改进功能

* Android 端上架 Google Play
* 开源 iOS 端
* 移除编辑器光标置于空白行时的提示文案
* 点击图表交互时不应该触发代码编辑
* 表格支持调整列宽
* `Tab 空格数` 文案修改，提示不会修改复制的内容
* Android 端沉浸式状态栏
* 导出 PDF/Word 时 IFrame 块使用超链接

### 文档相关

* 更新隐私政策
* Android/iOS 开源协议使用 AGPLv3

### 开发重构

* 重命名内核可执行文件

### 修复缺陷

* Android 7 无法打开程序
* 操作系统休眠重进后卡在加载界面的问题
* 网络图片转换为本地资源文件文件名不合法问题
* /fgx 光标错误
* 分隔线复制会触发状态异常

## v1.8.1 / 2022-02-08

### 改进功能

* 加回 Ctrl+P 并对其进行优化
* 查找替换支持代码、公式和超链接
* 升级五线谱 abcjs
* Ctrl+Shift+F 通过上下键切换选择的搜索内容
* Android 端状态栏和全屏状态调整
* 开源 Android 端
* 图片之前的空行无法删除
* Android 端首次运行弹窗展示 `隐私条款` 和 `使用授权`

### 文档相关

* 用户指南 - 快捷键文档内容重复

### 开发重构

* Android 端升级 SDK 到 12.0
* Android 端最低版本要求从 5.1 改为 6.0

### 修复缺陷

* 三次点击块内部不能选中一句话而是光标跳到了下一行

## v1.8.0 / 2022-02-05

### 引入特性

* 挂件
* 挂件集市
* 支持查找替换

### 改进功能

* 跨页滚动多选提示
* 移动功能加入快捷键
* 设置 - 搜索文案改进
* 改进一个块中 Shift + 上下光标跨行选中
* 块引搜索条件为空时去重
* 提供 32 位的 Android 端安装包

### 修复缺陷

* 导出 PDF 文件时表格分页表头错位
* iOS 中文输入法无法保存内容
* 合并、取消超级块时，嵌入块会复制多一份结果
* 列表块撤销状态异常

## v1.7.11 / 2022-02-02

### 改进功能

* iOS 主题和安全距离适配
* 改进同步开关交互
* 网络图片转换本地资源文件支持 http 协议

### 修复缺陷

* 查询嵌入块刷新问题
* 点击集市包 README 中的一些链接导致白屏
* Shift+Click 在同一块内无法选中
* 网络图片转换为本地资源文件丢失后缀问题
* 移动折叠标题后倒序问题

## v1.7.10 / 2022-01-31

### 改进功能

* 创建文档副本
* 挂件块数据导出属性 `data-export-md`
* 云端同步自动检查间隔改为 5 分钟一次
* 内核退出时清理临时文件
* 将文档中的网络图片转换为本地资源文件
* 切换主题时如果该主题存在 theme.js 则直接刷新界面
* 集市包 README 渲染不启用软换行转换硬换行

### 修复缺陷

* 大于 100MB 的文件不应该计入云端空间大小检查
* 点击集市包 README 中的一些链接导致白屏
* 部分集市包 README 乱码
* 键入 SQL 查询后焦点没有自动跳转到 SQL 编辑框内
* 分割线中也可以输入文字

## v1.7.9 / 2022-01-28

### 改进功能

* iOS 端上架 App Store
* 改进集市
* 改进端到端加密密码设置后的提示文案
* 切换主题的时候通过 theme.js 增加的元素不会被清除
* 选中的文本为链接时 Ctrl+K 不使用剪切板中的链接
* 使用 API 创建笔记本时返回新建的笔记本的 ID
* 折叠后展开位置不对
* 被外部锁文件时提示 `未找到 ID 为 [xxx] 的内容块` 改进

### 修复缺陷

* 文档树面板和编辑器焦点切换问题
* Tooltip 拖拽后显示异常的问题
* 使用 API 插入空字符串出现错误
* 文档末尾粘贴时可能触发重复的动态加载
* 代码块三击复制问题
* 列表项行级公式结尾回车问题

## v1.7.8 / 2022-01-25

### 改进功能

* 多个视频相邻时 Shift+Click 多选无效
* 支持配置 PlantUML 服务器地址
* 上传资源文件到云端如果失败需要提示
* 默认限制创建文档层级最大深度为 7 层

### 修复缺陷

* 表格中难以在行内公式之前插入文字
* 按住 Shift 试图多选失败
* XSS 安全漏洞
* 云端同步报错 500 问题

## v1.7.7 / 2022-01-23

### 改进功能

* iOS 端短时间关闭屏幕会出现内核中断
* Windows 端安装程序工作空间确认框弹出一次
* iOS 端时区问题
* 设置搜索不到 `标签` 和 `Pandoc`
* Android 端状态栏不隐藏
* 提升 Android 端启动速度
* “在中英文间插入空格”功能对行内公式不生效

### 开发重构

* 升级 Electron

### 修复缺陷

* 打开文档并定位文档所在位置后打开日记，文档树会全部折叠
* 代码块后用中文输入法格式会乱
* 一些系统上播放多媒体文件崩溃的问题
* 引用文件夹被误计入未引用资源
* 三击列表项编辑时出现运行时异常
* 移动端只读模式光标插入符问题
* 导入 Markdown 后有时界面会卡死
* 移动端帮助文档有时无法打开

## v1.7.6 / 2022-01-21

### 改进功能

* 优化编辑器光标移动
* 图片支持单击后 Delete/Backspace 删除
* 文档编辑器 `...` 菜单中新增 `中西文间插入空格` 格式化功能
* 桌面端支持设置 Pandoc 可执行文件路径
* 通过 Pandoc 导出 Word .docx 文件
* 改进安装包下载分发网络

### 修复缺陷

* 导出 PDF 时行内加粗/行内斜体内的行内公式消失
* 题头图调整移动以后图片会被计入未引用资源
* 上下键无法选中数学公式和视频
* 资源文件上传 API 在未指定 `assetsDirPath` 时存放路径不正确

## v1.7.5 / 2022-01-20

### 改进功能

* 支持题头图上下拖动调整位置
* 主题支持加载 theme.js
* 顶部工具栏标题居中
* 新增按 Markdown 导出 HTML
* 大纲面板中的块标视觉调整

### 文档相关

* 新增微信提醒帮助文档

### 修复缺陷

* 字体加颜色并添加块引用后无法插入字符
* 使用 `[]`  输入任务列表时会导致父级列表被清空
* 在某些版本的 Windows 上无法正常进入应用
* 导出 Markdown 不应该带超级块语法

## v1.7.4 / 2022-01-19

### 改进功能

* 全局搜索支持类型过滤
* 标签面板排序
* 调整桌面端顶部工具栏
* 空标签数据订正
* 标签重命名判空
* :emoji 提示面板高度自适应
* 启动应用时转圈动画改为静态图片
* 拖拽分屏添加动效

### 修复缺陷

* 数学公式回车光标错误
* 快捷键修改后无法保存

## v1.7.3 / 2022-01-17

### 改进功能

* 标签重命名
* 相同文档分屏后，大纲、引用块点击无法定位到当前激活的文档中
* ctrl+shift+f 改为全局快捷键
* 文档树展开不丝滑
* 属性过长过多时仅显示一行
* 以下操作再次按快捷键时界面关闭
* ctrl+w, ctrl+f, ctr+shiftl+f 使用时不需点击编辑器
* Windows 端安装程序执行前弹出确认框
* 设置书签属性后编辑器内将书签渲染在命名左侧
* emoji 搜索时移除分类

### 修复缺陷

* PDF 导出不了图片
* 加载图标和主题使用文件夹名称而不是 json 配置中的名称
* 编辑器内属性值未转义
* 包含子文档时无法转换为标题的判断不正确
* 数学公式后连续回车，列表不会进行反向缩进

## v1.7.2 / 2022-01-15

### 改进功能

* 改进页签滚动条
* 插入链接时的一个小问题
* 移动文档后不再自动展开目标文档
* 钉住的页签不显示斜体
* Windows 端在缺失 robocopy 命令的情况下使用内置实现
* 主题和图标加载时带上版本号
* HTML/PDF 导出标签样式

### 文档相关

* 端到端加密具体内容说明

### 移除功能

* 移除 `![foo](data:image)` 图片解析支持
* 移除幻灯片演示
* 移除发光和彩色字体效果
* 移除中西文间自动插入空格

### 修复缺陷

* 文档树鼠标悬浮显示的文档备注未转义
* 使用非默认主题无法导出 HTML

## v1.7.1 / 2022-01-14

### 改进功能

* PDF 标注导出锚文本选项
* 导出 PDF 支持设置一些参数
* Linux 上无法导出 PDF
* 搜索页签添加历史
* 导出 HTML 时保留块属性
* 导出 HTML 时自包含 js/css 和 assets
* 导出 PDF 和 HTML 时支持超级块横排
* 导出 PDF 支持空格导出
* 云端备份上传去掉中间的确认框
* Alt+5 创建日记后光标应保持在用户可见区域内
* 拖拽标题后大纲没有更新
* 切换页签/搜索打开页签后大纲没有高亮
* 微信提醒设置后进行消息提示

### 移除功能

* 移除 Word .docx 导出

### 修复缺陷

* 设置属性命名后复制为引用块不生效
* 钉住页签在开启 `在当前页签中打开` 功能时失效
* 菜单点击后不消失
* 某些版本的 Windows 系统上每次同步时都会上传很多文件
* 文档树鼠标悬浮文档时不显示命名备注
* 移动端冒号输入表情错误

## v1.7.0 / 2022-01-11

### 引入特性

* 切换页签
* 钉住页签
* 支持网络链接题头图
* 移动端支持切换文档树排序模式
* 新增 `prependBlock` 和 `appendBlock` API
* 支持自动生成端到端加密密码

### 改进功能

* 去除下拉列表中初始自带的书签
* iOS 反链、大纲面板无法滚动
* 同步后进行增量索引
* 改进移动包含大量子文档层级的文档时的性能
* 来自 127.0.0.1 的本机资源文件请求不鉴权
* 移动端大纲点击后不应刷新界面
* 反链提及过滤中加入文档名
* 剪藏扩展时间格式改为 `yyyy-MM-dd HH:mm:ss`
* 延迟显示块标菜单提示
* 设置 - 账号加入个人主页背景图
* `insertBlock` API 支持多块插入
* 启动应用时的云端同步过程动态显示
* 云端同步和备份下载数据时先使用索引文件验证解密

### 开发重构

* 降级 Electron 

### 移除功能

* 移除同步间隔设置

### 修复缺陷

* 复制粘贴块后创建时间小于更新时间
* 开启访问授权以后导出 PDF/Word 时图片丢失
* 移动带子文档的文档时日志报错
* 反链面板请求响应顺序问题
* 一些系统上播放多媒体文件崩溃的问题

## v1.6.3 / 2022-01-06

### 改进功能

* 面包屑缩略悬浮预览
* 同步/备份限制单个文件最大为 100MB
* 移除 Rsync 统一使用 HTTPS 实现同步
* 改进云端服务网络稳定性
* 工作空间路径支持中文和空格
* 云端同步目录最多支持 7 个

### 修复缺陷

* 移动面板无法使用上下键
* 文档名的虚拟引用提示不存在符合条件的内容块
* Cannot access `showCreateConfDirErrBox` 
* macOS 上文档数量较多时索引失败
* 多设备同步时文件存在冗余的下载请求

## v1.6.2 / 2022-01-04

### 改进功能

* 移动块时更新修改时间
* 块标菜单增加 `移动`
* ~/.config/ 权限不足需弹窗告诉用户
* 调整云端服务网络架构
* 大纲项显示对不齐
* 超链接悬浮显示链接+标题
* 移动端添加 API token 的显示和复制

### 修复缺陷

* 笔记本名称为空时无法导出
* PDF 搜索无结果时显示错误
* 同一文档分屏后拖拽问题
* 大纲未渲染行级元素
* PDF 页签切换导致渲染异常

## v1.6.1 / 2022-01-02

### 改进功能

* 为超链接输入框中加入标题输入框
* 文档属性的多行备注预览被遮挡

### 修复缺陷

* iframe、嵌入块标菜单失效
* 文档标签重复入库问题
* 图片和超链接标题 `"` 未转义问题
* 快速删除文字后再撤销错误

## v1.6.0 / 2022-01-01

### 引入特性

* 移动端显示大纲
* 提供插入块、更新块和删除块的 API
* 微信提醒推送
* 移动端显示反向链接

### 改进功能

* 支持设置虚拟引用搜索过滤
* 标签面板添加引用计数
* 发起互联网服务请求时进行标准化处理
* 托盘菜单添加显示主界面
* cpp 代码高亮渲染错误
* 云端备份不再包括历史
* Windows 端安装时可选择仅为当前用户安装
* 提及关键字搜索最大支持 512 个

### 开发重构

* 升级 Electron
* 移除 blocks_fts 虚拟表

### 修复缺陷

* 浮窗中面包屑消失后不会重显
* ``` 后粘贴回导致状态异常
* macOS 端界面图标不显示
* `/` 菜单点击插入图片链接无响应
* 搜索资源文件时显示错位的问题

## v1.5.6 / 2021-12-29

### 改进功能

* 引用块和嵌入块搜索结果左侧缩略优化
* `/` 菜单加入插入图片或文件
* `/` 菜单加入创建子文档
* 编辑时不显示面包屑导航
* 支持设置反链提及搜索过滤
* 改进面包屑导航加载性能
* Windows 端安装时默认为所有用户安装
* 自定义表情不随标题大小进行变化

### 文档相关

* 繁体中文帮助文档

### 开发重构

* 升级 Electron

### 修复缺陷

* 复制公式块时的转义问题
* 导出 Markdown 时缺少带空格文件名的资源文件
* 预览中公式编号位置错误
* 云端同步时出现全量传输的问题
* 导出预览不显示自定义表情
* Windows 端同步文件权限问题

## v1.5.5 / 2021-12-26

### 改进功能

* 多选块外观设置
* 增加区分默认图标和自定义图标样式类
* Ctrl 点击多选块
* SQL 不支持字符串拼接操作
* iOS 端点击超链接跳转浏览器
* 优化层级较深的列表下保存数据的性能
* 自动切换同步方式，提升同步可用性
* 改进数据一致性
* 搜索界面反向链接浮窗被遮挡
* 降低编辑时的 CPU 占用
* 对自定义 emoji 搜索结果进行匹配优先排序
* 降低导入 Markdown 文件夹内存占用
* 插入资源文件时允许下划线
* 导出 Word 时支持通过属性设置 Pandoc 参数
* 剪藏扩展自动滚动页面加载图片
* 搜索页签改为上下布局
* 数学公式编号遮挡
* Windows 上代码签名仅支持 SHA256 算法
* iOS 端息屏后内核退出，再次进入时重新拉起内核
* Android 端顶部适配打孔屏
* 工具栏和停靠栏调大
* 改进块引锚文本实现方式以提升稳定性和性能
* 固定优先使用数据库
* 集市中主题安装后不应该再显示下载按钮
* 为数学公式提供字体变量 `--b3-font-family-math`
* 反链面板用户调整过布局后将不再自动计算
* shift+click 和 alt+click 调整
* Rsync 传输设置 30m 超时
* 移动端提供退出应用按钮
* 启动时不再根据内置主题自动合并 custom.css 
* 新增模板函数 `parseTime`
* 页签样式修改
* 切换引用后链接光标丢失
* 启动时不再根据数据内容校验是否直接使用数据库
* 云端备份下载并恢复由手动重启改为自动重建索引
* IPAD思源无法卸载主题，卸载APP后重装仍然有
* iOS 支持打开 Excel 和 Word
* 集市更新包时进行数据覆盖提示
* 改进集市加载和下载速度
* 升级 mermaid，支持用户图、git 图、类图、实体关系图
* 表格出现滚动条时，在末尾输入数字后滚动条会向前移动
* 支持复制块 ID
* 反向链接、书签、标签和关系图刷新按钮动效

### 开发重构

* 新增 blocks_fts 虚拟表用于全文检索

### 移除功能

* 移除导入时对 `[[Wikilink]]` 和 `#Tag` 的转换处理
* 移除文档树笔记本级重建索引

### 修复缺陷

* Android 开关网络伺服后未自动退出
* 折叠展开导致的内核崩溃
* 自定义排序后新建的文档排序不一致
* 集市包版本更新但不出现更新按钮问题
* 分屏情况下，全屏后文章显示错误
* 云端空间大小显示问题
* 图片光标乱跳问题
* 引用嵌套导出问题
* macOS 端打开时报错
* 在一些 Windows 系统上 SSH 同步报错
* 折叠标题后转换为段落丢失原标题下方块
* 反链面板搜索后 `显示更多` 无效
* 跨笔记本反向链接不全
* 导出为模板时空引述问题
* iOS 搜索面板无法滚动
* 存在两处 XSS
* 图表导出转义问题
* 【【 输入锚文本后 shift+enter 无效
* 无法关闭自动更新检查
* 自动清理历史遗漏了 data/.Scribli/history
* 无法搜索到 iframe 块、视频块和音频块
* iOS 端有时无法滚动进行动态加载
* iPhone 上 PDF 无法滚动和退出
* 嵌入块中脚本换行时导出为模板后不能正确解析
* 代码块 mermaid 不显示
* 表格内复制、样式问题
* macOS 鼠须管输入法在 `$x+y$` 后面无法输入内容

## v1.5.4 / 2021-11-28

### 改进功能

* 编辑器全屏后，其余编辑器也需要全屏
* 优化列表项粘贴到列表中
* 选中文本按 `Ctrl Alt A` 快速设置命名
* 增加插入代码块的快捷键
* 降低数据索引内存占用
* 窗口最小化时，思源协议无法唤起窗口
* 编辑器全屏后，为面包屑添加双击和拖拽
* Shift+Enter 换行在导出为 Markdown 时使用硬换行
* 模板和挂件搜索跳过 `.` 开头的文件
* PDF 支持 Ctrl + 鼠标滚轮进行缩放
* :: 后不进行表情提示
* 减少表情提示数量，以减少卡顿
* 统一内核 booting 启动界面
* 前进后退和文本块引搜索快捷键修改
* 移动端图片宽度菜单显示不完全
* 每个 PDF 需单独记住阅读位置
* 渲染块大小调整，不进行语法错误识别
* 优化反链面板加载性能
* 集市下载包问题改进
* 调整块引搜索列表大小
* 文档树排序切换后不再展开笔记本
* 分屏打开改进
* 反向链接面板计入文档自身引用
* 插入资源文件时自动将后缀统一为小写
*  反链面板“上下文”文案调整为“显示更多”
* 嵌入块为长文档时浮窗无法移动位置

### 修复缺陷

* iOS 端数据索引阶段崩溃问题
* 如果多选拖拽的不是第一个块标，拖拽后顺序错误
* 悬浮窗不应该在属性面板之上
* 资源文件名中带有 `#` 时打不开
* XSS 导致客户端 RCE 2
* 无法插入包含特殊符号文件名的文件
* 反链面板中的锚文本显示有误
* PDF 中的超链接无法点击跳转
* 列表和反链块标无法进行拖拽
* 搜索页签某些情况下无法打开
* 查询嵌入块脚本为空导致的卡死问题
* 表格同一行无法输入两个 `$` 
* 反链面板移动块导致的运行时异常
* 仅对 PDF 进行标注时无法触发同步
* 不显示提及
* 文档树排序对笔记本级别无用

## v1.5.3 / 2021-11-18

### 改进功能

* 支持多选块拖拽移动
* 优化列表编辑保存性能
* 使用同步时两次连续下载后不再退出内核
* 优化数据索引引用块性能
* 重命名包含大量子文档的文档时遮罩显示进度
* 优化数据索引解析 `.sy` 性能
* 优化属性设置保存性能
* 启动时优先使用已有数据库
* 使用 align 时，公式出现滚动条
* 支持对图片和链接的标题进行搜索
* 选择行内数学后点击公式按钮应取消公式
* Android 长按菜单和双击菜单重叠

### 开发重构

* 数据库锁模式切换为排它模式

### 修复缺陷

* `/code` 插入代码块问题
* 大量多层级文档时部分文档层级路径为空的问题
* 移动面板搜索不全问题
* 数据解析报错导致的启动闪退问题
* PDF 标注中包含双引号 `"` 的解析问题
* 块引用锚文本编辑时光标丢失
* 粘贴块引用锚文本为空的块
* XSS 导致客户端 RCE

## v1.5.2 / 2021-11-14

### 改进功能

* 录音改进
* 改进列表上下移动逻辑
* 同文档块引转脚注缩略定义
* 公式编辑输入框在窗口切换后失焦
* 修改面包屑部分样式会导致输入时卡死
* 普通提示和警告提示视觉优化
* 数据解析报错记录日志
* 导出时动态渲染锚文本最大长度按照配置处理
* 文档移动搜索时支持命名、别名和备注
* 文档移动支持移动到笔记本根路径

### 开发重构

* 重构数据文件读写包 `filesys`

### 修复缺陷

* 引用锚文本解析导致的挂起无响应问题
* 迁移 `~/.Scribli` 到 `~/.config/Scribli` 改进
* 输入 `//mermaid` 后回车报错
* 软换行后输入 ``` 报错
* 代码块不换行输入导致块元数据暴露并出现运行时异常
* PDF 多次打开

## v1.5.1 / 2021-11-11

### 改进功能

* 优化冒号后自动弹出 emoji 选择框的场景
* Emoji 加入斜杆菜单
* Docker 容器以 1000:1000 用户组执行
* 块引锚文本修改光标位置不正常
* 优化数据索引性能
* 为文档图标的 emoji 选择面板添加上下和回车键
* 编辑器内的 emoji 选择面板改为文档图标选择面板
* 端到端密码支持非数字和字母

### 修复缺陷

* 列表项转换文档触发运行时异常
* 查询嵌入块和引用嵌套导致的挂起无响应问题
* 端到端密码无法设置
* 使用 `~` 输入围栏代码块时运行时异常

## v1.5.0 / 2021-11-09

### 引入特性

* 列表项块转换为文档块
* 引述块转换为段落块

### 改进功能

* 块引浮窗交互改进
* 开发者工具警告`无法加载来源映射`
* `~/.config` 下 Electron 相关文件夹名称改为 `Scribli-Electron`
* HTTPS 同步复用客户端、回收空闲连接
* 能否加入\ket \bra等更全的 tex 符号支持
* 重建索引遮罩
* 浏览器环境下标题没有及时显示
* 文档标题支持复制引用快捷键
* 外观支持繁体中文语言
* 列表项中有多个块就允许折叠

### 修复缺陷

* 挂起无响应问题
* ctrl+k 在空块中无效
* 引用列表项时，引用块为文档时动态渲染锚文本为空
* 自定义快捷键bug——表格功能中左右方向键无法识别
* 空列表项上下无法选中
* 大纲跳转不正常
* 代码块输入块元数据后导致运行时异常
* 标题中如果正在拼写，使用下键会阻断继续的输入

## v1.4.8 / 2021-11-06

### 改进功能

* 日志大于 2M 后清空重置
* 列表项大于一个时 【】不再将其转换为任务列表
* 链接聚焦优化

### 修复缺陷

* App does not boot

## v1.4.7 / 2021-11-05

### 改进功能

* 移动面板支持跨笔记本
* 输入 `[]`，`【】` 时，转换为任务列表
* 优先使用系统 $PATH 下的 pandoc
* 同步配置向导
* 清理未引用资源时忽略挂件引用的数据文件
* 块引动态渲染的锚文本长度支持配置
* 移动端扩展可点击的空白区域
* 优化关系图渲染效果
* 优化块引搜索性能
* iOS 端同步完整性改进
* HTTPS 同步大文件性能改进
* 简化云端备份功能
* 超链接/块引设置对话框改进
* 推荐订阅送终身会员，512 个名额送完即止
* 双击悬浮层顶部栏进行 pin/unpin 操作
* 上传资源文件 API 支持覆盖已有资源文件
* 缩放聚焦容器块时计入反链
* 各面板中的树的箭头保持显示
* 日志文件存放到 temp 下
* 块引动态渲染的锚文本优先使用命名
* 没有块的时候对嵌入和引用进行搜索时应进行提示
* 在页签右/下侧打开文档，应保持在该页签紧邻的右/下侧
* 文件树上 alt/shift/ctrl 点击后不松开，依旧保持原有的打开方式
* 为引用打开悬浮窗/页签添加快捷键
* SQL 不支持公共表表达式
* 搜索/搜索页签/引用块打开文档，没有光标
* 全局配置目录放置到 ~/.config/Scribli
* 桌面端 app.log 放置到 ~/.config/Scribli
* 属性面板中的备注输入框增加高度

### 修复缺陷

* 修改密码后已登录账号的设备账号没有过期
* 工作空间路切换时的报错文案误导问题
* 数据库表 spans 未写入行级高亮元素
* 脑图不显示
* 引用提示请求覆盖
* 闭合行级代码后粘贴内容仍然在行级代码内
* 新建文档后位置错位及文档树定位错误
* 某些输入法下分隔线语法解析问题
* 列表项下挂代码块时竖线显示问题
* 大纲出现 `...` 问题
* 开启在当前页签中选项，关闭右侧页签错误
* 右键删除有序列表项后，序号不会改变
* 剪切板中为链接且无选中内容，ctrl+k 无效
* 转义符暴露搜索高亮代码问题
* 超链接、块引用、PDF 标注锚文本转义问题
* 任务列表完成状态改变时 updated 字段设值错误
* Linux dolpin 文件管理器复制粘贴图片无效

## v1.4.6 / 2021-10-24

### 改进功能

* Android 端支持上传文件
* 移动端点击空白处无法编辑
* 剪藏扩展插入原文链接、摘要和剪藏时间
* 移动端左右滑动改为前进后退
* 全局搜索路径缓存处理改进
* 粘贴文本中包含 `((` 时不应该出现块引提示
* 设置页面按钮宽度自适应
* iOS 端公测同步
* 优化 HTTPS 同步性能
* 移动端取消滑动拉出侧栏和搜索
* 支持嵌入 B 站视频时去弹幕

### 修复缺陷

* 数学公式导出 `&` 转义问题
* 反链面板容器块自动渲染锚文本问题
* 块引搜索转圈不停
* 移动端编辑器 `...` 菜单问题
* 剪藏时图片后标题解析问题
* 反链面板搜索关键字失效问题

## v1.4.5 / 2021-10-22

### 改进功能

* 引用超大的单个块会导致无响应
* 改进反链面板列表项子级展现判断
* 移动端返回桌面时进行一次同步
* 选择文本插入超链接改进
* 工作空间中文路径校验
* 图片菜单样式改进
* 列表中连续代码块之间没有空隙
* 打包所有 highlight.js 支持的高亮
* 改进引述容器为空的处理
* 大纲和嵌入块慢半拍改进
* 块引自动渲染锚文本最大长度设置为 64
* 查询为空时默认的块引排序规则按最近使用优先
* 优化文档树展开性能

### 修复缺陷

* 加粗和斜体等空格情况标记符暴露
* 设置账号无法登出
* 复制为嵌入块时重复
* 列表标记符暴露问题
* 导出软换行和有序列表序号问题
* 没有标签时输入 `#` 后转圈不停止
* 列表撤销导致状态异常
* 容器块自动渲染锚文本问题
* 最后一个代码块右键失效
* 自动空格导致输入法吞字

## v1.4.4 / 2021-10-20

### 改进功能

* Docker 镜像版本化

### 修复缺陷

* 嵌入块查询异常导致重新索引
* 频繁报错状态异常

## v1.4.3 / 2021-10-20

### 改进功能

* 优化暗黑模式下选择代码后的显式效果
* 行内公式与文字垂直居中排版
* 引用容器块时自动渲染锚文本改进
* 通过异步写入优化内核保存数据性能
* 改进输入实现
* 主题文件夹没有 theme.json 时不应该影响内核启动
* 剪藏某些网站代码块换行丢失
* 减少列表竖线间距
* 设置工作空间路径时校验不能放置在程序安装路径下
* 列表项折叠，除第一个子块外其余子块都隐藏
* 云端空间总大小在 .5G 时不显示
* 改进导出时嵌入和引用嵌套的处理
* 为搜索、块嵌入、块引用添加 loading
* 订阅推荐码改为付款后输入
* 嵌入块过长时，单击弹出的悬浮窗位置居下
* 删除笔记本时复制 assets 到全局 assets 下
* 表格内公式含有 `|` 符号导致表格异常分割
* 同步改进 - 引入基于 HTTPS 的同步方式
* iOS 初版公测
* 移动端自定义图标对不齐
* 内核连接中断退出调整为重建索引
* 移动端文档树禁用修改图标
* 为每个面板添加属性，增加自定义样式的灵活性
* 文档树动画效果
* 虚拟引用排除命中自身块命名和别名的情况
* 改进嵌入块查询性能
* 改进导入 Markdown 相对路径资源文件处理

### 移除功能

* 废弃导出选项引用块转换为原始块和引述块
* 去掉移动端设置按钮

### 修复缺陷

* 列表中引述侧边线对不齐
* 新建日记存放路径仅填 `/` 时文档打不开
* 列表项引用块下有任务列表时反链面板无法显示任何反链
* 系统休眠唤醒后同步报错 Failed to exec bin/ssh.exe
* 快捷键创建日志后文档树没有被选中
* 引用后的文本删除，锚文本会被填充内容
* 代码块里无法复制空格
* 视频居左拖拽大小异常
* 右键视频、音频、IFrame 禁止弹出复制粘贴菜单
* 列表折叠后的第一个子块竖线过长
* 导出为 Markdown 时段落开头空两格不生效
* 幻灯片播放后无法退出
* 表格中的换行复制粘贴后会变为 `<br>`
* 预览/导出使用块引转脚注时报错
* macOS 版全屏后无法关闭
* 同步排除的笔记本文档丢失

## v1.4.2 / 2021-10-11

### 改进功能

* 文档树拖拽文档层级时细化视觉
* 云端同步默认忽略帮助文档
* 跨笔记本移动时保持自定义排序
* 打包 Apple 芯片 arm64 版本
* 列表侧边竖线改进
* 改进 PDF 注解语法解析
* 13 后回车改进：由生成 1 同级列表修改为生成13下方的空块
* 开启同步时校验是否端到端密码为空

### 修复缺陷

* 软换行后移动数学公式导致内核中断
* 连续空列表项无法 shift + click 选中
* 网络角标显示问题
* 超级块下的列表项中再包含超级块时，样式错位
* `/无序` 唤出问题
* Android 端无法通过其他应用打开资源文件
* 主题配置文件损坏时内核启动崩溃

## v1.4.1 / 2021-10-09

### 改进功能

* 文档树双击为子文档的折叠或展开
* PDF 打开位置为包含 PDF 的页签组中
* 列表侧边竖线改进
* 全局搜索加入选项 - 是否排除容器块

### 开发重构

* 变更发版机制

### 修复缺陷

* 块引新建时选择到的行级代码重复
* 不能修改文档图标
* 设置文档图标导致自动创建空的笔记本
* 子文档不能拖到笔记本下
* PDF 标注复制引用后有时查询不到块
* 部分站点的 iframe 加载问题
* macOS 中 Dock 右键退出无法退出
* 大纲中显示次级标题的数量不全
* 网络代理设置文案错误
* 解析文本 `<<` 时错误识别为文件标注语法
* 设置搜索失效
* 导出模板时任务列表问题

## v1.4.0 / 2021-10-07

### 引入特性

* 工作空间列表切换
* 笔记本排序
* 笔记本图标

### 改进功能

* 当光标没有定位到块时，文件无法拖到笔记中
* 改进输入法兼容性
* 资源文件搜索结果显示不完整
* 点击文档后，大纲同步滚动
* 虚拟引用不再排除自身定义块
* 资源文件搜索去重
* 标签筛选组合需按住 Ctrl
* 文档中的 PDF 标注无法通过 Backspace 删除
* 多个软换行后应形成新的块
* 剪藏扩展支持笔记本 assets
* 嵌入块面包屑滑动会弹出左右菜单
* 开启同步以后，如果同步失败可选择强制退出

### 修复缺陷

* 在代码块输入 &copy; 自动转变字符
* 调整块宽度影响图片大小
* 开启最小化到托盘后 Quit 无效
* 部分站点的 iframe 加载问题
* 文档名包含 `"` 和 `>` 等符号的转义问题
* 网络角标显示问题
* 任务列表下的引用导致反链显示问题
* 某些移动端 Pad 设备上无法进入
* 查询嵌入块导出问题

## v1.3.9 / 2021-10-03

### 引入特性

* 插入已有资源文件

### 改进功能

* 文档标题互转后，编辑区会跳转到开头
* Windows 下图表弹框拖拽卡顿
* PDF 搜索或更多，点击空白地方不会消失
* 不同步符号链接
* 大纲点击后文档标题定位到顶部
* 改进 PDF 标注渲染效果
* 英文句尾换行问题
* 列表项不应该允许合并超级块

### 修复缺陷

* 在 data 下的子文件同步时没有删除
* 代码块换行，复制按钮消失
* 反链面板中，某一项突然颜色变淡
* 退出应用问题
* 大纲不显示转义内容
* 列表项横排后撤销内核中断
* 有的 PDF 无法划选
* 端到端密码报错
* 虚拟引用通过命名搜索遗漏问题

## v1.3.8 / 2021-09-30

### 引入特性

* 支持 SOCKS5 网络代理
* PDF 标注支持背景和边框切换

### 改进功能

* 嵌入块未保留折叠显示
* PDF 需精准定位到注解
* 剪藏扩展点击发送到思源后关闭面板
* 插入资源文件时不替换标点符号和空格
* 云端同步的 4.1kB 应该显示为 0
* 变更同步忽略文件夹语法
* 剪藏扩展保留代码高亮语言
* 为待办列表添加 ctrl/shift/alt +click 事件

### 修复缺陷

* 同步忽略列表删除源文件问题
* 查询嵌入块渲染导致的内核崩溃
* 列表下标题过长导致样式错位
* Code is inline by mistake when copy-paste
* 需要登录的 iframe 无法登录
* 软换行后，数据没有保存
* 连续粘贴 PDF 标注不生效

## v1.3.7 / 2021-09-28

### 引入特性

* 同步排除文件列表

### 改进功能

* Alt+F4/Command+Q 和关闭按钮保持逻辑一致
* 编辑时 ESC 取消光标并选中当前块
* 锁屏界面多语言支持
* 浏览器剪藏扩展剪藏同名网页改进

### 开发重构

* 升级 Electron
* 重构系统剪切板读取文件路径

### 修复缺陷

* macOS 关闭后无法保存布局状态
* 代码块无法对齐
* 悬浮窗点击更多，鼠标移动到菜单上时，悬浮窗消失
* 打不开使用 `file://` 链接的 PDF
* 列表项拖动内核中断
* 历史无法滚动

## v1.3.6 / 2021-09-27

### 引入特性

* 一键清空所有历史

### 改进功能

* 直接粘贴公式后无法撤销
* 反链面板层级、上下文和移动改进
* 嵌入块右键显示菜单
* 文档导出模板保留空行和任务列表
* 编辑器字体选择支持 TTC 和 OTF
* 嵌入块交互改进
* PDF 划选背景改为边框
* 折叠标题改动层级时需要先展开
* PDF 画框复制注解锚文本改进
* 被其他程序锁定的文件加入关闭按钮
* 汇编语法高亮
* 代码签名 rsync 相关 exe
* 数据库表 spans 不再存储纯文本元素
* 整理表格、标题、代码块、引述第一个字符前，前向删除逻辑
* PDF 标注文本需要在大纲显示
* 隐藏 PDF 文件上使用其他编辑器做的标注

### 开发重构

* 重构历史恢复机制

### 修复缺陷

* 加粗邻接时标记符和属性暴露
* 文档导出模板包含自定义 emoji 问题
* 应用主题以后模式选择保留
* 软换行后输入 >> 内核中断
* 转义符导致的行级元素样式属性暴露
* 跨文档移动列表项时父 ID 指向不对
* 动态加载最后一个块可能出现的边界计算问题

## v1.3.5 / 2021-09-23

### 引入特性

* HTTP API
* 导出 PDF 书签大纲
* PDF 标注双链
* 预览 PDF 支持书签大纲跳转、搜索、缩放等常用功能
* Linux 和 macOS 上支持选择字体
* 集市显示仓库星标数
* 浏览器剪藏扩展支持一键发送

### 改进功能

* 导出增加文档标题选项
* 支持 VHDL、Scala 语法高亮
* 列表中的标题没有对齐
* 移除超级块内部边距
* 支持标签层级范围搜索
* 字体列表中仅支持常规字形的字体
* 自动同步支持设置时间间隔
* 超链接提示干扰
* 优化同步获取云端信息
* get upload token failed: 401
* 上下移动导致大纲面板闪烁
* 点击停靠栏两次后面板才会收起
* 行内数学公式被遮挡
* 属性面板、代码编辑框等使用 Ctrl Enter 确认并关闭
* 引用过多时悬浮窗很难 resize
* 嵌入块中的超链接支持点击打开
* 嵌入块折叠后选项按钮消失
* 为二级菜单添加阴影效果
* 修改暗黑主题下错误、警告、信息、成功的背景色
* 打开用户指南时提示请勿写入重要数据

### 开发重构

* 重新打包 Windows 版 Rsync

### 修复缺陷

* Android 11 以下无法正常进入
* 列表下嵌入块图标位置显示错误
* 搜索结果上下键切换问题
* 某些字体设置失效
* 标签带空格时搜索失效问题
* 引用跳转高亮消失
* 下划线合并问题
* 窗口位置超出屏幕之外
* 空白处粘贴超级块显示不出来
* Markdown link parse failed
* 未鉴权的情况下可以访问 assets 下的图片
* 文档末尾表格内右键无效
* 打开刚关闭的文档不显示大纲
* 虚拟引用别名判断重复包含问题
* 剪藏扩展处理 SVG 公式时路径不正确
* 数学公式内容中包含 `\$` 时解析报错
* 选中整个块粘贴再撤销会导致选中的内容丢失
* 快捷键无法设置为 alt + 左右
* 无网络情况无法通过设置 - 云端关闭同步
* 文档块属性换行符转义
* 表格内的数学公式复制粘贴无效
* 五线谱导出 PDF 不全
* 内核只读模式下仍然可以移动块
* 视频框拖拽粘滞
* XSS 漏洞修复
* 折叠标题转换为文档时需要自动展开下方块
* 列表中代码块回车后会回到顶部

## v1.3.4 / 2021-09-06

### 引入特性

* 导出时将块引用转换为脚注

### 改进功能

* 同步鉴权失败后自动重试
* 文档导出为模板支持属性
* 编辑器文档块标菜单添加删除文档
* 添加超链接时如果不填链接地址则取消超链接
* 标签面板不再显示块，点击标签以后通过搜索页签展现
* 编辑标题时大纲闪烁
* (( 新建文档 32 个字符限制改为 512个
* 使用非中文和英文外观时统一使用英文帮助文档

### 修复缺陷

* 列表项内用ctrl+↑折叠，块标变成段落块
* 移动端右侧滑动卡
* 搜索预览区动态加载时丢失关键字高亮
* 移动端搜索跳转定位不对
* 移动端粘贴菜单无效
* 导出嵌入超级块重复

## v1.3.3 / 2021-09-04

### 引入特性

* 文档另存为模板
* 嵌入块导出选项
* 为各面板添加全部折叠/展开的快捷键

### 改进功能

* 文档动态加载优化
* 切换外观模式时卡顿
* 鼠标移动到命名，别名，备注上，对应的块需进行高亮
* 代码块编辑改进

### 开发重构

* 重构运行环境容器判断

### 修复缺陷

* 代码块 // 注释后粘贴, 光标错误
* 新建的笔记本查看历史报错
* 无法复制自定义 emoji
* 同一个块内有2个引用时产生的问题
* 导出嵌入块时未处理嵌套情况
* 锚文本不支持单个字符的问题
* 移动端浏览器使用时链接不能跳转的问题
* 反链提及排序不稳定
* 反链面板上下文按钮状态问题
* 悬浮窗改变宽度后滚动条消失

## v1.3.2 / 2021-09-01

### 引入特性

* 支持跨笔记本移动块

### 改进功能

* 列表项多选禁止转换
* 支持关闭网络图片角标
* 导出时嵌入块使用原始文本
* 点击左边空白，光标置于段首
* 同步/备份限制单个文件最大为 1G
* 嵌入多行块，第二行会显示一半
* 改进复制网页内容时的换行处理
* 文档的标签不能用顶级标签
* 改进锚文本修改后光标的位置
* 列表项下的标题和圆点对不齐
* 移除空链接
* 嵌入块内的引用无法点击打开

### 修复缺陷

* 某些空行删除不了
* Android 端无法打开 PDF 资源文件
* 云端报错 failed to connect cloud server
* `\` 撤销问题
* 添加颜色后英文单词之间无法添加空格
* 浮窗中的列表项无法拖拽到文档中
* 移动带子文档的文档后子文档打不开
* 休眠后内核连接中断
* 悬浮窗内容显示不全
* 未使用过字体染色后，alt+x 报错
* 从 Word 粘贴时空列表项处理

## v1.3.1 / 2021-08-29

### 引入特性

* 支持浮窗移动块

### 改进功能

* 添加  Vim Script 语言代码高亮，扩展 TOML、C#
* 标签跳转折叠块
* 自定义 emoji 支持子文件夹
* 改进主界面加载性能
* 折叠嵌入块 UI 改进
* 出现内核中断问题以后下次启动重建索引
* 文档树 icon 大小不一致

### 开发重构

* 块 hash 字段减少长度

### 修复缺陷

* 弹框编辑删除内核中断
* 全局搜索鼠标单击跳转问题
* 移动端登录账号问题
* 容器块 updated 字段未更新
* 段首不能软换行
* 第一个块的块首按左键或最后一个块块末按右键，光标丢失
* 有序列表下方多选块删除内核中断
* 从历史恢复时报错，查看历史入口统一到笔记本右键菜单
* 搜索类型过滤失效
* 账号鉴权自动续期问题
* 撤销导致的内核中断
* 反链面板中点击悬浮窗中的面包屑，反链面板不应更新

## v1.3.0 / 2021-08-28

### 引入特性

* 工作空间端到端加密数据同步

### 改进功能

* Code signing on macOS
* Zoom-in while searching folded blocks
* 全局搜索排除容器块
* 自定义前进/后退/聚焦进入/聚焦返回快捷键
* 各功能面板在选中时需显示展开的箭头
* ``` 代码块输入改进
* 桌面端启动检查端口改进
* 调整链接打开规则
* 为“移动”功能添加可配置的快捷键
* 内核启动时自动优化数据库文件
* 优化集市加载速度
* 有序列表批量删除需更新序号
* 自动检查版本更新
* 文档树展开箭头和文档图标视觉优化

### 移除功能

* 移除自动更新相关遗留代码

### 修复缺陷

* 空表格里不能 Backspace 空行
* 文档树添加 Ctrl F 快捷键
* 页面按向下箭头丢失焦点
* 句首和句尾的英文不显示虚拟引用
* 反链面板布局状态保留问题
* 代码块光标乱跳
* macOS 上删除所有 emoji 文件之后仍存在一个破损文件
* Docker 部署帮助文档自动打开问题

## v1.2.9 / 2021-08-26

### 引入特性

* 支持扩展 Emoji

### 改进功能

* 调整图片外链标识位置
* 块引搜索候选列表排序优化
* 代码高亮渲染改进

### 移除功能

* 移除涉及侵权的 Emoji

### 修复缺陷

* 【【块引呼出问题
* 句末不能使用软换行
* 代码块回车抖动
* 文档图标搜索删除和文档标题重命名删除会触发删除文档
* 点击块引进去的文档不显示反链信息
* 悬浮窗列表项无法展开
* 列表内斜体后的最后一个字符无法选中

## v1.2.8 / 2021-08-24

### 引入特性

* 网络图片提示角标

### 改进功能

* 文档树 Alt 1 展开后获得焦点、上下键选择以及 Enter 打开
* 启动进入主界面加载优化
* 引用块时粘贴的文本无法显示新建
* PDF 链接 ctrl+click 使用本地默认工具打开
* 文档树支持 Del 键删除笔记
* 虚拟引用对英语使用空格分词
* 视频音频等文件插入去重
* 嵌入块为代码块的时候，右上角的功能会被覆盖

### 移除功能

* 移除 v1.2.5 数据迁移支持

### 修复缺陷

* 开启虚拟引用导致的内容乱码问题
* 文档标签搜索不出
* 文档标签区分大小写
* 虚拟引用搜索多个别名时的问题
* 文档题头工具栏遮挡问题
* 文档标签下拉菜单没有文档级标签

## v1.2.7 / 2021-08-23

### 引入特性

* 文档块支持标签

### 改进功能

* 拖拽文件夹到编辑器内，错误信息为空
* 虚拟引用涵盖命名、别名、定义块文本
* 启动思源后，点击页签，光标不会聚焦到编辑器上
* 头图上传插入去重
* 虚拟引用支持关键字排除列表
* 笔记本名称、文档标题允许使用特殊符号
* 移动端使用系统自带 emoji
* 支持直接使用 ID 搜索块

### 修复缺陷

* 后退后再操作，后退缺少一步
* 文档树点击已打开的文档、切换页签没有记录到后退中
* 设置属性以后没有同步到数据库
* 重名文档导出只导出一个文档问题
* 表格单元格内颜色文字后无法继续输入
* 打开文档不会更新大纲
* 无法消除的高亮
* 标题转换为段落时，大纲没有更新
* 列表大纲定位错误

## v1.2.6 / 2021-08-21

### 引入特性

* 虚拟引用 Virtual ref

### 改进功能

* 导入时 assets 的放置改进
* F5 刷新增加标题后的引用数更新
* 移动端支持切换主题
* 改进已关闭笔记本列表

### 修复缺陷

* 有关闭的笔记本时文档树排序报查询笔记本失败
* 自定义排序失效问题
* 剪切单个块粘贴后id会变，且再次粘贴错误
* 文档表情标签残留
* 重命名具有共同前缀的文档标题时的问题
* 查询嵌入块的递归问题
* 调整设置后不能使用 emoji
* 导入以后删除文档报错
* 大纲点击跳转不稳定
* 块引浮窗面包屑根路径缺失父路径
* 折叠下方为空的标题时内核中断

## v1.2.5 / 2021-08-20

### 引入特性

* Back and forward for editor
* 从文件夹结构迁移到子文档结构
* 支持鼠标中键关闭页签，侧键前进后退
* 文档图标
* 自动保存常用 Emoji

### 改进功能

* Add Scribli emoji
* 文档数据文件名使用 ID
* 全局搜索预览编辑区支持行级文本内容高亮
* End-to-End testing via Cypress
* 同步报错文案优化
* 从临时文件恢复
* 延长文档锁定时间为 5 分钟
* 不允许同时选中文本和块
* 稳定 shift+click 算法，选中后定位光标
* 剪藏扩展发布到 Edge 浏览器商店
* macOS 和 Linux 上使用系统自带的 rsync 兜底
* 通过模版创建日记时支持 id 和 title 变量
* 没有配置授权码时锁屏需要提示
* 设置 - 云端相关加载改进
* 修改默认快捷不再需要进行重置
* 重写移动端文档树
* 同步时连续两次从云端下载则退出应用
* 代码块下的空段落块无法删除
* 创建文档流程改进
* 多选块以后右键需打开多选块菜单
* 全局搜索快捷调整大小写敏感
* 笔记本关闭后依旧保留在文档树上
* 文档树上的引用计数和文档引用计数区分
* 引述块折叠会显示一行半的文字
* 改进剪藏扩展拉取图片稳定性

### 开发重构

* WebSocket 重构为 HTTP API
* 重新实现文档树自定义排序

### 移除功能

* 移除老版本到 v1.2.0 的迁移支持

### 修复缺陷

* 重命名和使用别名后反向链接显示错误
* 引用超级块时没有渲染文本
* 重命名笔记本和笔记的问题
* 点击行内元素光标乱跳
* mac 拼音输入法在大纲中的 bug
* 创建标题后对应子内容父亲字段未更新
* 嵌入超级块，图片拖拽导致界面异常
* 事务失败导致的内核连接中断
* 移动端滑动表格会出现菜单
* 表格选择需移除空元素
* 修改链接光标和标题转义问题
* 任务列表空项引起的解析报错
* SQL `"` 转义处理问题
* 链接提示不会消失改进
* 改进搜索历史
* 重启思源导致列表背景色丢失
* 面包屑自动订正数据引起的内核中断问题
* 反向缩进内容丢失
* 代码块中的 kramdown 代码会被解析渲染
* 引用前后加 `**` 重启后引用消失
* 折叠标题更新和移动导致内容错乱的问题
* 表格内使用微软拼音的问题
* 从 Word 中复制多段落时自动添加空格问题
* 数学公式染色 bug

## v1.2.31 / 2021-08-01

### 引入特性

* 笔记本重命名
* API token
* 笔记本删除

### 改进功能

* 剪藏扩展加入 API token 配置
* 优化数据保存性能
* 鼠标单击行尾空白处时，光标有时会定位在行首而非行末
* Docker 容器监听 `0.0.0.0`
* 面包屑导航列表使用第一个子块文本
* 不允许创建以 `.` 开头的文档
* 编辑时自动移除空标签
* 改进折叠块的块标显示

### 开发重构

* SQL 表 blocks 移除两列 `previous_id` 和 `next_id`
* 笔记本路径迁移

### 修复缺陷

* 有序列表块选择移动问题
* 多行块属性导致内核崩溃
* 未引用资源在自建子文件夹的情况下计算错误
* 着色后，不在同一个块中继续输入，光标会乱跳
* 导出 Word 时中文段首缩进失效
* 链接无法保存
* 折叠标题修改数据丢失
* 数学公式前输入公式排版格式消失

## v1.2.3 / 2021-07-28

### 引入特性

* 文件排序加入“按创建时间排序”

### 改进功能

* 更新文档名后，引用不会实时更新
* 数据写入遇到“关键错误”时强制退出内核进程
* 移除本地同步目录、本地备份目录设置
* 删除大纲标题后面包屑不会立即刷新
* 版本升级时强制结束老内核进程
* 更换授权码字段为 `accessAuthCode`
* 浏览器中点击附件改进
* 安卓端长公式的手势改进
* 支持鉴权页面拖拽

### 修复缺陷

* 标签异常高亮
* 工作空间所在磁盘分区不存在时内核启动失败
* 表格 ctrl+z 撤销的问题
* 修改文件树或编辑器内的文件名，大纲顶部的文件名不会刷新
* Markdown 语法 `<url>` 问题
* 手机端面包屑过长时的问题
* 更改鉴权密码后不能关闭软件
* 吞超链接
* 移动端选择后没有复制选项
* 转义符 `\[` 引起的关键错误

## v1.2.2 / 2021-07-27

### 升级须知

* 升级前请在老版本上将 <kbd>设置 - 关于 - 访问授权码</kbd> 设置为空或者设置为一个自己常用的授权码（设置为空时新版打开不需要鉴权，不为空时需要输入授权码解锁）
* 数据同步功能已经开始公测，使用前请查看帮助文档，备份数据后再开启同步

### 引入特性

* 重制鉴权
* 文档树新增按文档被引用数排序
* 对接 Web Clipper
* 设置 - 搜索支持大小写敏感选项
* 光标所在块右键不弹块菜单，弹出复制/全选等菜单
* 选中块元素后， 可使用上下键选择附近的块
* 面包屑缩放中高亮当前块
* Android 端支持后退
* 自动清理历史

### 改进功能

* 反链提及搜索排除链接文本
* 优化备份/恢复性能
* shift + 方向键和全选快捷键的操作建议
* 优化未引用资源文件加载性能
* 导出块引时支持仅锚文本模式
* 帮助文档仅在初次安装时打开
* 正常退出内核时等待数据全部写入后再退出
* 使用设置 - 搜索 - 搜索结果显示数来限制反链提及搜索结果条目
* 导入 Markdown 文档前进行重名检查
* alt+x 需全局缓存记忆
* 所有本地功能完全免费
* 大纲中的全部展开改为保持全部展开
* 面包屑导航文本优先使用命名
*  `shift+左/右` 选中行内公式后 `ctr+m` 可进行编辑
* 删除笔记本配置 .Scribli/conf.json 中的更新时间字段
* 复制/剪切空格时会应用到块上
* 不使用系统临时目录
* / 菜单交互改进
* 列表前的原点操作和块图标保持一致

### 开发重构

* 重构块移动和删除
* 重构数据提交事务实现
* 重构容器块类型转换实现

### 修复缺陷

* 别名、命名等属性未转义
* 文档内粘贴并换行后文档自动关闭
* 移动超级块内的折叠标题会丢失数据
* 删除空行导致文本块和代码块合并
* 导入 Markdown 时存在行级 HTML 解析报错
* 将软换行段落使用标题打断为多个块后乱序的问题
* 标签自动完成候选列表不全
* 推荐码使用过多，无法看到剩余天数
* 不期待的任务列表输入方式
* 块中有行内公式，输入 Emoji 会消失
* 粗体、颜色选中后再输入 bug
* 内容块弹窗无法折叠和展开
* 相对路径 assets 上传不了云端图床
* 代码块换行没有进行保存
* 日文片假名长音 `ー` 等 Unicode 字母修饰符自动空格问题
* 任务列表前有的有 id，有的没有
* 给行内公式加下划线样式错误
* /api/lute/html2BlockDOM 接口转换错误
* 模板自定义属性问题
* 完善引用提示补全

## v1.2.1 / 2021-07-16

### Features

* 内容块聚焦关系图
* 关系图全屏
* 为图片提供更多设置

### Enhancements

* 优化未引用资源文件加载性能
* 复制块级元素到表格中时转换为行级元素
* 设置备份目录路径改进
* 文档标题保存在属性中
* 导入 Markdown 文档时同时导入引用的资源文件

### Bug fixes

* 打开帮助文档后引起的未找到内容块
* 资源文件打开报错
* 未引用资源文件统计错误
* 导入 Markdown 文件夹时 `\` 路径未处理
* 文档标题选中后粘贴应删除选中的内容

## v1.2.0 / 2021-07-15

### Bug fixes

* `#标签` 自动完成性能不好
* 行级公式内容为空时应该自动移除标记符
* 当前光标所在位置块字数统计错误

## v1.2.0-rc3 / 2021-07-14

### Bug fixes

* 横排标题折叠后使用 Ctrl ↓ 展开失效
* 打开/关闭笔记本和刷新文档树并发问题
* 云端备份 rsync 路径错误
* Android 端文档树返回上一层失效
* Windows 安装程序问题
* 复制图片后不应该转换 PNG 格式
* 插入资源文件时避免重复

## v1.2.0-rc2 / 2021-07-13

### Documentation

* 安装/升级后自动打开帮助文档

### Bug fixes

* 代码高亮渲染不完整
* 横排标题折叠后使用 Ctrl ↓ 展开失效
* Alt M 未恢复窗体状态
* 去掉 Ctrl Click 聚焦
* 重命名文档时 `\n`、`\t` 等没有处理
* Windows 上 rsync 存在性修复
* 不允许将工作空间、备份目录设置到带空格的路径下
* Android 端左滑、右滑、下滑触发失效问题
* 浏览器剪藏时没有将资源文件保存在手动建立的 assets 中
* PDF 页签图标错误
* 行级公式 `&amp;` 数据订正

## v1.2.0-rc1 / 2021-07-11

### Bug fixes

* 显示/隐藏窗口快捷键唤出时窗口不在最前
* 导出设置块引为锚文本模式时报错
* 托盘退出时关闭内核
* 重命名工作空间文件夹后重启会新建一个原有的工作空间文件夹
* Docker 导出 Markdown 时下载地址不对
* 行级元素 Shift 跳出不连续问题
* 行级元素软换行后删除不了
* 页签/文档树等 UI 元素过窄时界面会失去焦点，交互操作无响应
* 空列表项粘贴嵌入块再撤销，就没法回车继续回车了
* 列表内有嵌入块，列表下方按删除键无反应
* 折叠组合移动 bug
* 导出时子列表内标记符重复
* 新建文档名对话框输入自适应宽度
* 空公式解析问题
* 中西文自动空格对上下标排版后失效
* foo /math 后重新打开还存在
* 禁止行级公式派生块级公式
* 图片下间距太长，不美观
* 大纲、面包屑内容转义问题
* 软换行后数学公式不会弹出公式输入框

## v1.2.0-beta16 / 2021-07-09

### Enhancements

* 列表项带子块聚焦
* Windows 版允许设置安装位置
* 阻止长按 enter 回车
* 设置备份目录路径检查
* 图片预览由单击改为双击
* 大纲默认全部展开
* 优化属性设置的性能

### Bug fixes

* 冷启动过慢的问题
* 回滚文档大小为 0K 的问题
* 图片上下空行问题
* `<iframe ` 解析问题

## v1.2.0-beta15 / 2021-07-08

### Features

* 双击未修改过的页签后不再进行替换
* Shift+↑/↓ 延续选中上/下兄弟块
* 支持段落块段首空格或 Tab 缩进

### Enhancements

* 有序列表拖拽第一项后应保持顺序不变
* 面包屑文档根节点导航点击改进
* 文字选中优化
* 空列表数据订正
* 缩放快捷键 `Alt+→/←`
* 缩放 `Ctrl+Click`/`Ctrl+RightClick` 支持在非块标上操作
* 图片右键应弹出图片菜单而非段落块菜单
* 块引用时排除当前块

### Bug fixes

* 浏览历史为空的问题
* 帮助文档重复挂载问题
* 删除标题块以后编辑报错查询内容块失败
* 列表项拖拽合并到另一个列表第一项时的问题
* Docker 镜像缺失 Pandoc 
* 粘贴行级代码时 HTML 转义问题
* 有序列表缩进到无序列表中的 bug

## v1.2.0-beta14 / 2021-07-06

### Enhancements

* 从右往左选中优化
* 新版启动时自动结束老版内核
* 全局缓存代码语言
* 代码块自定义默认配置需和全局保持一致

### Bug fixes

* List block data loss
* 图片末尾回车 bug
* 删除文件夹时报文件被占用锁定问题

## v1.2.0-beta13 / 2021-07-05

### Enhancements

* 增加行宽度75
* 被其他软件占用文件时需要弹框锁定
* 块引浮窗取消动态加载，除非点击过上下文加载按钮才启用动态加载
* 改进重命名机制，避免使用同步盘时潜在的数据不一致
* 体验改进
* 浏览器中支持 Ctrl+K
* 文档树上文件夹和文档的视觉区分

### Bug fixes

* 导出 Markdown 时没有导出笔记本级 assets
* 历史记录写入报错
* 移动端切换文档时面包屑不会刷新
* 引术块属性 bug
* 点击两行文字的中间光标消失
* 微软输入法中文状态下输入数字的问题
* 编辑标题块以后内容块查询失败的问题

## v1.2.0-beta12 / 2021-07-03

### Features

* 支持插入音频、视频和图片后设置链接
* 为块元素添加字体、背景、镂空、阴影、发光效果

### Enhancements

* 块引浮窗上下文加载按钮
* 折叠标题性能优化
* 导出时块引锚文本加上 scribli:// 块链
* 拖拽时隐藏块前图标提示
* 固定面包屑导航
* 拖拽块前图标无法向上滚动
* 导出的多级列表样式和编辑器中不一致
* 优化全局搜索结果排序
* 支持行内代码连字
* 列表样式不随着字体大小改变而变化
* 兼容主题菜单样式错误
* 支持视频、音频、iframe 的居中、居左、居右配置
* 嵌入块 `{{` 后不弹出 SQL 代码框，而是类似 `((` 后自动搜索关键字

### Bug fixes

* 子块折叠后位置错位
* 列表删除后仍然可被引用
* 输入中文双引号时，会出现文本显示错位，之后输入文本，光标会丢失
* 公式导出时大于号、小于号等错误地转换为了 HTML 实体
* 文件树拖拽间隔过短会失败
* 段落块意外的块属性修正
* 文档转换标题时层级计算溢出

## v1.2.0-beta11 / 2021-07-01

### Features

* 列表项多选缩进及反向缩进
* 重写文档数据写入机制，锁定文件提升稳定性

### Enhancements

* 用 `Ctrl+/` 调出块标菜单后可支持方向键和回车键进行操作
* 点击文档，高亮大纲中对应的标题
* 表格细节改进
* 点击块右上角的属性后将光标定位到该属性的输入框中
* 剪切链接后粘贴问题
* 输入法中数字的句号无法转换为点
* 增加段落块转换类型支持
* 引用过多时，优化悬浮窗性能
* 搜索结果中存在命名别名的块排序优先
* 嵌入块搜索忽略类型过滤
* 搜索结果高亮
* 文档手动刷新按钮
* 优化移动端横向排版效果
* 顿号唤出菜单仅限于斜杆，反斜杠按钮打出的顿号不再唤出菜单
* 改进空块提示
* 调整编辑器大小时，块符号不跟随
* 切换/编辑文档时保持大纲折叠状态
* 从备份恢复后需要重新启动
* Android 通过通知栏保活
* 简化块标、聚焦面包屑导航

### Bug fixes

* `<kbd>` 中反斜杠转义问题
* 首行是列表块时光标上移 bug
* 标题折叠展开相关问题
* Do not allow to write data during boot
* Docker 部署时下载云端数据备份异常
* 代码块粘贴文本以换行符结尾时, 最后一行无法删除
* wrap code bug
* 登录账号需要输入验证码时没有显示验证码的问题
* 剪藏微信公众号全选报错问题
* 列表中嵌套超级块后无法点击左侧图标
* 表格内存在行级公式时编辑会产生换行

## v1.2.0-beta10 / 2021-06-26

### Features

* 浏览器剪藏扩展支持设置内核接口地址
* 资源文件相对路径伺服

### Enhancements

* 高亮排版时删除内部多余的空格
* 大纲折叠优化
* 菜单项过多时需进行滚动
* 行首为公式时无法使用 `Home` 键定位到行首
* 文档大纲缩进优化
* PDF、HTML 导出需要付费订阅
* 任务列表块内容和方块标记不对齐
* 文章标题能够自动换行
* 汉字与假名的自动空格问题
* 文档编辑时如果没有权限写入数据需要及时在界面上提醒
* 斜杆菜单位置和大小优化
* 重命名后光标应再回到编辑器中
* 增加图片拖拽区域范围

### Bug fixes

* 代码块在缩小字体字号时，左边有部分内容被隐藏，整体往左偏移了一些像素。
* 搜索面板搜索为空且关闭报错
* 后缀名为空的文件拖动到笔记时无法自动复制
* 含有视频网站链接的文章无法导出 pdf
* SQL 全局搜索不应该转换小写
* 查看云端空间占用为零的问题
* 列表前一个块结尾 Delete 后删除逻辑问题
* 嵌入块中的图片图标遮挡住了嵌入块的操作按钮
* 调整历史生成间隔以后没有即时生效
* 链接后粘贴链接问题
* 粘贴 Excel 变成图片的问题

## v1.2.0-beta9 / 2021-06-23

### 引入特性

* Global search with path filter

### 改进功能

* 中西文自动空格计入 `@` 符号
* 表格内容三击操作有问题
* copy individual image locally
* 持久化保存备份密码，避免每次都需要输入
* 笔记本 backup 目录重命名为 history
* 数学公式改进
* 嵌入块手动刷新
* 行级公式编辑框不允许换行
* 脑图编辑支持 tab
* 表格内粘贴优化

### 修复缺陷

* 整段文字加颜色着重,行内公式上移
* 大小写影响提及转换链接
* 标题折叠取消超级块时内容丢失
* 文档标题栏不应该显示其子块引用计数
* 任务列表转换有序列表序号不正确
* 导出 Markdown 时包含中文名的资源文件没有导出
* 删除折叠标题时恢复下方块为展开状态
* 同一个文档中移动折叠标题导致内容重复
* 块引时候选列表中点击块标插入位置问题
* 双栏操作下光标会出现跳回上次操作的位置
* 多选先“转换为引述”，再“转换为无序列表”内容丢失

## v1.2.0-beta8 / 2021-06-18

### Features

* 标题块带下方块折叠和移动
* 为代码块单独提供换行、连字符和行号配置

### Enhancements

* 切换 tab 不应该失去光标焦点
* 禁止在表格中多选单元格进行格式化排版
* 引用块面板全局滚动条无法使用鼠标进行拖动
* Improve undo
* 属性面板支持回车确定

### Bug fixes

* 命名及备注标识被遮挡
* Cannot remove bookmark for doc
* 正文中出现滑块
* 列表项中包含子列表和段落，回车错误
* 脑图和图表上下动态加载后会消失

## v1.2.0-beta7 / 2021-06-16

### Features

* Docker rsync
* 笔记本 assets 资源文件伺服
* 支持剪藏 SVG

### Enhancements

* 属性设置交互优化
* resize heading with folding quoteblock
* Cursor Position Wrong When Typing Math Formula
* Enhancement of naming and memo functions
* 块引悬浮层加载上下文
* 折叠逻辑修改
* 改进导出 pdf 中的数学公式渲染
* Add refresh button on the bookmark toolbar
* 优化粘贴大量数据时的性能

### Bug fixes

* 设置 - 资源界面卡住问题
* 剪藏某些网页图片路径重复问题
* PPT 渲染无法解析数学公式、代码块等
* Resize image not work
* 带编号的公式显示不正常
* 反斜杠 `\` 转义后再打开文档会重复显示
* 模板空列表项解析问题
* 自定义排序移动文件夹后失效的问题
* 无法撤销第一个操作

## v1.2.0-beta6 / 2021-06-13

### Features

* Support custom block attributes
* 添加居中/左/右快捷键
* 横排分栏宽度调整

### Enhancements

* 命名、别名、备注和书签合并到属性设置中
* 图片标题入库
* Want to unify the folding style for list and list item
* 移动端代码块折行问题
* 从其他软件复制带图内容改进
* 行内公式的新问题
* 结合 shift↑ 为块添加展开/折叠快捷键 ⌘↑
* 图片居中以后，同一段落输入文本，图片的居中会被取消
* 不锁定日志文件
* 缩短备份文件名，避免一些系统上路径过长报错

### Bug fixes

* 任务列表转换段落问题
* Android 端横屏重启问题
* 行级元素拖拽移动问题
* 备注预览显示默认 `memo` 文案问题
* 重启后访问授权码被随机重置问题
* 未引用资源预览样式不正确、最后一个资源无法删除
* 剪藏时如果包含长图片名路径时会报错
* 备份恢复报错 `open xxx/decrypt: is a directory`
* 剪切、粘贴引用块后，被剪切的块会留在原地

## v1.2.0-beta5 / 2021-06-10

### Features

* 引入挂件块伺服

### Enhancements

* Support select, copy, cut and enter to edit 
* 文字选中进行操作处理时,能否自动跳过公式?
* 自定义加密备份存放路径
* 备注悬浮预览
* 上下键会跳过空段落
* 字体加粗后删除依旧为粗体
* 数据库结构变更后强制重建库
* 开源端到端加密代码
* 为代码块添加 144 种主题及对 verilog 的支持

### Bug fixes

* Blockquote data loss
* 不同笔记本下创建了同名笔记的问题
* 行内公式在一行之首时无法缩到上一行
* 块引单引号影响搜索
* 标题转换文档数据丢失
* 一些网站无法使用 IFrame 嵌入的问题

## v1.2.0-beta4 / 2021-06-07

### Features

* Mermaid click callback 

### Enhancements

* 发布新的 Docker 镜像，请注意备份数据和修改启动参数

### Bug fixes

* `kbd` 和下划线的解析问题 
* 资源文件 Assets 相关的一些列问题
* 添加的标签没有显示在标签栏
* 插入模板后会出现在页面的上方

## v1.2.0-beta3 / 2021-06-07

Bug fixes and improves details.

## v1.2.0-beta2 / 2021-06-05

Bug fixes and improves details.

## v1.2.0-beta1 / 2021-06-03

### Features

* Outline expand collapse all
* Horizontal layout of content blocks
* Block zoom-in
* Support underline
* Support cross notebook block ref
* Document dynamic loading
* Block drag
* Use Protyle instead of Vditor for the editor
* List outline
* WYSIWYG instead of IR
* Local workspace dir
* New assets serve
* Chrome extension for content copy
* Support upload assets of a single doc to cloud
* Auto copy local attachments to assets when pasting from Word
* Batch export standard Markdown with assets
* Support export to `.docx`
* End-to-end encryption backup

### Enhancements

* Editing freezes when rendering a large number of mathematical formulas
* Support the preview and export for query embed block
* Improve function in image scale mode
* Editor redo/undo
* Rename editor options
* Change the doc data file format to `.sy`
* Optimize the writing performance of large document data
* Change embed query block syntax from `!{{script}}` to `{{script}}`
* Template use `.md`, save under workspace data dir `templates`
* Conf dir move to $workspace/conf/
* Boot parameter `--workspace` instead of `--conf` and `--data`
* Table `blocks` add columns
* Improve performance of boot indexing

### Docs

* Weaken Markdown related content in the user guide

### Abolishment

* Remove editor options
* Remove export option `fixTermTypo`
* Remove YAML Front Matter support
* Remove HTML Block and Inline HTML rendering
* Remove block ref anchor text template `{{.text}}`
* Remove block ref anchor text inline parsing
* Remove block embed `!((id))`
* Remove Markdown footnotes support
* Remove Markdown link ref support
* Remove find in page and find replace
* Remove filetree options
* Remove WebDAV support
* Remove template call syntax `{{`, use `/` as the entry
* Remove expert mode
* Remove sync option in notebook conf and global conf
* Remove `[toc]`
* Remove `scribli://notebooks/{notebook_name}/blocks/{id}`
* Remove Mindmap
* Remove auto fetch remote image to local
* Remove search text mode
* Remove indent code block
* Remove TextBundle export
* Remove MathJax engine
* Remove cloud online workspace and publishing

### Bug fixes

* Change account then sync: auth failed
* Edit heading after code block issue
* Emoji issue when using Microsoft PinYin
* Doc tree custom sorting bug

## v1.1.83 / 2021-04-09

### Enhancements

* Improve export block ref mode option
* Template folder should NOT be included while rendering global graph

### Bug fixes

* The image is not displayed when other machines in the local area network access
* Boot failed on macOS M1

## v1.1.82 / 2021-04-06

### Enhancements

* Android edit even when there is no internet
* Persist auth code on Android
* Built-in rsync for macOS Apple Silicon
* Optimize the network, reduce the delay of login and synchronization
* Copy content should remove heading marker
* Do not check requests from `127.0.0.1`
* Export PDF remove iframe
* Update the right-click menu of the file tree and tabs

### Bug fixes

* Can't show the image when export PDF with network images
* Content loss caused by iframe not closing
* `# 1. foo` ref as anchor text template will be rendered to `foo`
* Enter the sublist under the task list
* List in the blockquote can not move to up

## v1.1.81 / 2021-04-04

### Enhancements

* Add copy button for block code
* Improve sync stability
* When there is only one block in the list item, insert an empty block to become a list item
* Add remove block for gutter icon

### Bug fixes

* When pasting code in code block it creates new lines

## v1.1.8 / 2021-04-03

### Features

* Use file copy instead of Git for versioning

### Enhancements

* Gutter icon add cut function
* Add some shortcut keys
* Improve conf file read/write
* The spacing between pictures with description and pictures without description should be the same
* Built-int rsync on macOS 10
* Add local protocol recognition

### Bug fixes

* Uploading assets takes a lot of time
* Boot param `--authCode` not work
* Delete at the beginning of the block below will delete the end of the block above
* Sometimes failed to parse standard Markdown when importing
* Fix the display of markers in popover and embed blocks

## v1.1.7 / 2021-04-02

### Features

* Open Scribli via protocol `scribli://`
* Insert mp4/mov/webm convert to `<video>`
* Support fold/unfold block on Android
* When exporting html, the image can use the CDN address

### Enhancements

* Sometimes the arrow keys cannot control the cursor
* Add a switch option for cloud assets storage
* There is an empty paragraph before the heading, delete before the heading should keep the title and its id
* Convert `<br>` to `\n` when copy as standard Markdown
* Pasting two paragraphs into the table should not be merged
* Built-in rsync on macOS
* Display boot progress on Android
* No longer provide Windows zip decompression version
* No longer support open multiple instances
* After clicking, hide the prompt panel
* It is not allowed to close the tab during upload

### Bug fixes

* Copy the order list and paste it will become a list
* The result of the query with level-3 tags is empty
* Failed to parse when copy code block
* Data is not overwritten according to the update time when multiple devices are synchronized
* Block ref inline code issue
* WebSocket connection auth
* Changing the task to the list should remove the class vditor-task-complete

## v1.1.6 / 2021-03-29

### Features

* Support config Tab width in the code block
* Edit code block in place
* Support backup for deleting files
* Support auto sync mode

### Enhancements

* Code block line wrap configuration
* Improve Ctrl+Shift+B in the list item
* Improve code block search
* Optimize the drag and drop performance of block elements in the editor
* Code block or math block ref anchor text template display all content
* Add copy code function for Export HTML
* Count the contents of code blocks and formula blocks into the character count
* When copying a block reference, the anchor text is the name
* Supports sync of notebooks that contain spaces in the path
* In the editor, you can set whether to render mathematical formulas
* Support mathematica, lisp, clojure, Fortran language in code block
* Add copy block id
* Outline search ignores case
* Add a link to automatically recognize the file protocol
* The attachment in the embed block cannot be opened by clicking
* Improve init language detect on Android

### Bug fixes

* Do not change `[[wikilink]]` text if not found ref
* Blockquote in the list, enter will change to list item
* The database will be cleared when the UI is launched for the second time while the kernel is resident
* Import templates should not be wrapped in paragraphs
* Failed to parse name, alias or memo (IAL properties) including `}`
* Invalid permission when sync download
* Can't convert backmention to backlink for doc name ref

## v1.1.5 / 2021-03-23

### Features

* Delete bookmarks directly in the bookmarks panel
* Add copy function to assets files

### Enhancements

* Failed to fetch image when copy-pasting HTML
* Support sync path containing non-ASCII characters on Windows
* Graph's title and label display HTML entity
* Support using CSS to set Graph font family
* Variables missing in the theme are replaced by official theme variables
* Support for querying the set fonts
* Local graph show related tag only
* Table `blocks` add field `length`
* Improve HTML code block parse
* Pin pdf toolbar
* Keep the editable state of the embed block consistent with the editor
* Add hotkey to open new tab for ref and embed block
* `Ctrl+Shift+X` in the block ref, only remove ref
* Improve file tree listing performance
* After pressing enter before the heading, the previous element needs to become a paragraph

### Bug fixes

* A newline will be added after the heading of the super block
* Gutter icon can not show updated time
* Lost content when indenting list items with Tab
* Custom theme styles are corrupted after the restart 
* Lost properties after converting Doc-Heading
* Invalid Git commit time on macOS
* Improve list outdent
* Cannot open embed block in Android

## v1.1.4 / 2021-03-19

### Features

* Use vis.js instead of D3.js for graph
* Support custom font color and background color

### Enhancements

* Improve thematic break editing
* Update font and background color
* Tab indent without children, ctrl+shift+i indent with children

### Bug fixes

* Can not open graph if exists duplicated nodes
* Can't not sync on macOS
* Unrecognized local theme and reset css
* After the heading is cut and then pasted, it becomes text
* Local and cloud space display is inconsistent
* After the table enter, the next block ID will change

## v1.1.3 / 2021-03-18

### Features

* Use Rsync instead of Git for sync
  * Since the cloud data has been emptied, it is necessary to upload the local data to the cloud through synchronization-upload first
  * Removed the automatic synchronization function, if you need to synchronize, please operate manually
  * The Git automatic commit interval will be reset to 0, that is, the Git version management function is disabled. If you need to open the version management function, please set the interval value to a value greater than 0. It is recommended to set it to 10, which means that the version will be submitted automatically every 10 minutes
* The initial size of cloud space has been expanded from 4G to 8G
* The graph has undergone a preliminary remake, please manually reset the graph parameters once

### Enhancements

* File tree and recent document exchange location on Android
* Double click gutter icon to update memo
* Improve reload after sync error
* `、` is only useful at the beginning
* Input `》` can also be converted to blockquote
* Improve name style in the table, code block and math block
* Support ⌘ARROWDOWN/⌘ARROWUP for config hotkey
* Backmention doc name is so short
* Expand the default cloud storage space to 8G
* Improve boot speed on Android
* Improve search sort
* Click the daily notes button to select notebook
* When dragging a image to Scribli, disable its cursor selection
* Improve click and dblclick at the end of the block
* You can use tabs for indentation anywhere in the list
* Clicking on the outline cannot locate the collapsed heading

### Bug fixes

* After Ctrl+A can not remove embed ref
* Only show one backlink
* Copy document ref, paste as embed ref is error
* No authentication is required when URL include /stage/
* The same block ref show twice sometimes
* Failed to parse `<table>` tag
* The graph can not show if customized graph style
* Cut and paste will cause duplicate id
* The text before the ref cannot use alt+z
* Export PDF can not load static resource
* DeleteContentForward at the end of the paragraph, when there is a ref in the next paragraph, the ref is wrong
* `*` after entering a space, the list disappears
* Folders cannot be sorted before and after the document by dragging and dropping

## v1.1.2 / 2021-03-10

### Enhancements

* Improve graph performance
* Preview and export has no class `vditor-task--done`
* Set sync flag to true after clone

### Bug fixes

* Complex sql parsing problems
* Failed to custom appearance on Android
* Boot hangs sometimes
* Cloud assets path issue
* Not download assets after clone

## v1.1.1 / 2021-03-09

### Features

* Filter daily note in the graph
* Drag asset from the file tree to the doc
* Block ref '((' support name, alias and memo
* Global graph support type filtering
* SQL query API

### Enhancements

* Copy or drag content with pictures to a different folder
* Divide assets icons into video, audio, image, pdf
* Open or new file in the focus panel
* Ctrl+F  code block
* Anchor text is incomplete when pasting block ref
* Add search settings name, alias and memo
* Improve Ctrl+F/R performance
* Display boot progress
* Separate the settings of the global graph and the local graph
* Improve focus in the file tree
* Improve export for block ref
* Real-time display of memo on gutter icon
* After `shift + tab`, the first list item cannot be changed into paragraphs
* Save the layout of the PC browser
* Improve import performance
* Improve graphviz style
* Improve backmention to backlink

### Bug fixes

* Switch theme mode, the code theme is incorrect
* Ctrl+F continuous search bug
* Improve enter at the heading with fold
* Failed to create a doc on WebDAV
* Mind map will be lost data

## v1.1.0 / 2021-03-04

### Features

* Add "Move to" in the file tree and editor tabs

### Enhancements

* Improve rename case sensitive
* Android cannot keep the last opened document
* Flatten backlinks
* Heading in the list item, the gutter icon can not align
* Remove .git in sub folders when syncing
* Improve wiki link convert when importing
* Reduce Android size
* Improve fold list style
* Ctrl+F performance optimization
* Improve link text parse
* Slash menu remove `、` hint
* Improve performance for indexing
* Change asset ID from prefix to suffix
* Trim starting empty blocks when rendering templates

### Refactor

* Upgrade Electron

### Bug fixes

* Cannot synchronize using the sync button in the browser through android APP
* The problem when creating daily note using template
* After resizing in the dock panel, closing one will leave blank

## v1.0.9 / 2021-03-02

### Features

* Support open/close/new/recent notebook for the online workspace and Docker

### Enhancements

* Code signing on Windows
* Import template can not keep the empty block
* Use built-in Git on Windows
* Add copy block id to file tree
* Android server supports copying to yuque
* Only sync .md for Git
* Improve order list outdent

### Bug fixes

* Ref in heading is error
* Use the template, the anchor text is error
* Empty task item render error when open again
* Before saving the file on Android, go to other files will be overwritten
* Ref create doc save location and Template path can not save `"`
* Undo will overwrite the content of the current document with the content of the last opened document
* `Alt+Ctrl+A` The handle is invalid

## v1.0.8 / 2021-02-28

### Features

* Historical search conditions

### Enhancements

* Improve ref code block anchor text template
* Improve asset file name link text
* Improve daily note selection
* Prevent repeated clicks when refreshing the file tree and sync
* Improve online workspace/publishing loading
* Global graph show unrelated nodes
* WebDAV connect URL must specify to folder
* Improve list enter
* Improve sync message notify
* Improve memo on icon for fold
* Add notebook settings for Android

### Bug fixes

* The tag in the doc have no lines in the graph
* Apple Silicon version fails to open
* Text mode `created between` syntax does not work
* Kernel crash on saving doc sometimes if using WebDAV notebook
* Can not sync after open User Guide

## v1.0.7 / 2021-02-26

### Features

* Search settings

### Enhancements

* Support ARM architecture on macOS (Apple Silicon)
* Deleting a checkbox block will add several newlines below
* Improve update
* Support Windows 32-bit
* Improve drag icon to the checkbox

### Bug fixes

* Failed to create daily note
* WebDAV can not save the box config

## v1.0.6 / 2021-02-26

### Features

* Notebook settings
* Copy standard Markdown, Zhihu, Yuque and WeChat MP using cloud asset path
* Auto-sync at interval

### Enhancements

* Improve sync
* Display the latest sync time
* Improve assets download performance
* Do not reload UI if there are no changes when syncing
* Add template var `{{.alias}}` for doc
* AppImage for Linux
* Improve `Ctrl+Shift+A`
* Improve drag list item to another list item

### Bug fixes

* Import folder, custom sort can not work
* Create doc failed sometimes
* Some formats of assets upload failed
* The first child of the list item is the code block, and the collapsed list item shows an error
* Parse error when `+`  in the middle of ref

## v1.0.5 / 2021-02-24

### Features

* Assets storage in the cloud
  
  Starting from this version, cloud synchronization will be divided into Git synchronization and asset file synchronization, which can significantly improve performance and reduce space usage by about half.

  * Git synchronization is only used to synchronize .md files, automatically ignore the assets folder synchronization through .gitignore
  * The asset files referenced in the document under the assets folder will be synchronized through upload and download, and the asset files that are not referenced will not be synchronized

  Note:

  * Every time Scribli synchronizes, it will automatically add the `assets` line in .gitignore, that is, ignore the assets folder submission
  * Because assets are not included in Git management, there will be no version history support, please confirm clearly when deleting asset files
  * If you use other Git warehouse services, please manually modify .gitignore and then use `git`

  Upgrade suggestions:

  * Please follow `Help Document - Versioning and Synchronization - FAQ - How to delete unnecessary history records to reduce space usage and improve performance`

  In the future, we will continue to improve cloud asset file storage services, adding management functions such as uploading, viewing, and deleting to facilitate users to share documents across platforms and applications. In addition, we plan to complete the free expansion of cloud space for all paying users before the end of March. The basic space will be expanded from 4G to 8G. Thank you for your company and encouragement. We will continue to work hard.
* Add database table assets

### Enhancements

* Git sync ignore assets by default
* Inline math and math block are not the same size
* When link contains a image, can not jump to the corresponding link after clicking
* In mind map, press space can edit it
* No prompt after `#xxx` input `#`
* Support MIUI 12.5
* Assets name retain scores and underscores

### Bug fixes

* The Dynamic query does not work in template
* Fold heading show `Failed to query content block`
* Import list problem
* Delete under the collapsed list item will delete its subitems

## v1.0.4 / 2021-02-23

### Features

* Open new tab by ctrl+click  when set `Open in the current tab`

### Enhancements

* Render result after the cursor leaves dynamic query
* Add alt+click for `Open Below the Tab`
* Add option `Close the kernel when exiting the interface`
* Rendering error when the app opened for the first time contains math
* Ignore .Scribli sync
* Bazaar templates/themes sort by update time desc

### Bug fixes

* Folding of the embed block will cause the outer layer to be folded
* UI process does not exit sometime
* Can't use super block in templates
* Cannot create diary after setting template
* File Tree cannot be refreshed automatically

## v1.0.3 / 2021-02-21

### Features

* Save update time of blocks

### Enhancements

* Showing tags on the graph
* Add template var `{{.id}}` for doc
* Improve def block render performance
* (( After allowing input of `!`, `/` and `、` to filter
* Database table blocks `time` field rename to `created`

### Bug Fixes

* Export pdf can not use custom.css
* Copy the ref and then paste it incorrectly
* The heading bar is dragged incorrectly from top to bottom
* Order list Shift + Tab is incorrect
* Some files in the file tree cannot be displayed
* List item ID changed if set background color

## v1.0.2 / 2021-02-20

### Features

* Copy image into clipboard

### Enhancements

* Support Android 11
* In browser, can remove webdav
* Use bellow method, can not save layout
* Change installation method on Windows
* End of support for auto-update on macOS and Linux
* End of support for auto-update if using zip package on Windows
* Support search/replace for inline math
* Let the prompt box display completely

### Fix bugs

* Select marker * cannot set the font color
* Task list can not input ref
* The name of the list item will be transmitted to the next list item after the line break
* Report an error after remove `alt+M` in setting -> hotkey

## v1.0.1 / 2021-02-19

### Enhancements

* If task list item checked, add class `vditor-task--done`
* Add default value for the diary storage path
* Improve update
* Before clicking ref to open the document, close the pop-up window
* Improve clone performance
* Improve sync performance

### Development refactoring

* Kernel upgrade to Go 1.16

### Fix bugs

* Bookmark invalid
* Heading cannot be converted into a doc
* Gutter icon are not aligned
* `((` type filter problem

## v1.0.0 / 2021-02-18

### Features

* Support copy to yuque.com

### Enhancements

* Support custom keymap for Alt+M
* End early bird discount subscription
* Improve sync process on Android
* Optimize the editing performance on the large document
* Improve boot process

### Fix bugs

* Heading as list item problem
* Crash on Android 8.1
* Show/hide super block mark, wrong position of gutter icon
* Kernel interrupt
* UI error when editor is fullscreen
* Exception after renaming the same file twice
* Sync issue on Android 8.1
* After `:`, press `alt+z` error
* Outline display error

## v0.9.9 / 2021-02-16

### Enhancements

* Improve select multiple blocks for type conversion
* Improve Alt+M
* Improve update
* Change folder to private storage on Android
* File tree hide `.md` on Android
* Improve sync
* Improve navigation bar on Android
* When mouse move in dock panel, show its toolbar and tree arrow icon

### Fix bugs

* Android set font size can not work
* Tag hotkey can not work
* Template call is not responding
* Empty list exposes ID problem
* Error: A JavaScript error occurred in them main process

## v0.9.8 / 2021-02-15

### Features

* File tree custom sorting
* Highlight panel by focus

### Enhancements

* The fold state of the parent block does not affect the child block
* Stick toolbar in the backlink tab
* Improve the ctrl+z behavior after cut/paste
* Android create diary
* Optimize performance
* Change Scribli to private folder on Android
  Existing data needs to be manually migrated to the new in-app data directory.

### Fix bugs

* Paste content in dynamic query embedding will be repeated
* Frozen after pressing alt+m twice
* Embed block can not use hint and code block
* Failed to query content block when folding
* Can't rename file when focus is not in editor
* Android soft keyboard hide editable content at the bottom

## v0.9.7 / 2021-02-13

### Features

* Android open word, excel and hyperlink

### Enhancements

* Display tag and bookmark in flat
* Ref block needs to be processed when copying as markdown
* Android open account settings using the external browser
* Remove auto space of inline math
* Improve booting
* Improve block ref, hyperlink and tag link jump
* Right-click menu display is incomplete

### Fix bugs

* Copy block ref in search panel is error
* Remove block hotkey can not auto save

## v0.9.6 / 2021-02-11

Happy Chinese New Year 🎉

### Features

* `/` can quickly prompt frequently used functions
* Android add appearance setting

### Enhancements

* Improve update mechanism
* Android add tag/slash/template hint

### Fix bugs

* Android Wrong time zone
* Android query embed can not display

## v0.9.5 / 2021-02-10

### Features

* Android APP
* Android supports synchronizing cloud repositories
* Android supports cloning cloud repositories

### Enhancements

* Blank lines are generated when using the arrow keys to move out of the code block
* Distinguish the type of task list block and normal list block
* after drag/resize, keep then cursor in the editor
* Subtype of blocks
* Check existence of kernel binary before booting it
* Add icon and name for kernel on Windows

### Fix bugs

* List collapsed picture problem
* Open more instances of Scribli on macOS problem

In addition to the above, the Scribli static site generator OceanPress developed by the community contributor [崮生](https://github.com/2234839) is officially migrated to the community organization, everyone is welcome to try and contribute ❤️

## v0.9.2 / 2021-02-08

### Enhancements

* PDF browsing interface optimization
* List item ctrl+shift+x problem
* Trim trailing newline when copying code block
* Improve application exit mechanism
* Improve the handling of bold content when copying content from web pages

### Fix bugs

* Blockquote in the super block will automatically add blank lines
* Image centering does not work when exporting PDF
* The cursor is lost when the super block is continuously entered
* Inserting a picture into the table causes the problem of adding rows
* LaTeX cannot be rendered when exporting to PDF
* Docker container crash
* After the ordered list exceeds 10, pressing tab will not indent correctly
* List tab and then press ctrl+z, the behavior is abnormal
* Issue of inserting a picture in the folder name containing '#'
* Cannot fold the code block after shift enter
* Click the asset file link in preview mode to report an error
* Asset tab can not locate on the file tree

## v0.9.0 / 2021-02-07

### Enhancements

* Member subscription paid function prompt

  If you do not have a paid subscription, you will be prompted when you use Advanced Features. After v1.0.0 is released on 2021-02-19, advanced features can only be used after paid subscription. **If you already have data stored in the cloud and do not plan to pay for subscription, please export it as soon as possible**.
* Search and merge text mode and SQL mode

  Removed the SQL mode button on the quick search box, if you need to use SQL search to write SQL statements directly.
* Android mobile terminal has started public testing, which can be downloaded on GitHub or Baidu Netdisk
* Support shortcut keys for picture setting position
* Block aliases and notes support shortcut keys
* Improvement of floating window interference of relationship graph node
* Online workspace, sharing and publishing entrance adjustment

  Make the entrance bigger and more visible and easier to click.
* Block folding optimization, support <kbd>Alt+Click</kbd> click block icon to collapse

### Development refactoring

* Upgrade Electron framework on desktop

### Fix defects

* File tree location open file display defect
* Modify the content in the floating box of the block quote
* Fix the problem that the label auto-complete list prompts residual
* Fix the problem that the list block cannot be expanded after being collapsed

---

In addition, we have divided the language of the community:

* For Chinese feedback, please go to Scribli Notes-Domain-Link Drop
* For English feedback, please go to Issues · Scribli-note/Scribli

**The content of the wrong partition will be closed or deleted directly**, thank you for your support ❤️

## v0.8.5 / 2021-02-04

### 引入特性

* 支持跨文档拖拽移动块
* Android 端支持打开 data 文件夹下的笔记本

### 改进功能

* 优化导入性能

### 修复缺陷

* 网页标题粘贴至表格的问题
* MS Excel 粘贴带图表格问题
* MS Word 表格粘贴问题
* 文件夹内的文档块不支持书签
* WebDAV 连接报错

## v0.8.0 / 2021-02-03

### 引入特性

* 支持将文档渲染为幻灯片演示

### 改进功能

* 内容块备注展现
* 支持 CMake 代码高亮
* 停靠面板加入最小化操作按钮

### 修复缺陷

* SQL 动态查询空表名 `dual` 报错问题
* 列表 Tab 携带同级项问题
* 文档仅索引 128 个的问题
* 任务列表空段落问题
* 设置书签后没有在书签面板中实时刷新的问题

## v0.7.8 / 2021-02-02

### 引入特性

* 支持 PlantUML 在线渲染

### 改进功能

* 列表项创建的编辑逻辑优化
* 统一列表渲染为松散模式
* 网页端 Favicon 
* 简化主体界面
* 列表 Tab 带子项层级缩进
* 增加图表悬浮提示

### 修复缺陷

* 标签和列表全选剪切的问题
* 窗口图标对齐问题
* 列表缩进关联子项的问题
* 停靠栏上下方按钮消失问题
* 超链接和分隔线排版解析问题

## v0.7.5 / 2021-02-01

### 引入特性

* 界面支持 Dock 停靠栏

### 改进功能

* 自动拉取图片时 jpe 后缀改为 jpg

### 修复缺陷

* 复制导致 ID 重复的问题
* 列表回车导致图片缩放失效的问题
* 访问授权码置空 `BEYOND` 不生效问题
* 有序列表回车返回父级 ID 暴露问题
* 大纲中无法显示 HTML 标签包裹文字

## v0.7.1 / 2021-01-31

### 引入特性

* 引入新的编辑模式 - 专家模式
* 关系图节点支持编辑浮窗

### 改进功能

* 列表回车跳出需添加当层列表一级
* 多级列表的编辑逻辑优化
* 粘贴代码时代入最近语言
* 新增百度云、蓝奏云下载渠道
* 文件树最大列出数量支持配置

### 修复缺陷

* 脑图模式下列表项查询内容块失败
* 列表项缩进和反向缩进问题
* 列表项退格删除问题
* 列表项之间回车新建列表项问题
* 列表、列表项间隙不一致的问题
* 空的列表项回车反向缩进层级问题
* 标题块模板片段显示异常
* 使用表达式查询时标题块没有显示下方块的问题

## v0.7.0 / 2021-01-29

### 引入特性

* 关系图搜索过滤和渲染参数配置
* 展现标签所属块之间的关系

### 改进功能

* 已设置书签的文档需要在书签设置菜单中高亮
* 悬浮窗无法上下滑动
* 关系图节点信息优先显示块名或备注
* 关系图和全局关系图图标区分
* 双链浮窗 Esc 键关闭
* 标签面板内的结果排除模板
* 获取引用浮窗、计数等性能优化
* 粘贴后撤销行为异常

### 修复缺陷

* 双链列表项渲染问题
* 文件树上的文档点击引用计数报错
* 模板函数无法嵌套文档名变量 `{{.title}}`
* 导出 PDF 时 Mermaid、YFM 没有渲染的问题
* 窗口激活时顶部单像素宽的白边
* 文件树拖拽覆盖重名文档问题

## v0.6.8 / 2021-01-28

### 引入特性

* 反链页签上下文展现
* 支持停靠栏显示/隐藏
* 支持 PDF 页码跳转

### 改进功能

* 标签页签上下文展现
* 书签页签上下文展现
* 带样式导出 PDF 和 HTML
* `((` 触发块引搜索默认选中优化
* 搜素页签默认展开第一个搜索结果
* 同一文件夹下资源文件过多不提示
* 移除 .sy.md 和 .sy.export.md 识别
* 搜索默认不仅在根一级上进行

### 修复缺陷

* Alt Tab 切换窗口后会触发折叠
* 文档重命名问题
* 设置中搜索无法匹配关键词
* 在本地浏览器打开失败
* 某些文件保存不了（或者提示没保存）的问题
* 设置中快捷键设置无效
* 粘贴不了 MS Office Excel 内容的问题

## v0.6.7 / 2021-01-25

### 引入特性

* 支持 SQL 查询数据库模板函数
* 初步引入专家模式
* 图片支持设置位置
* 初步引入 Dock 停靠栏
* 支持创建本地笔记本

### 改进功能

* 按下 Alt 时鼠标所在位置需高亮
* 新增安装包下载渠道
* 链滴社区中为订阅用户赋予“订阅者”头衔
* 设置 - 帐号中可配置是否显示顶部工具栏中的头衔和 VIP 标识

### 修复缺陷

* 编辑器内搜索偶尔失效的问题

## v0.6.6 / 2021-01-21

### 引入特性

* 支持历史文件回滚
* 编辑器支持 Git 冲突标记解析渲染

### 改进功能

* 撤销操作页面没有跟踪光标处
* 中文右双引号无法输入
* 导出 HTML 设置编码
* 导入 `[[wikilink]]` 时锚文本使用模板变量 `{{.text}}`
* 按下 Alt 时鼠标所在位置的块进行高亮

### 修复缺陷

* 「Shift + 左箭头」从右向左多选文字时可能出现问题
* 撤回时页面会向上滚动一大截
* 网页复制粘贴自动空格问题
* 列表项中选中文本新建文档时缩进异常
* 撤销操作无法复制代码
* 块引锚文本内行级元素渲染问题

## v0.6.5 / 2021-01-20

### 引入特性

* 导入 Markdown 到已有笔记本文件夹下

### 改进功能

* 文件树展开折叠状态保持
* 字体样式设置应放在右键选项中
* 反链里的容器块按子块简化展现
* 去掉文件夹下对非思源 .md 的导入支持
* 内容块折叠操作方式优化

### 修复缺陷

* 清除选中内容中的所有字体格式
* 删除嵌入块前面的内容导致的解析渲染问题
* URL 中的 `&not` 自动转换成字符 `¬`
* 反链提及重复问题
* 搜索结果带 HTML 标签时显示异常
* 主题自定义 (custom.css) 更新问题
* 内容块动态查询嵌入 SQL 模式问题
* 列表项的命名块无法使用 `!{{name:xxx}}` 查询
* 表格块下多次正向链接，在关系图中只显示一个节点的问题
* 导出 TextBundle/Markdown 时本地图片路径错误

## v0.6.4 / 2021-01-19

### 引入特性

* 编辑器支持查找替换
* 点击文档块引用计数显示引用出处

### 改进功能

* 模式选择菜单显示当前选中模式
* 复制标准 Markdown 和复制 kramdown 分开
* 拖拽插入资源文件 128M 限制改为 1G
* 降低 conf.json 配置写入

### 修复缺陷

* 导出 PDF 代码压缩问题
* 全屏模式下编辑器工具栏点击识别问题
* blockquote 上下键及回车问题修复
* IP 网址粘贴创建了空链接
* 正则搜索时转义符 `\` 处理问题
* 使用模板生成的日记内容格式混乱
* blockquote 中多个代码块之间出现多余空行
* 块命名丢失及编码问题

## v0.6.3 / 2021-01-18

### 引入特性

* 大纲搜索过滤
* 内容块别名

### 改进功能

* 文档块引用计数展示
* 文档块支持备注
* 内容块命名的名称展示
* 带有链接的块折叠时没有视觉提示
* 方向键上键有时无法控制光标！
* 使用划选的内容作为搜索引用锚文本或者快速搜索关键字
* 初始化时设置随机的访问鉴权密码

### 修复缺陷

* 更新内容块内容后属性丢失
* 快速搜索中最近使用的块源码暴露问题
* 多关键词检索时预览区没有高亮
* 调用模板时，动态内容块嵌入 未渲染问题
* 正则表达式搜索失效问题
* 调用模板后，字体颜色效果消失

## v0.6.2 / 2021-01-17

### 引入特性

* 编辑器内容右键划词搜索并链接
* 模板集市

### 改进功能

* 只读模式下禁用编辑功能
* 颜色取消后，粗体页需要取消
* 在引用块的中间部分按下Enter两次会直接跳出引用块
* 编辑器右键划词搜索支持新建搜索页签
* 调整悬浮窗口和搜索、反链等非主编辑区的字号行距
* 优化行级数学公式光标位置
* 保留空段落

### 修复缺陷

* 含有行内公式的句子加粗问题
* 工具栏设置标题只能设置为一级标题问题
* 脑图渲染问题
* 悬浮框编辑时右键菜单重叠被覆盖
* 搜索页签子标签无法搜索到内容

## v0.6.1 / 2021-01-16

### 引入特性

* 搜索页签
* 编辑器 Ctrl 点击标签触发搜索页签
* 编辑器划词触发搜索页签

### 改进功能

* 代码块复制不带 ```lang
* 清理未引用资源时包含文件夹引用情况
* `#` 在代码块也会产生不必要的下拉菜单

### 开发重构

* 包路径重构

### 修复缺陷

* 内容块嵌入中的文本字体设置失效
* 表格内 Tab 键切换单元格问题
* 去掉脑图中引用的 `{{.text}}`
* 图片缩放兼容 Firefox
* 嵌入块显示的时候空白过大问题
* 代码块清空内容后仍然保留的问题
* 复制引用时结尾去掉多余的锚文本
* 图片路径编辑问题

## v0.6.0 / 2021-01-14

### 引入特性

* 引入新的编辑模式 - 脑图模式
* 内容块动态查询支持搜索表达式

### 改进功能

* 支持 Stata 代码块高亮
* 支持 PowerShell 代码块高亮
* 日记设置中的笔记本名称由输入改为下拉选择
* 支持在链接文本和代码内容中搜索

### 修复缺陷

* 编辑器内搜索表格无法定位
* 块嵌入时内部引用渲染问题
* 文档增删后父级文件夹计数不更新
* 缩放图片后不居中问题
* 模板中使用内容块动态查询报错问题
* 嵌入被错误识别成引用

## v0.5.9 / 2021-01-11

### 引入特性

* 日记

### 改进功能

* 缩放图片提供常用缩放百分比
* 标签补全提示优化
* 改进内核启动机制

### 修复缺陷

* 数学公式尾行按上键会跳出
* 点击图片偶尔不会显示资源菜单
* 超过 2520 个块的文档打不开问题
* 搜索时搜不到行级排版元素的问题
* 设置背景色后多层级块图标会重叠

## v0.5.8 / 2021-01-08

### 引入特性

* 批量导出标准 Markdown 文件
* 内容块命名、备注和样式
* 笔记本内笔记数量显示

### 改进功能

* 反链提及使用内容块名称进行搜索
* 折叠块时报“查询内容块失败”
* 超级块改进
* 块图标交互修改
* 编辑器右键菜单改进

### 修复缺陷

* 标题块折叠拖动问题
* 引用链接超过 7 个显示异常

## v0.5.7 / 2021-01-07

### 引入特性

* 支持内容块折叠
* 支持插入图片大小设置

### 改进功能

* 左侧图标和系统的最大化图标距离太近了
* 优化文件树文件定位
* 鼠标从右到左选择文字设置字体颜色优化
* 行级属性不暴露在编辑器中
* 标题块拖拽移动时包含下方内容块

### 修复缺陷

* 快捷键重置问题
* 字体彩色变糊问题
* 循环引用导致内存泄漏进程崩溃问题
* 移动文档时 assets 附件不跟随移动的问题

## v0.5.6 / 2021-01-06

### 引入特性

* 支持加粗、强调、删除线和代码设置属性，实现自定义文字颜色等样式
* 自定义快捷键

### 改进功能

* 代码块复制按钮改进
* 简化容器块的反链提及结果
* 引用数标识应显示多个定义块
* 改进 HTML 转换 Markdown 时加粗、斜体等空格的处理
* 文件树上同一文件夹下最多显示 512 个文档

### 开发重构

* 重写底层，降低内存占用
* 文档内容变更以后数据状态通知
* 重构反向链接、书签底层实现

### 修复缺陷

* 反链提及快速链接问题
* 配置搜索报错
* 开启下标语法后删除线语法失效
* 嵌套引用锚文本模板渲染问题
* 使用模板导致标题块图标在错误的位置

## v0.5.5 / 2021-01-01

### 引入特性

* 内容块书签右侧标识
* 支持内容块拖动排版
* 鼠标移动到引用数上查看被引用的块
* 连接 WebDAV 支持文件夹选择

### 改进功能

* 移动端浏览器兼容
* 支持通过鼠标中键关闭页签
* 搜索时包含文档块且文档块排最前面
* 编辑器标题块标识和书签标识是否显示的配置

### 开发重构

* 重构标签、模版等底层实现

### 修复缺陷

* QQ 拼音输入法自动补全时块引符号时末尾多出 `]]`
* 有时启动会一直 loading 进不去

## v0.5.46 / 2020-12-30

### 引入特性

* 内容块标识区域显示层级结构和引用数
* 邀请订阅奖励机制

### 改进功能

* 页签右键添加关闭左侧、右侧、未修改功能
* 预览悬浮窗口默认可编辑，支持钉住（Pin）
* 内容块加入创建时间显示
* v1.0.0 发布时间
* 改进文件树上的文档拖拽转换标题时的交互
* 简化全局和块引搜索结果

### 修复缺陷

* 上下标无法解析 `+` 和 `-` 问题
* 模版插入未自动保存问题
* 安装集市主题后自定义修改不回显的问题
* 搜索忽略高亮 `==Mark==` 标记符和空格
* 预览悬浮窗口大小计算问题

## v0.5.45 / 2020-12-28

### 引入特性

* 托盘快捷键
* 文件树上加入刷新操作
* 编辑器内资源右键添加删除功能

### 改进功能

* 双击后快捷键改进
* 块引支持搜索表达式
* 搜索支持锚文本模板

### 文档相关

* 完善 Docker 镜像搭建服务相关文档

### 修复缺陷

* Windows 上 Alt F4 改为完全退出程序
* 预览时大纲无法定位
* 卸载主题问题

## v0.5.44 / 2020-12-27

### 引入特性

* 主题集市

### 改进功能

* 加入启动参数 `--ssl`
* 重命名时需要判断大小写
* 支持年付订阅
* 全局搜索改进
* 换行时不要强制居中打字机
* `protocol://` 协议打开其他软件
* 超级块导出去掉标记符

### 修复缺陷

* 火狐使用时无法复制
* 图片块引用展示异常
* WebDAV 贴图问题
* 引用块无法拷贝 ref id
* 带 Emoji 的文件夹/文档排序不稳定问题
* 列表项块引用修改提示找不到

## v0.5.43 / 2020-12-25

### 引入特性

* 搜索支持表达式

### 改进功能

* 移除清理 ID 功能

### 修复缺陷

* 行级公式渲染大小自适应标题级别
* 块嵌入的表格内换行符消失
* 使用本地绝对路径引用资源时的问题
* 块引用的锚文本行级渲染问题
* 反链提及文档名未正常显示
* 中西文添加空格等选项从编辑时改为预览/导出时
* 移除中文后标点符号自动替换为中文标点
* 不同文件夹下嵌入的图片预览时不显示
* B 站视频插入播放问题

## v0.5.42 / 2020-12-21

### 引入特性

* 导出时支持配置标签的开闭标记符
* 代码块预览加入行号

### 改进功能

* 文档关系图按逻辑层级呈现
* 导出完成提示自动关闭
* 改进网页上的代码块剪藏
* 改进 `[[wikilink]]` 导入，识别 Obsidian 短路径格式
* 在安装目录下写入应用日志 app.log
* 全局关系图仅体现文档块之间的联系
* 改进内核退出，让退出过程更快一些
* 查看编辑历史性能优化

### 修复缺陷

* 网页内容复制后无法剪藏
* 列表内粘贴插入位置错误
* 配置在安装目录下时自定义外观问题
* 内容块嵌入编辑时内容不保存
* 自定义主题重启不生效以及代码重复生成问题
* 打开空文件夹不应该走导入向导
* 导入导出问题
* 快速插入标签额外多插入了一对 “#”

## v0.5.41 / 2020-12-18

### 引入特性

* 支持文档块书签

### 改进功能

* 去掉代码块预览选项，代码块默认预览高亮
* 去掉实时导出 Markdown 功能
* 上线付费订阅
* 从安装目录下读取配置文件夹
* 废弃锚文本模板变量 `{{.title}}`

### 修复缺陷

* 列表项块反链包含逻辑问题和引用计数问题
* 导出 PDF 有时图片、代码块和图表等渲染有问题
* 笔记本文件夹根一层没有 .md 时的问题
* 全局搜索卡顿问题
* 从托盘处激活报错

## v0.5.4 / 2020-12-16

### 引入特性

* PDF 导出
* 点击查看大图功能
* 在引用内右键新增删除引用操作

### 改进功能

* 表格编辑改进
* HTML 块预览开关
* assets 资源文件支持改进
* 文件树上显示文档更新时间和大小
* 冒号后 emoji 输入直接回车行为改进
* 块引创建文档时支持指定路径
* 优化多开启动
* 动态查询嵌入默认隐藏 SQL
* 文档名去掉 `_id.sy` 段
* 独占一行的图片默认居中
* 搜索面板中的更多修改为右键
* 新增模板变量 `title`

### 文档相关

* 同步第三方远程仓库的操作文档

### 修复缺陷

* {{.title}} 模板需要修正的几个地方
* jfif 后缀资源图片清理未引用资源问题
* 剪切块内容时，无法进行撤销
* 文档块标题块转换时需复制关联的资源文件
* 鼠标悬停被任务栏遮挡

## v0.5.3 / 2020-12-12

### 引入特性

* 打通正文与文件树的壁障

### 改进功能

* 图注支持加粗、斜体和公式等行级渲染
* 修改标题块嵌入时提示不支持保存
* 块引搜索不区分大小写，支持类型前缀转义
* 支持分屏并移动
* 文件树排序忽略 Emoji
* 嵌入块的锚文本应当使用 .title
* 右键复制块引用修改为模版变量

### 修复缺陷

* 页内搜索问题
* 打开最近笔记本如果是 WebDAV 的话会报错
* 通过远程 IP 访问时图片不显示的问题
* 大纲渲染 `<foo>` 时的转义问题
* 预览时段落开头空两格对齐
* 清理未引用资源时需要将 HTML src 计入
* 标题上使用模板的问题

## v0.5.2 / 2020-12-07

### 引入特性

* 模板片段
* 块引锚文本支持模板变量
* 新建文档时文档名支持模板片段

### 改进功能

* 内核退出和监测机制改进
* 支持导入 .markdown 后缀
* 在编辑器内复制时不应该带块 ID

### 开发重构

* 重新实现最近使用的块

### 修复缺陷

* 下标渲染为上标问题
* 内容块动态查询嵌入跳转问题
* 音频资源文件插入问题
* 大纲拖拽引起的窗口布局问题
* 复制代码到代码块问题
* 自动空格设置为关闭后还是会添加空格的问题
* WebDAV 图片显示问题
* 加密配置文件中的 WebDAV 密码字段
* 粘贴不了 Excel 内容的问题

## v0.5.1 / 2020-12-03

### 引入特性

* 最近的笔记本列表
* 支持上标下标语法

### 改进功能

* 块引悬浮框大小支持拖动
* 手机浏览器打开 assets 资源文件
* 稳定表格的 Markdown 格式化
* 代码块高亮支持语言由 45 种扩展为 51 种
* 文件树中文档前加上图标
* 文件树排序配置持久化
* `private.key` 使用临时随机文件
* 代码块主题样式由 37 种扩展为 98 种
* 同步检查本地笔记本大小时排除 .git 元数据

### 修复缺陷

* 删除空行后公式错位
* F11 全屏后窗口最大化状态不对的问题

## v0.5.0 / 2020-12-01

### 引入特性

* 超级块 {{{ blocks }}}
* 内容块 URL 定位

### 改进功能

* 改进 ToC 渲染，支持点击跳转
* Ctrl B 加粗文字后光标位置改进
* 在新窗口打开块引
* 预览模式下粘贴公式到公众号公式尺寸异常
* 块嵌入内容直接选择复制
* 表格内 `<br>` 编辑表现不一致
* 行级公式作为西文对待加空格
* 笔记本路径不能让用户选择在安装路径上
* `echarts` 代码区编辑体验问题
* macOS 窗体按钮改进
* 编辑右键菜单“设为文档标题”
* 文件树字母排序考虑拼音
* 改进同步实现
* 内核连接检查

### 修复缺陷

* YAML Front Matter 中删除问题
* 切换明亮暗黑模式后图表相关渲染没有切换
* 块引用块内按 Ctrl+B 会自动换行
* 大纲层级折叠问题
* 代码块渲染问题
* 预览时图片等附件资源链接错误
* 第一次导入时不应该添加中西文自动空格
* 软换行后粘贴的图片无法正确渲染

## v0.4.9 / 2020-11-23

### 引入特性

* 选中内容右键“作为内容新建文档”
* 编辑器自定义字号
* 支持图片标题渲染
* 流程图、甘特图、时序图、图表、脑图、五线谱、添加暗黑模式

### 改进功能

* 粘贴链接优化
* 列表项批量缩进和取消缩进
* 数学公式块去除背景
* 搜索结果一键复制
* 嵌入编辑进入时不跳到第一行
* 改进启动引导

### 修复缺陷

* 资源附件本地打开问题
* 使用 MathJax 引擎编辑公式时白屏的问题
* 大纲只能折叠根节点下第一层的问题
* 输入 HTML 实体之后无法继续再输入其它字符
* 标签无法显示
* 引用计数的气泡被侧边栏遮住了

## v0.4.8 / 2020-11-19

### 引入特性

* 支持表格编辑
* 大纲折叠

### 改进功能

* 打开笔记本文件夹时提示文案
* Ctrk J 任务列表后再取消遗留空格
* 大纲中渲染 Latex、行级代码、粗体斜体
* 公式块中的 `\tag` 语法样式改进
* 搜索忽略大小写

### 修复缺陷

* 笔记录音问题
* 鼠标移动到滚动条上的样式问题
* 路径带 `%20` 的图片清理和导出 TextBundle 问题
* 行级数学公式无法正确渲染
* 脑图, e-charts,mermaid 编辑后无法渲染
* 松散任务列表块初始化渲染问题

## v0.4.7 / 2020-11-17

### 引入特性

* 内容块动态查询嵌入
* 内容块 URL

### 改进功能

* 编辑栏的高亮按钮和快捷键
* Latex 渲染大小自适应标题级别
* 流程图自适应大小
* 字数统计和大文档保存性能优化

### 修复缺陷

* 无序列表变成有序列表后块 ID 改变的问题
* 长文档不能自动保存
* 悬浮预览内容为空的内容块时闪烁问题
* Markdown 解析异常导致内核启动失败
* 文件夹下文档反链提及搜索为空

## v0.4.6 / 2020-11-15

### 引入特性

* 内容块嵌入编辑
* 块引悬浮预览嵌套浏览
* 搜索支持 SQL

### 改进功能

* 版本管理开关
* 云端笔记本克隆到本地时增加提示
* 粘贴时减少抖动优化
* 支持标签搜索
* 用“剪切，粘贴”移动被引用的块之后，引用丢失目标
* 移动端支持上传
* 同步库名支持空格、短横线等符号

### 修复缺陷

* 块引使用标题前缀 # 过滤时前面多了一个 (
* 使用 assets 路径的 `<img>` 标签图片无法显示

## v0.4.5 / 2020-11-13

### 引入特性

* 反向链接支持搜索过滤
* 反向链接提及支持一键转为内部链接
* 块引用的锚文本支持 Latex、加粗、强调等行级排版
* 支持图片路径带空格的情况

### 改进功能

* 分享发布设置私有时即时生效
* 从选中文字新建文档名称不全的问题

### 修复缺陷

* 块引标题自动完成问题
* 任务列表编辑渲染问题
* 文档块 URL 打开问题

## v0.4.4 / 2020-11-11

### 引入特性

* 浏览器拖拽图文剪藏
* 支持设置浏览器端访问鉴权

### 改进功能

* 脚注改进
* Windows 版本内置集成 Git 
* 反链提及中默认纳入当前文档名作为锚文本进行搜索
* 链接引用改进
* 加入持久化块 ID 的配置选项

### 修复缺陷

* 修复启动白屏 404 问题
* 预览/引用/嵌入内容重复问题

## v0.4.3 / 2020-11-09

### 引入特性

* 文档块 URL

### 改进功能

* PDF 预览效果添加使用浏览器和本地 PDF 工具打开
* 改进只读模式
* 在关于页面加入伺服地址
* 为右键菜单加上图标
* 同步依赖官方 Git 安装
* URL 由 /assets/ 换为 /stage/

### 修复缺陷

* 偶发的启动白屏问题
* 右键菜单遮挡内容问题

## v0.4.2 / 2020-11-06

### 引入特性

* 支持当前编辑器页签打开新文档
* 在线发布

### 改进功能

* 预览模式点击 PDF 无法关闭
* 改进更新检查提示
* 脚注语法支持改进
* 链接引用语法支持改进

### 修复缺陷

* 新建文件夹不显示问题
* 导入时重复建立文件夹的问题
* HTML 标签 `<code>` 编辑解析问题
* 任务列表暴露 ID 问题

## v0.4.1 / 2020-11-04

### 引入特性

* 自定义书签标识
* 支持设置块引时新建文档默认存储路径
* 文件树排序加上自然序

### 改进功能

* 版本管理从自动保存时提交改为定时提交
* 优化创建、移动文档/文件夹/资源文件的性能

### 修复缺陷

* 账号登录状态过期问题
* 表格快捷键 Ctrl Shift + 失效
* 嵌入块无法复制 ID
* 自动保存时软换行失效问题
* 云端使用图片不显示问题
* HTML 标签 `<ms>` 导致卡死的问题
* - 列表项和 Setext 解析冲突导致内核崩溃问题

## v0.4.0 / 2020-11-02

### 引入特性

* 桌面版浏览器支持
* 当前编辑器页签文件树定位
* 内核加入只读模式

### 改进功能

* 数学公式显示优化
* 右键菜单中加入删除/剪切块
* 思源在线工作空间会话鉴权
* 块引用和嵌入加上路径信息

### 修复缺陷

* 粘贴块引用无效
* 文件夹下可以新建空文档名的问题
* 文本加行级代码软换行问题

## v0.3.9 / 2020-10-31

### 引入特性

* 文件树排序
* 自动识别桌面端/移动端浏览器并重定向到对应版本界面

### 改进功能

* 添加粗体、斜体、标签等行级元素文字颜色配置
* 文件上右键新增新建文档/文件入口
* 改进快捷键提示文案与本地系统一致
* 文档关系图层级结构优化

### 修复缺陷

* 任务列表项中嵌入内容块问题
* 内容块标识菜单分屏时无法弹出的问题
* 软换行行级代码失效问题
* HTML `<scope>` 标签解析渲染问题
* 列表项行级排版自动换行问题

## v0.3.8 / 2020-10-30

### 引入特性

* 以预览模式打开文档

### 改进功能

* 清理未引用资源-图片预览
* 标签及反向链接列表内容和路径分两行显示
* 清理未引用 ID 仅在发生变动情况下才写盘
* 链接/图片路径中存在中文时不进行编码处理
* 代码块、数学公式间距过大

### 开发重构

* 重写右键菜单

### 修复缺陷

* 重命名文件夹后图片显示异常
* 书签丢失问题
* 任务列表暴露 ID 问题
* 行级 HTML 解析问题
* 列表项中嵌入内容块问题
* 移动文档报错问题

## v0.3.7 / 2020-10-28

### 引入特性

* 支持文档导出为 TextBundle
* 块引搜索类型过滤

### 改进功能

* Mac 窗口按钮重做
* 块引用候选列表内容块预览
* 标签及书签中块列表增加路径信息
* Shift 单击时新窗口打开优化
* 粘贴后需定位

### 修复缺陷

* 编辑器行级内容拖拽问题
* 表格内容空格消失问题

## v0.3.6 / 2020-10-27

### 引入特性

* 复制内容块快捷操作

### 改进功能

* 选中文本后进行内容块粘贴时锚文本的优化
* 块 ID 复制粘贴改进

### 修复缺陷

* 多开时实例隔离问题
* 标签页签排序不稳定的问题
* 任务列表块编辑渲染问题
* 文件夹拖拽移动问题

## v0.3.5 / 2020-10-26

### 引入特性

* assets 资源预览页签
* 移动文档

### 改进功能

* 清理未引用资源加上加载中示意
* 增大文件重命名界面宽度

### 修复缺陷

* Setext 风格标题编辑问题
* 加粗、强调、标记等软换行失效问题
* 块引用搜索展示不全
* 提示窗口关闭回调错误
* 列表项缩进问题
* 清理未引用资源报错问题

## v0.3.4 / 2020-10-23

### 引入特性

* 清理 assets 文件夹未引用资源文件
* 支持列表项块

### 改进功能

* 输入法符号自动补全时块引补全兼容
* 数学公式预览开关
* 反链提及中高亮锚文本

### 修复缺陷

* 自顶向下法新建文档后光标定位问题
* 自动更新在 macOS 和 Linux 上不工作的问题
* 资源文件删除失效问题
* 列表编辑解析问题

## v0.3.3 / 2020-10-21

### 引入特性

* 编辑器右键添加复制、粘贴、剪切
* 新建文档块自顶向下用法
* 自动更新
* Windows 版发布包增加 Zip 绿色解压版
* 在块的标识上增加单击后的功能【复制块内容】
* 选择文本右键选项“创建以此为标题的新笔记”

### 改进功能

* 优化启动速度
* 设置 - 关于中增加“打开配置文件夹”

### 修复缺陷

* 安装路径中有空格时启动异常

## v0.3.2 / 2020-10-20

### 引入特性

* 反链提及
* 思源在线（Xanadu）移动端添加 PWA

### 改进功能

* 设置 - 外观中增加“点击关闭按钮时的行为”
* 窗口 最大化/向下还原 切换时，分屏布局宽度高度自适应
* 书签、文件树、标签扩大折叠展开箭头的点击范围
* 内核进程增加常驻内存参数
* macOS 版使用 Windows 风格

### 修复缺陷

* 联动关系图页签关闭问题
* 标签自动完成问题
* 关闭程序后窗口布局持久化、清理 ID 失效问题
* 包含行内代码块的标题无法在大纲中正常渲染
* 编辑器内任务列表复制报错问题
* 导入时处理某些行级 HTML 导致的崩溃问题

## v0.3.1 / 2020-10-18

### 引入特性

* 标签输入自动完成
* 支持调整外观配色
* 退出时清理未使用的块 ID，减少对笔记文本的”污染“

### 改进功能

* 复制块 ID 后，增加粘贴成嵌入块选项
* 工具栏标题快捷键 Ctrl h 取消优化
* 搜索预览窗复制块 ID 和编辑器内复制块 ID 锚文本不一致
* 移动端文件树的滑动问题

## v0.3.0 / 2020-10-17

### 引入特性

* 支持删除云端仓库

### 改进功能

* 导出选项 - 块引锚文本包裹符号
* 启动加载界面
* 块引用悬浮预览窗口加入跳转按钮
* 搜索预览时支持复制内容块 ID

### 修复缺陷

* 联动关系图问题
* 导入时选择原地存储路径导致无限嵌套问题
* 在部分版本的 Windows 10 操作系统上界面样式异常问题
* 快速搜索最近打开的块中包含关闭笔记本的块
* 内容块中存在 `<font>` 时悬浮预览问题
* 标题内容中存在 `<font>` 时大纲点击失效问题

## v0.2.9 / 2020-10-14

### 引入特性

* 从云端仓库克隆笔记本
* 支持 flowchart.js 渲染
* 快速搜索改进并支持正则表达式
* 打开笔记本时新增导入流程

### 改进功能

* 同步后实时刷新文档
* #标签# 标记符隐藏

### 修复缺陷

* 重命名文件夹打不开问题
* assets 和新导入的 .md 版本控制问题
* 编辑器复制块引用 `>` 问题
* 修复云端仓库访问控制漏洞

## v0.2.8 / 2020-10-11

### 引入特性

* 移动端云服务
* 移动端加入同步支持
* 文件树空白可以新建，文件夹移上去添加更多和新建按钮

### 改进功能

* 启动时恢复上一次窗口大小和位置
* 文件树交互改进
* 更新账号信息需进行提示
* 初始化树组件异常报错
* 设置 - 同步
* 为页签添加背景色配置
* 为坚果云 WebDAV 做单独的优化

### 修复缺陷

* WebDAV 缺陷修复
* 文件查询失败
* YAML Front Matter 导入问题
* 文档开头为空块时关系图连线不显示问题
* 文件夹重命名为同名文件失败无提示
* 粘贴带有 alt 属性的图片后无法显示
* 批量插入图片问题

## v0.2.7 / 2020-10-08

### 引入特性

* 数据同步
* 文件树支持分屏打开文档

### 改进功能

* 内容联动大纲改进
* 快速搜索内容块预览关键词高亮
* 标签页签与书签页签内容块预览
* 解决知乎不支持引用嵌套
* 表格复制到知乎问题
* 细化编辑器标记符配色
* 添加启动日志开头标识
* 移动端浏览图片和块嵌入
* 抽取关系图配色值
* 降低内存占用

### 修复缺陷

* 打开空文件夹失败
* 联动编辑跳转的问题
* 全局关系图在某些情况下没有箭头的问题
* #标签# 中文空格问题
* 页签拖拽无限复制和拖拽至编辑器问题

## v0.2.6 / 2020-10-06

### 引入特性

* 支持 Windows 系统托盘
* 浏览器上直接使用
* 发布内核 Docker 镜像

### 改进功能

* 内容块引用自动完成列表条目优化
* Windows 版发布包改为安装程序

### 修复缺陷

* MathJax 引擎 `\boldsymbol{}` 问题
* WebDAV 报错导致内核崩溃
* 本地仓库初始化时获取 Git 用户报错
* 快速搜索报错

## v0.2.5 / 2020-10-02

### 引入特性

* 支持版本管理，本地 Git 仓库
* 快速搜索加入内容块预览

### 改进功能

* 修改主题 CSS 时即时呈现
* 内容块联动大纲
* 最大宽度设置优化
* 打开帮助文档优化
* 同时打开反链、关系图会报错
* 设置 - 语言合并到外观中作为一个选项
* 保存布局时，页签多语言失效
* 嵌入内容块能够被选中复制
* 废除软删除 .deleted 机制

### 文档相关

* 用户指南文档独立成库

### 修复缺陷

* 关闭笔记本时清理相关的页签

## v0.2.4 / 2020-09-29

### 引入特性

* assets 资源文件展现
* 窗口布局持久化
* 支持 asciinema 嵌入播放
* 引入 git 本地仓库

### 改进功能

* 关系图上显示文档块标题块文本
* 文档最大宽度设置
* 修改应用标题
* 重命名实时导出文档
* 嵌入块编辑优化

### 修复缺陷

* 搜索关键词不能定位问题
* 加粗渲染问题

## v0.2.3 / 2020-09-27

### 引入特性

* 标签页签，支持层级标签
* 搜索条件持久化

### 改进功能

* 粘贴图片使用时间戳作为文件名
* 笔记本打开仅支持文件夹
* ==标记==语法自动折行优化
* 移除中文分词，依靠输入时空格分词
* 降低内存使用

### 修复缺陷

* 任务列表起始位置使用 `<font>` 标签的预览问题
* 子列表中粘贴代码块问题
* 复制粘贴内容后搜索引用不到问题
* 任务列表嵌套缩进问题
* 网络图片引用失效
* 修改内容块后书签丢失问题
* 软删除重名问题

## v0.2.2 / 2020-09-25

### 引入特性

* 接入社区账号
* #标签 语法导入为 #标签#
* 本地文件支持使用 Ctrl + Click 打开

### 改进功能

* 反链列表中的引用块支持悬浮预览和关联高亮
* 内容块嵌入编辑体验优化
* 文件夹拖入使用绝对路径

### 文档相关

* 公开外观、文档仓库 

### 修复缺陷

* 新建文件后，文件树错落
* 同一文档中多次嵌入同一个块的问题
* 嵌入标题块时聚合内容块截断问题

## v0.2.1 / 2020-09-23

### 引入特性

* 自定义外观样式
* 内联 HTML 标签编辑渲染
* #标签# 语法支持

### 改进功能

* 关系图性能优化
* 内联 HTML `<br>` 不折叠渲染
* 嵌入块标识，不允许引用嵌入块
* 搜索和输入法兼容优化
* 引用代码块细节体验优化

### 文档相关

* 自定义外观文档

### 修复缺陷

* 列表内的公式块和列表文字之间空行问题
* 本地图片加载问题
* 强调、加粗和标记等软换行光标丢失问题
* 外观缓存问题
* `[[wikilink|text]]` 导入问题
* mermaid 问题修复

## v0.2.0 / 2020-09-21

### 引入特性

* 文档反向链接页签
* 联动文档关系图页签
* 联动大纲页签
* 联动反向链接页签
* 支持内容块嵌入语法 !((id "text")) 

### 改进功能

* MathJax 引擎 \boldsymbol{} 支持
* 代码块预览开关刷新编辑器
* 块嵌入渲染距离调整
* 搜索结果排序优化
* ==Mark== 必须使用两个等号
* 大纲隐藏 Markdown 标记符
* 新增一些快捷键

### 修复缺陷

* 书签自动收缩问题
* 用回退删除空行后面行被删除的问题
* 关系图节点闪烁
* 预览细节问题修复
* 紧凑列表变松散列表问题
* 关闭笔记本后关系图依然显示问题

## v0.1.9 / 2020-09-19

### 引入特性

* 添加代码块主题配置
* 添加代码块是否预览配置
* 搜索支持精确模式和中文分词

### 改进功能

* 按配置的导出选项进行预览
* 预览时显示内容块嵌入字数统计
* 打开笔记本后自动展开
* 优化导出选项，新增摘要 + 嵌入
* 调小编辑撤销可用时间间隔
* 创建文档/文件夹需要指定名称
* 搜索内容块结果按匹配长度升序
* 加载笔记本性能优化
* 新建文档改进

### 开发重构

* 外观组件化重构

### 修复缺陷

* 解决偶发的”列出笔记本 xxx 文件列表失败“问题
* 修复导出换行丢失问题
* 新建文档时文档块 ID 错误的问题
* 网络拉取图片渲染问题
* 引入 Block IAL 编辑器开发者工具报错
* HTML 块 ID 暴露问题
* 编辑块引用退格问题
* 公式块、代码块退格 ID 暴露
* 内联公式渲染撤销问题

## v0.1.8 / 2020-09-16

### 引入特性

* 页签拖拽插入分屏
* 迁移到 .md 格式

### 改进功能

* 粘贴图片默认放到文档同级 assets 文件夹下
* 编辑器内容最大宽度优化
* iframe 点击打开时使用默认浏览器
* 拖拽后编辑器窗口无法重置
* 拖拽线条进行高亮
* 优化大纲上存在块链时的显示

### 修复缺陷

* B 站视频嵌入代码问题
* 表格增减列、对齐快捷键问题
* 粘贴 `![foo](bar.png)` 图片时渲染问题

## v0.1.7 / 2020-09-11

### 引入特性

* 导出内容块引用可配置是否使用 Blockquote

### 改进功能

* 优化设置界面样式
* 支持文档（根块）书签
* 撤销后光标位置改进
* 拖拽页签到上下左右的小面板中应展开该面板
* 打开笔记本功能移到顶级导航
* 优化 (( 搜索条件为空时的性能

### 修复缺陷

* 汉语拼音中文自动空格问题
* 书签分屏报错
* 任务列表编辑问题
* =标记=中文自动空格问题

## v0.1.6 / 2020-09-08

### 引入特性

* 内容块书签
* 外观图标切换

### 修复缺陷

* 全局搜索打开定位问题
* ==Mark==首次加载渲染问题
* ECharts 图表编辑撤销问题

## v0.1.5 / 2020-09-08

### 引入特性

* 当前文档和文件树选中联动
* 实时导出 Markdown 文件
* 导出可配置是否启用全文引用

### 改进功能

* 内容块图标悬浮选中视觉优化
* 转义符 \ 渲染
* 内容块嵌入渲染自动刷新
* 内容块引用锚文本转义

### 修复缺陷

* 内联公式光标位置错误
* 图片不渲染的问题

## v0.1.4 / 2020-09-07

### 引入特性

* 全局关系图按引用数过滤
* 增加预览和关闭页签的快捷键

### 修复缺陷

* 报错后窗口关不掉问题
* 页签关闭产出空白问题
* 数学公式中出现中文不能渲染问题
* 列表块编辑后引用问题
* 图片渲染以及粘贴时从网络拉取图片问题

## v0.1.3 / 2020-09-06

### 引入特性

* 支持通过配置单击打开文档

### 改进功能

* 优化 (( 块引使用体验
* 内容块悬浮预览稍微延时
* 块搜索不进行限制
* 复制块 ID，粘贴时应进行自动完成
* 优化关系图节点大小
* 中文【【（（ 触发块引
* 代码块闪烁问题
* 设置面板从搜索面板中独立
* 触发搜索由 Double Shift 换成导航图标 Ctrl+p

## v0.1.2 / 2020-09-04

### 引入特性

* 标签页面右键添加关闭全部和关闭其他
* 页签拖拽可以插进到两个页签之间
* 支持“标题块”引用

### 改进功能

* 通过 [[ 触发内容块引用
* 块标识菜单对齐，添加表格块标识
* 标题变大变小快捷键及光标位置
* 标签页大小稍微调大几个像素

### 修复缺陷

* 导出列表块引用时异常问题
* 块链搜索不到的问题
* 复制块 ID 失效问题

## v0.1.1 / 2020-09-03

### 引入特性

* 支持连接 WebDAV

### 改进功能

* 优化块嵌入
* 数学公式块、代码块细节处理

### 修复缺陷

* 删除文件后文件树依然显示问题
* 白屏或者保存失败等界面无响应问题
* 悬浮预览图片问题
* 文档中的图片首次加载渲染问题

## v0.1.0 / 2020-08-30

你好，思源。
