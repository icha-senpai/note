## 概述

這一版，思源全面煥新，並朝著智慧化的方向更進一步。

### 重新設計的使用者介面

重新設計了使用者介面，整體更美觀，視覺層級更清晰，讓每次使用都賞心悅目。

### 行動端速記

靈感來時，長按應用圖示即可即開即寫。內容儲存為帶時間戳的筆記，儲存路徑可自訂，碎片想法不再溜走。

### 核心插件系統

插件常駐核心執行，作為單一資料來源，使多個視窗和實例之間始終保持一致，徹底告別多視窗場景下的資料衝突。

### 命令列介面

無需啟動思源即可直連核心資料層，可用於腳本與自動化任務，例如批次匯入匯出、定時處理、外部系統整合，一切盡在掌握。

### AI 知識庫

思源智能體與嵌入向量搜尋開始公開測試。用自然語言與筆記對話，用語意檢索管理知識，你的筆記從此會思考。

## 變更記錄

以下是此版本中的詳細變更。

### 引入特性

* 新的預設外觀圖示
* 手機端速記
* 全新的預設外觀主題
* 支援內核插件系統
* 命令列介面
* 新的設定介面
* 支援設定金鑰和變數

### 改進功能

* 支援跨文件撤銷
* 改進外觀底部停靠欄
* 支援在超級區塊中拖曳調整區塊寬度
* 依照字型檔自動調整編輯器字型字重
* 支援資料庫篩選器組合
* 合併頂部標題列和標籤列
* 資料庫項目支援 `建立副本`
* 改進包含合併儲存格的表格的貼上體驗
* 為 mermaid/graphviz 圖表新增縮放和平移選項
* 支援在行動端進行多塊選擇
* 改進資料庫日期欄位輸入格式
* 支援拖曳排序文件標籤
* 資料庫多重選取欄位值支援拖曳排序
* 在表格中撤銷編輯時保留水平捲動位置
* 支援在行動端切換手機介面和桌面介面
* 改進拖曳區塊時顯示的工具提示
* 資料庫頁尾欄位計算支援基於範本的計算
* 拖曳越過視窗後再捲動回來時選擇起點會發生偏移
* 改進工作區遺失後的啟動提示
* 滑鼠移離筆記本後工具提示不會隱藏
* 改進透過 `Alt+Enter` 在清單末端插入新項目的體驗
* 使用動態捲軸跳到底部時，內容應填滿視窗
* 新增 Markdown 匯出參數對話框
* macOS 上 GPU 使用異常
* 支援透過檔案名稱搜尋從資料快照返回檔案清單
* 桌面端檔案匯出不再依賴瀏覽器下載
* 不再向瀏覽器暴露絕對工作區路徑
* 改進空標題的邊緣情況
* 向編輯器貼上大量 `text/Scribli` 內容時，渲染進程不再崩潰
* 改進行級公式和圖片的複製，並使剪下行為與複製一致
* 改進字數統計
* 行動端檔案匯出不再依賴瀏覽器下載
* 表格中的鍵盤方向鍵導覽不應繞過合併儲存格
* 新增烏克蘭語支援
* 為 Linux 新增 rpm 發布套件
* 為搜尋新增標題和清單子類型篩選器
* 改進微信助理批次訊息的穩定性
* 改善資料索引穩定性
* 改進行級元素解析
* 支援在平板上拖曳
* 段落開頭為自訂表情時鍵盤導覽和 Delete 鍵失效
* 推送通知標題變更後視窗標題未能更新
* 支援在行動端使用 `跟隨系統鎖定畫面` 
* 支援將圖片從收集箱拖曳到編輯器
* 支援在行動裝置拖曳
* 改進嵌入區塊中標題層級的匯出
* 三指選取後無法立即複製
* 新增印地語支援
* 新增印尼文支援
* 新增荷蘭文支援
* 新增泰文支援
* 改進 Android 上的核心後台進程
* 新增遞歸折疊/展開塊功能
* 改進 IFrame 區塊
* 透過發布服務提供線上使用者指南
* 改進 PDF 匯出預覽介面
* 字型選單支援搜尋
* 將「存取授權碼」重新命名為「鎖定螢幕密碼」
* 避免資料倉庫清理過程中結束時的長時間等待
* 防止關閉 Markdown 標記後輸入時的樣式洩漏
* 改進刪除區塊時的編輯器狀態同步
* 改進區塊標
* 修復複製到微信公眾號時清單的左縮排問題
* 發布服務中停用插件時側邊欄布局錯亂
* 改進匯出預覽的 HTML 複製
* 嵌入區塊支援就地編輯
* 在套件卡片上將 data-obj 替換為 data-repourl，並從記憶體查詢資訊
* 本地 HTTPS + HTTP/2 支援
* 改進資料庫檢視效能
* 改進 HTML 瀏覽器剪藏擴充功能
* `取消儲存格合併` 時將 `<tbody>` 中的 `<th>` 轉換為 `<td>`
* 新增簡繁中文敏感度選項
* 改進剪下和貼上
* 支援在 iOS 上開啟使用者指南
* 在 Windows 上自訂 select 元素樣式
* 所有編輯器共享同一個 Lute 實例，以降低多分頁記憶體佔用
* 改進清單和超級區塊中的拖放體驗
* 區塊標支援在上方或下方快速插入區塊
* 側邊欄面板最小寬度未儲存到佈局，移動時無法維持寬度
* Windows 上資料同步因 DNS 問題失敗時，自動重新整理本地 DNS 快取並重試
* 簡化區塊標提示
* 桌面端支援以安全模式啟動

### 移除功能

* 不再支援直接將清單區塊插入到清單項區塊中

### 破壞性變更

* 核心服務現在需要明確使用 `serve` 子命令

### 修復缺陷

* 在聚焦區塊開頭按 Enter 會將新區塊插入到錯誤的位置
* 無外圍區塊的表格中方向鍵失效
* 行動端長按後「更多」選單項無法點擊
* Mermaid 圖表中的公式無法渲染
* 匯入 Markdown 後標籤頁自動關閉
* 啟動時缺少 `window.Scribli.storage` 會導致報錯
* 在搜尋視窗中為文件新增標籤時不正常運作
* 修復一些安全漏洞
* 在提示區塊內部刪除區塊引用後撤銷異常
* 隔離 reconnectWebSocket 的 ping，以在 CONNECTING 套接字下保持存活
* 當包含連續圖片的內容被設定為行級元素樣式時，圖片會消失
* 無法將筆記本拖曳到最末端
* 修復工作清單核取方塊切換問題
* 重新索引回退導致 sort.json 損壞
* 頁面載入完成前捲動會導致突然重新定位跳躍
* 反向連結面板中 Shift+Click 多選失效
* Android 上無法使用引用

### 文件

* 新增資料庫 API 端點文件
* 改進使用者指南
* 新增 `.sy` 格式規範文件

### 開發重構

* 升級至 Electron v42.5.0
* 不再建立資料表 `blocks_fts_case_insensitive`
* 升級至 highlight.js v11.11.2

### 開發者

* 提供用於匯出檔案的插件函式
* 破壞性變更：一些核心 API 的破壞性變更
* 從 API `/api/history/rollbackDocHistory` 端點移除 `notebook` 參數
* 改進 LocalStorage 相關 API
* `addDock` 允許在任意生命週期階段呼叫
* 插件安裝、解除安裝、啟用或停用後，全域快捷鍵註冊/註銷失效
* 改進 Linux 建置腳本
* 為 `/api/query/sql` 新增唯讀模式
* 新增核心 API `/api/lute/md2html`
* RTL 布局：為 `.protyle` 新增 `.rtl` 類別，並修復提示/區塊引用樣式
* 新增核心 API `/api/history/createDocHistory`
* 修復建構函式 super() 呼叫中的 this 轉義和上下文綁定問題
* 破壞性變更：棄用 updateTransaction 並替換為 updateTransactionElement
* 破壞性變更：修改 lang 值以符合 RFC 5646

## 下載

* Scribli contributors
* GitHub
