name: Docker Publishing Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Publishing disabled
        run: echo "Scribli Docker publishing is disabled until Scribli owns its release pipeline."
