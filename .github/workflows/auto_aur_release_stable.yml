name: AUR Publishing Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Publishing disabled
        run: echo "Scribli AUR publishing is disabled until Scribli owns its release pipeline."
